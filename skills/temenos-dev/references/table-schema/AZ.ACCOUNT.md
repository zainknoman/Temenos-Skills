# AZ.ACCOUNT — Table Schema

> Source: `INSERTS/I_F.AZ.ACCOUNT` in `AZ_Contract.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `AZ.CUSTOMER` | `AzAccount_Customer` | TField |  | Identifies the customer to whom the AZ account belongs. The value for this field is populated from the corresponding ACCOUNT record Validation Rules: 1-10 numeric customer code No input field |
| 2 | `AZ.CATEGORY` | `AzAccount_Category` | TField |  | Specifies the category to which the AZ account belongs. This field is populated from the corresponding ACCOUNT of the same ID. The category code in All in One module is used to group the AZ accounts under a Product code of the same category. A validation is done by the system to ensure that the category code of the AZ account and the AZ Product are the same. The category code is used to classify financial transactions in T24 according to the type of business operations or product type. The use of these codes with personal customer characteristics (like Residence, Status, Sector code etc.,) will enable the Banks to produce balance sheets and returns reflecting a coordinated and structured view of its operation, by directing business transactions to their appropriate place in the report. Validation Rules: 5-7 Numeric Codes It must be an existing code in the CATEGORY table For customer Accounts, it must be in the range of 1000 to 9999 Validated against the category code defined in AZ.PRODUCT.PARAMETER No input field |
| 3 | `AZ.CURRENCY` | `AzAccount_Currency` | TField |  | All entries passed to this account must be in this currency only. The value for this field is populated from the corresponding ACCOUNT record of the same ID. Validation Rules: Must be an existing code in the CURRENCY table 3 type SSS (upper case Alpha - like USD) or 1-3 numeric character currency code |
| 4 | `AZ.ALL.IN.ONE.PRODUCT` | `AzAccount_AllInOneProduct` | TField |  | Identifies the All in One Account Product, to which this AZ account belongs. The AZ account inherits the default features of the All in One product defined here, like penal rate, Repayment type, Maximum Interest only period, etc. However, at AZ account level also, changes are accepted to override the defaulted values. Certain validations like minimum term, maximum term, allowed category, maximum back date, minimum and maximum amount (in the case of deposit) are done at the AZ account, with respect to the values defined under the corresponding AZ Product. Validation Rules: Should be a valid ID in APP. The value cannot be changed after the account is authorised. |
| 5 | `AZ.PRINCIPAL` | `AzAccount_Principal` | TField |  | Defines the Principal amount of the loan or deposit account. Principal is one of the basis for calculating the repayment schedule in the case of loan account. In the case of Principal increase or decrease the schedule will be redefined based on the new principal amount. The Principal amount of a loan account is validated against the limit set for the account. Principal increase by way of pre defined draw downs can be done through B type of schedules. The drawdown amounts are validated against the Principal amount defined for the Loan account, so that the total draw down amount does not exceed the Principal. If the draw down type defined in APP as 'Full' and the draw down amount is not equal to Principal, an error is raised by the system. Similarly, if the draw down type is defined as 'PARTIAL' and draw down is done in full, an override is raised to that extent. If the repayment type is CREDIT CARD then for the AZ Main acct this field will be '0.00'. For the CREDIT CARD type sub accts this field is filled by the amount of draw down made. Whenever repayments are made the value in this fields is reduced accordingly till it becomes zero. Validation Rules: Standard amount field. Negative values not permitted. Input can be 0.00 if the repayment type is Credit Card and the Az acct is a main acct. In case of Credit card type of repayment for a Sub acct the value in this field diminishes as the repayment starts. Nochange field. |
| 6 | `AZ.ORIG.PRINCIPAL` | `AzAccount_OrigPrincipal` | TField |  | When the AZ Main account record is committed, the amount entered in the principal field will be automatically populated to this field. For an AZ main a/c this field will contain only 0.00. For AZ sub a/cs it contains the FIRST draw down amount. Validation Rules: : A no input field. Populated from the Principal field |
| 7 | `AZ.VALUE.DATE` | `AzAccount_ValueDate` | TField |  | Defines the date from which the loan/ deposit account is effective. Any date entered earlier than the business date, is validated against the MAX.BACK.DATE defined under the respective AZ Product parameter. The future date entered here cannot be greater than the Forward Maximum Date defined under DATES. If PROD.START.DATE or PROD.END.DATE is defined in APP then it is validated against it. Validation Rules: Standard date format. Default is current Business Date |
| 8 | `AZ.MATURITY.DATE` | `AzAccount_MaturityDate` | TField |  | Defines the maturity date of the loan / deposit account of All in One Product. The date is validated against the maximum term and minimum term defined in the AZ Product Parameter of the account. The date cannot be less than the business date/ Value date. If MULTI is set to 'Y' then for a CREDIT CARD type of loan or FIXED type of deposit this field should be left BLANK and the maturity date will be set in the sub accounts that are created. Validation Rules: Standard Date format Can accept values of period like number of days, months (example: 12D, 60M) also, for calculating Maturity Date. |
| 9 | `AZ.ORIG.MAT.DATE` | `AzAccount_OrigMatDate` | TField |  | For future use. |
| 10 | `AZ.INTEREST.RATE` | `AzAccount_InterestRate` | TField | No | This field is valid for products with FIXED RATE of interest. Rate of interest mentioned in the APP gets defaulted here. User can change the defaulted value, if needed. Validation Rules: Optional Field |
| 11 | `AZ.PENAL.RATE` | `AzAccount_PenalRate` | TField |  | Defines the penal rate of interest that can be applied on the value (like arrears) defined under PENALTY.ON field of the AZ product. At present, the penal rate is applied on the arrears amount only. The penal interest rate is defaulted to AZ loan account, from the AZ Product Parameter. However, the value can be changed at account level also, in case of need. Validation Rules: Rate type, numeric value Maximum value 100 Negative values not accepted |
| 12 | `AZ.TAX.E.I` | `AzAccount_TaxEI` | TField |  | Specifies whether the calculation of Tax on the Tax basis is Inclusive of Tax (I) or Exclusive of Tax(E) or no Tax (N). For example if Tax is on Interest and I (Inclusive ) is mentioned here then the calculated interest amount in the schedule includes the tax amount also. If E (Exclusive) is mentioned, then the interest amount does not include the tax portion and the tax amount will be shown as additional. If 'N' is defined here, no tax will be calculated. Validation Rules: Valid values are ' I ', 'E' or 'N' |
| 13 | `AZ.TAX.ON` | `AzAccount_TaxOn` | TField |  | This field indicates the amoun ton which the tax is to be calculated. Currently allowed value is 'I' (Interest). Allowed values are I or ''' |
| 14 | `AZ.TAX.KEY` | `AzAccount_TaxKey` | TField |  | This field is used to define the tax code, to be applied on the tax component like Interest. There are two ways of specifying the tax code here. One way is to enter the code from the TAX file. If any valid code already existing in TAX file is entered here, the relevant information will be picked up from the TAX file, for calculation purposes. Another way is to specify a valid code from TAX.TYPE.CONDITION file. In this case, the system first reads the CUSTOMER.CHARGE table and then based on the charge group of the customer, picks up the relevant tax record from TAX.TYPE.CONDITION. To distinguish the code of TAX.TYPE.CONDITION from the TAX file, the code is to be entered by prefixing ' * '. For example, if ' *VAT' is entered here, it will pick up the relevant code from the TAX.TYPE.CONDITION file. A code entered like '10' (without *), will refer TAX file and pick up the record relating to code 10. Validation Rules: Should have been already defined as a valid record in TAX / TAX.TYPE.CONDITION file. |
| 15 | `AZ.INTEREST.LIQU.ACCT` | `AzAccount_InterestLiquAcct` | TField |  | Identifies the customer account to which the interest for the AZ account is to be booked. This field is used only when interest is to be posted to a customer account, which is different from this AZ account. The value is defaulted from the ACCOUNT record, if it is already defined there. If no account is defined in this field, then the interest will be capitalised in the AZ account itself.( The ID account) Once an account becomes a loan az.account then this cannot be changed in the account. If an account becomes a deposit az.account then it is possible to change the account if already defined to another account, but if not defined then it cannot be defined and cannot be made null if already defined. Validation Rules: 2-14 numeric character customer account number or 3-10 type MNE Mnemonic It must be an existing Customer account |
| 16 | `AZ.REPAY.ACCOUNT` | `AzAccount_RepayAccount` | TField | Conditional | Defines the Customer account or the Internal account in T24, from which the repayment amount for AZ loan account is debited. Validation Rules: Optional Input Accepts existing valid Customer account or Internal Account In loans if PD.LINK.TO.AZ is defined as 'Y' then this field is mandatory. In deposits if BC schedule is to be defined then this field is mandatory. AZ.ACCOUNT not allowed. |
| 17 | `AZ.REPAY.STO.ACCT` | `AzAccount_RepayStoAcct` | TField |  | Defines the Debit account number for raising standing order ( STO ). On scheduled dates of the loan, STO of AC type will be generated for debiting the account defined here and crediting the AZ loan account for the scheduled amount. On reschedule if the Repayment amount changes, the STO amount also gets suitably altered. On due date of the loan or on pre-closure, STO is moved to history. For Nostro accounts defined under this field MT104 will be generated in advance based on the days defined in DAYS.BEF.EVENT Validation Rules: Allows only a valid Nostro or Internal account |
| 18 | `AZ.REP.INT.BANK` | `AzAccount_RepIntBank` | TField |  | Identifies the intermediary Bank through which the All in One repayment related funds passes. As there is minimum validation for this field, care must be taken by the user while entering free text in this field. If nothing is entered in this field, will be defaulted automatically as per the auto routing instruction on the Agency file. Validation Rules: 3-10 character Customer Mnemonic Type MNE or 1-6 numeric character customer code, or 35 character text |
| 19 | `AZ.REP.BEN.BANK` | `AzAccount_RepBenBank` |  |  |  |
| 20 | `AZ.REP.BEN.ACCT` | `AzAccount_RepBenAcct` | A (alphanumeric) | No | The Account details (number or name) of the ultimate beneficiary to be debited for the repayment amount. This field concerns transfers where the customer is requesting the funds from outside the branch. In the case where the customer is able to provide the branch with account details, such information may be entered in this field for use in the Receipt function. This field may be amended at any time during the life of the contract. 'Autorouting of payments' Standard payment details held on the Agency table for the contract customer will be used in this field when payment is required, only if no input is made by the user. Validation Rules: 1-20 type A (alphanumeric) characters. (Optional input.) |
| 21 | `AZ.NOMINATED.ACCOUNT` | `AzAccount_NominatedAccount` | TField | Conditional | Defines the Customer account or the Internal account in T24, to which the loan draw down amounts are credited. If no nominated account is defined, then the system will create a TELLER.DEFAULT for the draw down amount. Validation Rules: Optional Input In deposits if MATURITY.INSTR is set to 'Payment to Nominated Account' then this field is mandatory. AZ.ACCOUNT not allowed. |
| 22 | `AZ.NOM.INT.BANK` | `AzAccount_NomIntBank` | TField |  | Identifies the intermediary Bank through which the All in One nominated account funds ( transactions like credit of draw down amount) must pass prior to receipt by the Beneficiary Bank or account with Bank. As there is minimum validation for this field, care must be taken by the user while entering free text in this field. If nothing is entered in this field, will be defaulted automatically as per the auto routing instruction on the Agency file. Validation Rules: 3-10 character Customer Mnemonic Type MNE or 1-6 numeric character customer code, or 35 character Multi value field |
| 23 | `AZ.NOM.BEN.BANK` | `AzAccount_NomBenBank` |  |  |  |
| 24 | `AZ.NOM.BEN.ACCT` | `AzAccount_NomBenAcct` | A (alphanumeric) | No | The Account details (number or name) of the ultimate beneficiary to be credited with the Principal Amount. This field concerns transfers where the customer is requesting the delivery of funds outside the branch. In the case where the customer is able to provide the branch with account details, such information may be entered in this field for use in the Payment and Receipt function. This field may be amended at any time during the life of the contract. 'Auto-routing of payments' Standard payment details held on the Agency table for the contract customer will be used in this field when payment is required only if no input is made by the user. Validation Rules: 1-20 type A (alphanumeric) characters. (Optional input.) |
| 25 | `AZ.CHARGE.KEY` | `AzAccount_ChargeKey` | TField |  | Defines the charge and overrides applicable to the account. The codes are drawn from GENERAL charge records. Validation Rules: 1-4 numeric character General Charge Code There must be a valid record already defined in the GENERAL.CHARGE table for this code. |
| 26 | `AZ.DRAWDOWN.AMOUNT` | `AzAccount_DrawdownAmount` | TField |  | Defines the draw down amount for Loan accounts. The type of draw down is defined at the AZ Product level. Three types of draw down are, Full, Partial with no interest payment and Partial with Interest payment. The amount of draw down defined here is validated against the type defined in the AZ Product Parameter. If a nominated account is defined, the system will automatically transfer the drawdown amount on the specified drawdown date to the nominated account. If no nominated account is defined and if the drawdown is preferred through the AZ account itself, then a teller default will be created and the amount can be drawn through teller using the teller default feature. Validation Rules: Standard Amount field Negative values not accepted Valid for AZ Loan accounts only Validation is done with respect to the Principal |
| 27 | `AZ.AMOUNT.V.DATE` | `AzAccount_AmountVDate` | TField |  | Determines the value date for the draw down amount. Validation Rules: Standard Date format Input is allowed only if draw down amount is defined The date entered here cannot be less than the value date of the account. If it a back date then the date cannot be lesser than or equal to any schedule date. For example, Assume that we have scheduled repayment on 30th of every month and last due was on 30 Aug . We made online repayments on 5th &amp; 10th of Sep. Bank date is 12th Sep. Annuity loans: It is possible to set AMOUNT.V.DATE from 31st Aug onwards .In case REDUCE.LIMIT is NO, then it is possible do drawdowns to the extent of amount repaid till bank date. Non-annuity loans: AMOUNT.V.DATE is allowed only from 11th onwards. |
| 28 | `AZ.DD.INT.RATE` | `AzAccount_DdIntRate` | TField |  | When draw down is made through AZ.ACCOUNT using the draw down amount field the user is given the flexibility to input a new rate if desired which will OVERRIDE all the other inputs made on interest rate. This new rate will be assigned to that particular Sub AZ account. It will be cleared after the particular withdrawal, so that next withdrawal rate can be a different or the default one. While making a draw down if this field is left blank, then the rate of interest input in the APP is defaulted, unless it is changed in the account level. Input can also be in the format 'S+nn' or 'S-nn', where 'nn' is a number. Based on the input, the interest rate that is applied on the sub account during draw down will be arrived by either adding or subtracting from the existing interest rate. Validation Rules: Input permitted 1 to 100 Input in the format 'S+nn' or 'S-nn'. Allowed only for CREDIT CARD type products. |
| 29 | `AZ.REDEMPTION.AMT` | `AzAccount_RedemptionAmt` |  |  |  |
| 30 | `AZ.SUB.AZ.ACCT` | `AzAccount_SubAzAcct` |  |  |  |
| 31 | `AZ.ADDITIONAL.SPREAD` | `AzAccount_AdditionalSpread` | TField |  | This field specifies the additional spread that is to be added or subtracted from the interest rate at the time of sub account creation of Multi- Fixed deposits or rollover. Input can be a local routine, e.g., '@LOCAL.ROUTINE(ACCT.NO, ADDITIONAL.SPREAD)'. The local routine needs to be defined in the PGM.FILE. Input can be 'S+nn' or 'S-nn', where 'nn' is the additional spread that has to be added or subtracted from the interest rate. Allowed only for deposits. |
| 32 | `AZ.CHG.FROM.RED.AMT` | `AzAccount_ChgFromRedAmt` | TField |  | Applicable in case of EARLY/PART redemption of Deposits. Permitted inputs YES or NO. Option YES means-the early redemption charges and differential interest if any will be taken from the Nominated account. If NO then Early redemption charges are applied to the remaining Principal . For ex:-&gt; Total deposit is 10000 and if 3000 is early redeemed and 7000 is renewed. When the column is YES then 3000 MINUS the charge and differential interest if any is credited to the nominated account and if NO, then the charge amount and the differential interest will be deducted from remaining amount. I.e. remaining principal will be 7000 MINUS charges. Validation Rules: Permitted inputs are YES or NO. |
| 33 | `AZ.EARLY.RED.INT` | `AzAccount_EarlyRedInt` | TField | No | Specifies the PENAL rate applicable in case of EARLY/Part withdrawl. Defaulted from AZ Product Parameter. The inputter can alter the defaulted rates. Validation Rules: Optional field Allowed only for deposits. |
| 34 | `AZ.WAIVE.CHG` | `AzAccount_WaiveChg` | TField | Yes | Permitted inputs YES or NO. Charges are not collected if this field is 'YES' and collected if this field is NO. Validation Rules: 1.Permitted values YES or NO Mandatory field |
| 35 | `AZ.CHARGE.CODE` | `AzAccount_ChargeCode` |  |  |  |
| 36 | `AZ.CHARGE.CCY` | `AzAccount_ChargeCcy` |  |  |  |
| 37 | `AZ.CHARGE.AMOUNT` | `AzAccount_ChargeAmount` |  |  |  |
| 38 | `AZ.CHARGE.AMT.LCY` | `AzAccount_ChargeAmtLcy` |  |  |  |
| 39 | `AZ.CHARGE.DATE` | `AzAccount_ChargeDate` |  |  |  |
| 40 | `AZ.CHG.LIQ.ACCT` | `AzAccount_ChgLiqAcct` |  |  |  |
| 41 | `AZ.CHARGE.IMMED` | `AzAccount_ChargeImmed` |  |  |  |
| 42 | `AZ.CHG.PL.ACCT` | `AzAccount_ChgPlAcct` |  |  |  |
| 43 | `AZ.TAX.CODE` | `AzAccount_TaxCode` |  |  |  |
| 44 | `AZ.TAX.AMOUNT` | `AzAccount_TaxAmount` |  |  |  |
| 45 | `AZ.TAX.AMT.LCY` | `AzAccount_TaxAmtLcy` |  |  |  |
| 46 | `AZ.TAX.ACCT` | `AzAccount_TaxAcct` |  |  |  |
| 47 | `AZ.SCHEDULES` | `AzAccount_Schedules` | TField | No | Determines whether schedule for the AZ loan / deposit account is to be defined or not. If 'Y' is given, further fields relating to schedule will be available to the user for defining various types of schedules. If 'N' is given, schedule will not be permitted. Validation Rules: Optional field Valid options are, 'Y', 'N' or '' Default is taken as 'Y' |
| 48 | `AZ.REPAYMENT.TYPE` | `AzAccount_RepaymentType` | TField |  | Defines the Repayment type of the AZ Account. The value for this field is defaulted from the corresponding AZ Product Parameter, after the schedule is opted. However, this can be modified at AZ account level also. The repayment type determines the way in which the loan/ deposit account is to be repaid. Refer to APP for details. |
| 49 | `AZ.CALCULATION.BASE` | `AzAccount_CalculationBase` | TField |  | Defines the basis for calculation of repayment amount for the term of the loan or for recalculation for the remaining term. Available options are, Principal, Scheduled Balance and Current Balance. Currently allowed values are, If 'Principal' is defined, entire Principal amount will be taken into account, for deriving the repayment amount and interest calculations, irrespective of the amount of Principal drawn. If 'Schedule Balance' is defined, the amount drawn down till date will be taken into account for repayment amount and interest calculation purposes. If SCHEDULES is set to 'N' then allowed value is 'Principal' else 'Schedule Balance'. |
| 50 | `AZ.INT.ONLY` | `AzAccount_IntOnly` | TField |  | This field is used to define whether 'interest only repayment' is allowed for this account or not. If Y (Yes) is defined, the AZ loan account shall be able to have Interest Only repayment facility If N (No) is defined, the account shall not be able to have the Interest Only repayment facility. The value is defaulted from the corresponding AZ Product Parameter. Validation Rules: The permitted values are 'Y' or 'N' Valid only for Loan products Default is 'N' |
| 51 | `AZ.MAX.INSTL.INT.ONLY` | `AzAccount_MaxInstlIntOnly` | TField |  | Total period of time that the interest only repayment could be defined for a loan during the life time of a loan account. If input is given in numbers it is treated as number of days. If input is given prefixing P, it is treated as number of installments. The value is populated to this field from AZ product Parameter. Example: If input is given as 60, it implies that the total number of Interest Only repayment period cannot exceed 60 days during the life of the loan account. If input is given as P60, it implies that the total number Interest only repayment period cannot exceed 60 installments (of any frequency like daily, monthly) during the life of the loan account. Validation Rules: Value for this field will be accepted only if 'INT ONLY' field is defined as Y Valid only for Loan Product Cannot have negative values |
| 52 | `AZ.RESIDUAL.PRINCIPAL` | `AzAccount_ResidualPrincipal` | TField |  | Specifies the residual (remaining) amount of Principal that can be paid at the maturity date of the AZ loan account. Repayment schedule for the loan account is generated for the Principal amount after deducting the Residual portion. This is allowed only for 'PRINCIPLE' and 'SCHEDULE BALANCE' as the calculation base. It is not allowed for NON REDEMPTION and OTHER types of loan. As in OTHER type of loan the system will ask for the P sch amount to be input. The drawdown amount should be greater than the residual amount. Example: Principal amount : 100,000 Residual Principal: 25,000 Repayment schedule with different installment frequency will be generated based on 75,000 only. Residual amount 25,000 will be collected on the date of maturity. Once the Residual Principal is defined, it cannot be changed subsequently. Validation Rules: Standard amount field Input must be less than the Principal amount. |
| 53 | `AZ.TERM.PRIORITY` | `AzAccount_TermPriority` | TField |  | Defaulted from APP. Can be changed at az.account level. Refer to APP for details. |
| 54 | `AZ.ROLLOVER.AMT` | `AzAccount_RolloverAmt` | TField |  | The amount that has to be deducted from or added to the final maturity value of the deposit for further rollover. The amount specified in the field may be LESS, EQUAL or MORE than the deposit. In case the amount of roll over is more than the deposit amount- the system takes the difference between the deposit amount and the maturity amount from the REPAY account. In case the balance in the repay account is not sufficient to supplement the addition to the deposit - then the account is moved to AZOD category and the exception log is updated i.e. the instruction to rollover the deposit is IGNORED and the AZ is matured. In case of loans, the difference between principal amount and balance in the loan account is credited to the nominated account. If the rollover amount is for a value less than the deposit maturity value, then the difference is credited to the Nominated account. In case of loans, the difference between principal amount and balance in the loan account is debited from repay account. In case balance in the repay account is not sufficient to supplement the addition to the principal amount, then based on PD.LINK.TO.AZ field in APP the oustanding balance amount will be debited. If PD.LINK.TO.AZ is 'YES' then PD will be debited. If PD.LINK.TO.AZ is 'NO' then AZ.OVERDUES will be updated. If PD.LINK.TO.AZ is 'NULL' then FT on hold will be raised. For reducing the rollover amount, minus sign is to be prefixed with the amount specified under this field. For increasing the rollover amount, the amount by which it is to be increased must be specified under this field. Example: Maturity value of a deposit = 100200 If the user wishes to rollover for an amount of 60000, then -40200 is to be entered in this field. If the user wishes to renew for 150000, then 49800 is to be entered here. Validation Rules: Rule 1: ""-"" sign with amounts to be used in case of decrease in rollover amount. For increase in rollover amount simply the amount need to be mentioned without ""+"" sign. |
| 55 | `AZ.ROLLOVER.TERM` | `AzAccount_RolloverTerm` | TField |  | This field specifies the PERIOD for which the Rollover amount will be REINVESTED in the deposit account. Incase of loans the loan account will be extended for the period in this period. Validation Rules: 1.Permitted values:- D (Days), W (Week), M (month) and Y Year). |
| 56 | `AZ.ROLLOVER.INT.RATE` | `AzAccount_RolloverIntRate` | TField | No | Specifies the RATE OF INTEREST applicable for the rollover period. If undefined, then the rate of interest of the existing contract is taken by default. Only fixed interest rates are handled incase of loans. Validation Rules: Rule 1: Optional field. |
| 57 | `AZ.ROLLOVER.DATE` | `AzAccount_RolloverDate` | TField |  | The field defines the date on which the rollover has happened. Validation Rules: Rule 1:-NO INPUT field. |
| 58 | `AZ.FINAL.MATURITY` | `AzAccount_FinalMaturity` | TField |  | The date mentioned in the field is the maximum period up to which the ROLLOVER can be EXTENDED. I.e. this is the date when the deposit / loan FINALLY matures and should be credited / debited to the account or dispersed as per the customer directions. Validation Rules: Accepts date only. |
| 59 | `AZ.FORWARD.BACKWARD` | `AzAccount_ForwardBackward` | TField |  | Defines the method followed by the system, when the derived date of the generated P, B, N and C schedule, is a non working day. The various methods are: 1. Forward: The system will go forward to the next working day. 2. Backward: The system will go back to the last working day. 3. Fwd Month: The system will go forward to the next working day provided it is within the same month. If it is not within the same month, the system will go backward to the last working day. 4. Calendar: The system will use the same date, irrespective of whether it is holiday or not. This field is not applicable for maturity date. Validation Rules: Valid values are 1 Forward 2 Backward 3 Fwd Month 4 Calendar. |
| 60 | `AZ.TYPE.OF.SCHDLE` | `AzAccount_TypeOfSchdle` |  |  |  |
| 61 | `AZ.AMT.PERCENT` | `AzAccount_AmtPercent` |  |  |  |
| 62 | `AZ.AMOUNT` | `AzAccount_Amount` |  |  |  |
| 63 | `AZ.FREQUENCY` | `AzAccount_Frequency` |  |  |  |
| 64 | `AZ.NUMBER` | `AzAccount_Number` |  |  |  |
| 65 | `AZ.RATE.SCH.KEY` | `AzAccount_RateSchKey` |  |  |  |
| 66 | `AZ.RATE.SCH.SPREAD` | `AzAccount_RateSchSpread` |  |  |  |
| 67 | `AZ.RATE.SCH.OPERND` | `AzAccount_RateSchOpernd` |  |  |  |
| 68 | `AZ.RATE.SCH.PERCNT` | `AzAccount_RateSchPercnt` |  |  |  |
| 69 | `AZ.SCH.FIXED.RATE` | `AzAccount_SchFixedRate` |  |  |  |
| 70 | `AZ.PRD.RATE.KEY` | `AzAccount_PrdRateKey` |  |  |  |
| 71 | `AZ.PI.METHOD` | `AzAccount_PiMethod` |  |  |  |
| 72 | `AZ.TYPE.B.SYS` | `AzAccount_TypeBSys` |  |  |  |
| 73 | `AZ.SL.CHARGE.CODE` | `AzAccount_SlChargeCode` |  |  |  |
| 74 | `AZ.SL.CHG.BASE.AMT` | `AzAccount_SlChgBaseAmt` |  |  |  |
| 75 | `AZ.SL.CHG.LIQ.ACCT` | `AzAccount_SlChgLiqAcct` |  |  |  |
| 76 | `AZ.RES.4` | `AzAccount_Res4` |  |  |  |
| 77 | `AZ.RES.5` | `AzAccount_Res5` |  |  |  |
| 78 | `AZ.FINAL.DD.IND` | `AzAccount_FinalDdInd` | TField |  | Indicates whether the final draw down in the case of AZ Loan account is over or not. By defining 'Y' here, it is implied that the final draw down of loan amount is over an no more draw down could be made. When draw downs are made by using the AZ account, or through B schedules, when the total draw down amount is equal to Principal amount, the system automatically updates the final draw down indicator as 'Y'. If the Bank wishes to restrict the draw down to a certain amount, even if it is less than the sanctioned Principal amount, then this field can be filled with a value as 'Y'. After the final draw down is over, and this indicator is 'Y', Banks can use POSTING RESTRICTION to restrict any debit transaction like Redraw, in AZ loan accounts. Validation Rules: Valid values are 'Y' or 'N' Not allowed for CREDIT CARD type of products and revolving limt setup in loans. |
| 79 | `AZ.PRE.CLOSURE.IND` | `AzAccount_PreClosureInd` | TField |  | Indicates whether pre closure of the loan / deposit account is opted or not. If 'Y' is defined here, the system will inform the users the total amount due in the account, inclusive of interest. Based on these details, decisions can be made to proceed further in closing the account or not. Validation Rules: Valid values 'Y' or ' ' (Null). |
| 80 | `AZ.DAYS.BEF.EVENT` | `AzAccount_DaysBefEvent` | TField |  | Defines the number of days before which the delivery should be made. This field is used for generating MT104 advice in advance to the Beneficiary bank for the debit to be made in the Nostro account by STO. Validation Rules: Default will be from the CURRENCY table relating to ACCOUNT currency. |
| 81 | `AZ.DEL.CARRIER` | `AzAccount_DelCarrier` | TField |  | This field gives the type of delivery carriers available for delivery of messages, transfers etc. Input in this field should be a valid record in DE.CARRIER application. SWIFT, TELEX, SIC are some of the valid inputs. Validation Rules: Input in this field should exist in the DE.CARRIER. |
| 82 | `AZ.MESSAGE.CLASS` | `AzAccount_MessageClass` |  |  |  |
| 83 | `AZ.ACTIVITY` | `AzAccount_Activity` |  |  |  |
| 84 | `AZ.DELIVERY.REF` | `AzAccount_DeliveryRef` |  |  |  |
| 85 | `AZ.LOCAL.REF` | `AzAccount_LocalRef` |  |  |  |
| 86 | `AZ.REVOLVING.RATIO` | `AzAccount_RevolvingRatio` | TField |  | Defaults from APP. User can change it. Refer to APP for details. |
| 87 | `AZ.REPAY.AMT` | `AzAccount_RepayAmt` | TField |  | In loans when repayment has to be affected in the loan a/c directly through AZ account, instead of using other applications like TELLER, then this field is used. The effect of this repayment is ONLINE. When used the LOAN.REPAY txn code is used in the entry being raised. In deposits the deposit credit amount is input here. The DEPOSIT.CR txn code will be used in the entry being raised. VALIDATIONS Numeric input depicting amount Negative values not allowed. |
| 88 | `AZ.REPAY.V.DATE` | `AzAccount_RepayVDate` | TField |  | The effective date of Repay amount is specified here. When repaying the loan the date can be only today. If PD.LINK.TO.AZ is 'Y', the PD created will have this date as its value date. If this is a back date then the repay amount will be treated as back dated principal repayment instead of loan repayment . For credit cards with CREATE.PD.EOD as NO, if the actual payment is received only X days and if PD is created by the system, then the PD shall use this date as its value date. Backdated REPAY.V.DATE is not applicable if CREATE.PD.EOD is YES. Future date not allowed. VALIDATIONS Enter valid date format. |
| 89 | `AZ.MAT.DATE` | `AzAccount_MatDate` | TField |  | This field is used to specify the maturity date for the fixed deposits that are being created from AZ.ACCOUNT where MULTI is set to 'Y'. Must be greater then TODAY. Allowed for FIXED deposits. |
| 90 | `AZ.CANCEL.LAST.DD` | `AzAccount_CancelLastDd` | TField |  | In CREDIT CARD type of loan, draw down is made through draw down amount field. If such a draw down is to be cancelled for any reason then this field should be flagged as 'Y'. If the draw down is done through any other application (and not through Draw Down amount field in AZ) and subsequently if this field is flagged as 'Y' then the last draw down made through AZ account will be deleted. For ex: There are 3 drawls made through AZ, (1) 200000 (2) 550000 (3) 750000 A fresh drawl (4) is made through say teller for 650000. Now if this field is input with a 'Y' then the drawl (3) i.e. 750000 will be REVERSED in place of the last drawl (4). To recap drawl through any other application should be done through REVERSAL of the application and not through this field. VALIDATIONS Valid input is 'Y'. Input not allowed with INT.ADJ.AMT set. |
| 91 | `AZ.CC.PR.GRACE.PERIOD` | `AzAccount_CcPrGracePeriod` | TField |  | The value in this field is populated from AZ product parameter. User can alter the input manually. Input in this field indicates that the type is NON-redemption and the generation of CC schedules will be postponed until such time. Input in this field is numeric but should be read along with the frequency input in the card issue. If value entered here, are 3 and the frequency in card issue is Monthly, it means generation of 'CC' schedules will be postponed by 3 months. If the frequency is weekly then it means generation of CC schedules will be postponed by 3 weeks. VALIDATIONS Input allowed is numeric. Input should be read along with the frequency entered in card issue. Allowed for CREDIT CARD type of products. |
| 92 | `AZ.RATE.CHG.SUB.ACC` | `AzAccount_RateChgSubAcc` | TField |  | Input here can be 'Y' or Null If input as 'Y', it is to be understood that change in rate of interest will be applicable not only to the new drawls but also the existing ones. For the existing drawls, the changed rate will be applicable from the date such change becomes effective. For ex: Loan draw down is 10.11.2002. Applicable rate is 10%. If effective from 10.06.2003 this rate has been changed to, 14%, then (1) Any draw down made on or after 10.06.2003 will attract 14%. (2) For the existing draw downs the interest rate will be 10% up to 09.06.2003 and 14% from 10.06.2003 If the field is null, then only the future drawdowns will attract the changed rate while the existing one will continue to be ruled by the earlier rate. VALIDATIONS Input may be 'Y' or Null Allowed for CREDIT CARD type of products. Allowed in master account. |
| 93 | `AZ.CREDIT.AMOUNT` | `AzAccount_CreditAmount` | TField |  | This field defines the amount the periodic credits that are allowed in SAVINGS PLAN. This field should be read along with the field CREDIT.AMT.MULTI in APP. The amount that is input here will be validated against the B schedule amount. If CREDIT.AMT.MULTI is defined as 'YES' then the amount input in B schedules should be in multiples of the amount input here. Example, Case (1) CREDIT.AMT.MULTI is set as Yes and this field is null, then the "B" schedule amount can be input with any amount. Case (2) CREDIT.AMT.MULTI is set as Yes and this field is input with say 5000 then "B" schedule amount can be 5000 or multiples of 5000, say 10000, 15000 etc. But "B" schedule amount can't be LESS than the the value defined here. Case (3) CREDIT.AMT.MULTI field is '' and this field is input as 7000, then "B" schedule amount should be 7000 ONLY and even multiples of 7000 are not permitted. Validation Rules: Allowed only for SAVINGS PLAN type of deposit product. Allowed only if ACCT.SYNC is set to 'YES'. |
| 94 | `AZ.REDUCE.LIMIT` | `AzAccount_ReduceLimit` | TField |  | Defaulted from APP. Can be changed at az.account level. Refer to help in APP. |
| 95 | `AZ.CONSOL.KEY` | `AzAccount_ConsolKey` | TField |  | For future use. |
| 96 | `AZ.LAST.DAY.INCL` | `AzAccount_LastDayIncl` | TField |  | Defaulted from APP. Can be changed at az.account level during creation. Once defined cannot be changed. Refer to help in APP. |
| 97 | `AZ.MATURITY.INSTR` | `AzAccount_MaturityInstr` | TField |  | Defaulted from APP. Can eb changed at az account level. Refer to help in APP. |
| 98 | `AZ.GRACE.PERIOD` | `AzAccount_GracePeriod` | TField |  | Defaulted from APP. Can be changed at az.account level. Refer to help in APP. |
| 99 | `AZ.ORIG.INTEREST.RATE` | `AzAccount_OrigInterestRate` | TField |  | Original Interest rate of the contract at the time its Opening Validation Rules: NOINPUT field |
| 100 | `AZ.INT.ADJ.AMT` | `AzAccount_IntAdjAmt` | TField |  | For ANNUITY Contracts, this field has to be used if Interest adjustment is to be affected directly through AZ.ACCOUNT . If Repayment account is present, then the system shall debit AZINTADJ Suspense account for the interest adjustment amount and credit repayment account. Other wise Teller default shall be created. Amount debit from suspense shall be recovered back along with interest, during subsequent Online/ scheduled repayment, when 'IN' component is defined in AZ.SETTLEMENT.PRIORITY. VALIDATIONS Numeric input depicting amount Negative values not allowed. |
| 101 | `AZ.RESERVED.01` | `AzAccount_Reserved01` | TField |  |  |
| 102 | `AZ.LOAN.INT.ADJUST` | `AzAccount_LoanIntAdjust` | TField |  | This field is used to decide whether interest adjustment is allowed or not in an ANNUITY or CREDIT-CARD type AZ loan account. Refer helptext of AZ.PRODUCT.PARAMETER for further details |
| 103 | `AZ.INT.CORR.IMMED` | `AzAccount_IntCorrImmed` | TField |  | Valid inputs are CURRENT or NEXT If CURRENT is input then the accounting entries for interest correction are raised online by debiting repay account. If NEXT is input then the accounting entries for interest correction are raised online by debiting AZINTADJ suspense account &amp; then the actual debit to repay account shall happen only during NEXT online/ scheduled repayment. Validation Rules: Valid inputs are CURRENT or NEXT. NEXT allowed only if ANNUITY.ADJUST = 'YES'. If INT.CORR.RTN is defined in AZ.PRODUCT.PARAMETER, then it takes priority over the value in this field. |
| 104 | `AZ.CREATE.DATE` | `AzAccount_CreateDate` | TField |  | This field contains the date the AZ contract was created. It is a noinput field and updated by the system at INAU stage. This is used to validate if the contract was a NS contract. |
| 105 | `AZ.PRIN.CHG.AMT` | `AzAccount_PrinChgAmt` | TField |  | Amount field to hold the principal increase / decrease amount, formatted in account's currency .It should allow '-' to indicate principal decrease. Validation Rules: Standard amount field. Allowed only with ACCT.SYNC null. |
| 106 | `AZ.PRIN.EFF.DATE` | `AzAccount_PrinEffDate` | TField |  | Field to hold the princial change effective rate.Applicable only if PRIN.CHG.AMT is present. Validation rules: Noinput field. Applicable only with ACCT.SYNC null. |
| 107 | `AZ.MIN.MAT.DATE` | `AzAccount_MinMatDate` | TField | No | Define the minimum maturity date of the revolving loan account of all in one product. Repayment schedules need to be defined after MIN.MAT.DATE which specifies the drawdown end period. This field is applicable for all the revolving loan product other than credit card. The date cannot be less than or equal to value date and cannot be greater than or equal to Maturity date. Validation: 1. Standard date format 2. Optional field |
| 108 | `AZ.COMPOUND.TYPE` | `AzAccount_CompoundType` | TField |  | This field accepts the frequency like DAILY,WEEKn, MONTHnn, Nnn to determine the compounding period in a year VALIDATIONS ALLOWED for fixed deposits only NOCHANGE field |
| 109 | `AZ.ROLL.INT.CAP.FQU` | `AzAccount_RollIntCapFqu` | TField |  | This field is to capture the interest capitalisation frequency which would be used to define interest capitalisation schedules after rollover. This field is applicable only for fixed deposits with Maturity instruction is set as Automatic rollover and Schedules set as Y. Validation Rules: Types of input allowed are Dnn,Wnn,Mnn |
| 110 | `AZ.ROLL.INT.PAY.FQU` | `AzAccount_RollIntPayFqu` | TField |  | This field is to capture the interest payment frequency which would be used to define interest payment schedules after rollover. This field is applicable only for fixed deposits with Maturity instruction is set as Automatic rollover and Schedules set as Y. Validation Rules: Types of input allowed are Dnn,Wnn,Mnn |
| 111 | `AZ.PAY.INT.AT.MAT` | `AzAccount_PayIntAtMat` | TField |  | This field is used to decide whether accrued interest has to be paid out or rolled over during rollover of a fixed deposit. Allowed values are YES or NO If 'YES' is defined, then during rollover, the accrued interest before rollover will be paid out .When final maturity is reached or maturity instruction is Payment to Nominated account, then interest will be paid to Nominated account. If 'NO' is defined, then interest will be rolled over along with the principal deposit amount by default. The amount of rolled over deposit can anyway be overridden by the rollover amount definition at AZ.ACCOUNT level |
| 112 | `AZ.ROLL.AIO.PRODUCT` | `AzAccount_RollAioProduct` | TField |  | This field is used to decide the AZ.PRODUCT.PARAMETER that should be used during rollover of deposits. Applicable only for NON-MULTI FIXED DEPOSITS . Maturity instruction of current fixed deposit should be Automatic rollover During rollover, if the system cannot roll to new product then an exception log will be updated. |
| 113 | `AZ.PAY.INT.AT.PARTRED` | `AzAccount_PayIntAtPartred` | TField |  | Field at the account level to indicate if interest is to be paid to Settlement account immediately during part redemption. If set to NULL then interest is paid out immediately to Settlement account during part redemption. If set to NO then interest will be paid only during the next IN or on the maturity date. Validation Rules: Allowed only for normal Fixed deposits. Default is null. |
| 114 | `AZ.REDEMPTION.DATE` | `AzAccount_RedemptionDate` | TField |  | Field to indicate the effective date of part redemption or pre-closure of a normal Fixed type deposits. Validation Rules: Standard date field. Can be backdated or TODAY. Default is TODAY. Cannot be forward value. Cannot be beyond last capitalisation date. Pre-closure date cannot be beyond last redemption. |
| 115 | `AZ.IAS.CLASSIFICATION` | `AzAccount_IasClassification` | TField |  | This field holds the classification of contract based on IFRS standard. Validation Rules: Should be a Valid record ID in IAS.CLASSIFICATION file. Input not allowed for Credit-cards Input not allowed when ACCT.SYNC is set to null. Input not allowed for contracts with maturity date as TODAY This field cannot be amended after authorization |
| 116 | `AZ.IAS.SUB.TYPE` | `AzAccount_IasSubType` | TField |  | This field holds the Sub type for the corresponding classification defined in IAS.CLASSIFICATION field Validation Rules: Should be a valid sub type for the corresponding classification defined in IAS.CLASSIFICATION file. This field cannot be amended after authorization |
| 117 | `AZ.MARKET.RATE` | `AzAccount_MarketRate` | TField | Yes | Market rate to calculate the NPV based on future cashflow information .Validation Rules: The field can either be a Fixed rate or PERIODIC.INTEREST key with prefix 'P' Input is mandatory if IAS.CLASSIFICATION field is entered. |
| 118 | `AZ.MARKET.MARGIN` | `AzAccount_MarketMargin` | TField | No | Margin to calculate the NPV based on future cashflow information Validation Rules: The field can either be positive or negative. Input is optional. |
| 119 | `AZ.CAP.INT.AT.SOD` | `AzAccount_CapIntAtSod` | TField |  | Field at the account level, to indicate if the Schedule on the Due date should be processed during the Start of Day or End of Day. If set to YES, then the Schedule is processed during the Start of Day. If set to NO, then the Schedule is processed during the End of Day. Validation rules Allowed only for Fixed Deposits. Defaulted to NO |
| 120 | `AZ.RESERVED.2` | `AzAccount_Reserved2` | TField |  |  |
| 121 | `AZ.RESERVED.1` | `AzAccount_Reserved1` | TField |  |  |
| 122 | `AZ.STMT.NO` | `AzAccount_StmtNo` |  |  |  |
| 123 | `AZ.OVERRIDE` | `AzAccount_Override` |  |  |  |
| 124 | `AZ.RECORD.STATUS` | `AzAccount_RecordStatus` | String |  |  |
| 125 | `AZ.CURR.NO` | `AzAccount_CurrNo` | String |  |  |
| 126 | `AZ.INPUTTER` | `AzAccount_Inputter` |  |  |  |
| 127 | `AZ.DATE.TIME` | `AzAccount_DateTime` |  |  |  |
| 128 | `AZ.AUTHORISER` | `AzAccount_Authoriser` | String |  |  |
| 129 | `AZ.CO.CODE` | `AzAccount_CoCode` | String |  |  |
| 130 | `AZ.DEPT.CODE` | `AzAccount_DeptCode` | String |  |  |
| 131 | `AZ.AUDITOR.CODE` | `AzAccount_AuditorCode` | String |  |  |
| 132 | `AZ.AUDIT.DATE.TIME` | `AzAccount_AuditDateTime` | String |  |  |
