# BL.BILL — Table Schema

> Source: `INSERTS/I_F.BL.BILL` in `BL_Foundation.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `BL.BILL.OPERATION` | `BlBill_Operation` | TField | Yes | This specifies the operation of the Bill and based on this operation appropriate entries are generated. Valid operations are COLLATERAL / COLLECTION / DISCOUNT. Validation Rules: Mandatory |
| 2 | `BL.BILL.CUSTOMER` | `BlBill_Customer` | TField | Yes | This is the identification number or mnemonic assigned to the Customer. Validation Rules: 3-10 alphanumeric customer mnemonic or 1-10 numeric character customer number. (Mandatory input.) Must be a valid CUSTOMER number or mnemonic. Once the contract has been authorised this field cannot be changed. |
| 3 | `BL.BILL.TXN.TYPE.COND` | `BlBill_TxnTypeCond` | TField |  | This is a No Input field. Once the contract is committed, this field would be updated with the value id of BL.TXN.TYPE.CONDITION which satisfies the Bill operation and BL.TYPE . If these records exist in BTTC file for the Operation and BL.TYPE or Operation alone then that Id will get updated (Refer to Defaulting Mechanism). Otherwise value will be NULL. |
| 4 | `BL.BILL.POSITION.TYPE` | `BlBill_PositionType` | TField |  | Defaults from position type of BL.PARAMETER Validation Rules: Defaults value "TR" |
| 5 | `BL.BILL.CCY.MKT` | `BlBill_CcyMkt` | TField | No | Identifies the Currency Market related to this transaction. This field will be applicable to those users in different countries, where the exchange control authorities have defined different markets for foreign currency dealing. Validation Rules: 1 numeric digit Currency Market code. (Optional input. Default = 1) Must be a valid code on the CURRENCY.MARKET table. Once the contract has been authorised this field cannot be changed. |
| 6 | `BL.BILL.CATEGORY` | `BlBill_Category` | TField |  | Contains the Category Code to which this transaction will be assigned. This will default once it is defined in BL.TYPE. Validation Rules: 1-5 numeric characters. No input field Must exist on the CATEGORY table. Once authorised the contract Category code can not be changed through this Application |
| 7 | `BL.BILL.CURRENCY` | `BlBill_Currency` | TField | Yes | Indicates the Bill Contract currency. This field indicates the currency of the contract. The currency should be the same as the Currency of all attached Bill Registers . Validation Rules: 3 alphabetic currency mnemonic or 1-3 numeric character currency code. (Mandatory Input.) The currency code or mnemonic must exist on the CURRENCY table. Once the contract has been authorised this field cannot be changed. |
| 8 | `BL.BILL.BUS.DAY.DEFN` | `BlBill_BusDayDefn` | A (alphanumeric) | No | Indicates those countries whose calendars must be checked with regard to holiday validation. The default value will be the Country of the currency of the Contract but the User can input any number of extra codes if required. For example, the country of the participants. Validation Rules: 4 type A (alphanumeric) characters. (Optional input. Multivalue field.) The code must have a valid entry on the REGION/HOLIDAY table. Once authorised a change in this field will require redefinition of the schedules. |
| 9 | `BL.BILL.PRINCIPAL` | `BlBill_Principal` | TField | No | This is the total amount of the BL.Registers attached to the Bill Contract. This is an optional field. If the user enters amount, it would be validated against the total amount of all Bill Registers attached to the contract. If not entered by the user, this field would be updated by the system, when the contract is committed. Validation Rules: Optional Input |
| 10 | `BL.BILL.ORIG.PRINCIPAL` | `BlBill_OrigPrincipal` | TField |  | If there is a change in the Principal due to any amendment in the Bill, while the Principal field will show the latest Principal, this field will default the original principal, when first time contract is committed and authorised. This is a no input field. |
| 11 | `BL.BILL.DEAL.DATE` | `BlBill_DealDate` | TField |  | The current date being the defaulted date of the Bill Contract, is populated automatically Validation Rules: Deal date should be less than or equal to Today. |
| 12 | `BL.BILL.VALUE.DATE` | `BlBill_ValueDate` | A (alphanumeric) | No | This is the value date (or start) of the contract. When value is not entered defaults to Today. If entered by the User, it should be greater than or equal to Deal Date. This date should be greater than or equal to the Start Date of all attached Bill Registers. Validation Rules: 1-9 type A (alphanumeric) characters. (Optional input. Default equals system date.) An override will be required if the date as defined on the HOLIDAY table is a non working day. Must be prior to FIN.MAT.DATE. Once the contract has been authorised, this field cannot be changed. An override will be required if the date is not defined on the HOLIDAY table. |
| 13 | `BL.BILL.EXPOSURE.DATE` | `BlBill_ExposureDate` | TField |  | This is a No Input field. Defaults to value date. |
| 14 | `BL.BILL.MAT.DATE` | `BlBill_MatDate` | TField | No | The date when the Bill contract is due to mature. Optional field. When the user enters a date system check that the date is equal to latest maturity date. Otherwise System Defaults the latest maturity date of the Bill Register or Bill Batch when more than one Bill is attached in the same contract. Example Bill no. Maturity Date BR0102200005 20010120 BR0102200004 20010210 BR0102200002 20010205 If all the above BR's are entered in the same contract, the maturity date which is defaulting will be 20010210. The system raises override when the maturity date is a non working day in the country(ies) defined in BUS.DAY.DEFN. |
| 15 | `BL.BILL.LIMIT.REF` | `BlBill_LimitRef` | TField |  | This field will identify the LIMIT.REFERENCE number to be checked for limit control on BL transactions. Each BL transaction will be checked against the LIMIT file to verify the availability of a limit facility for the customer specified in BL Contract. If no input is made at this field, the LIMIT system will provide a default according to the values defined in the LIMIT.PARAMETER file. If required, more details can be found in the LIMIT user guide. For Collection Bills limit reference will be updated based on the Default.Info as defined in LIMIT.PARAMETER. Validation Rules: 3-7 numeric characters. Input to this field must be a valid LIMIT.REFERENCE number. If no limit exists for the customer, a corresponding error message will be displayed by the system with an override facility. |
| 16 | `BL.BILL.INTERPOLATE` | `BlBill_Interpolate` | TField |  | If the interest key is specified as PERIODIC.INTEREST key , then Interpolation will be done to arrive the interest rate for a period (Like interpolation done for Money Market Application) Validation Rules: Defaulted to "YES" No Input field. |
| 17 | `BL.BILL.INT.BASIS` | `BlBill_IntBasis` | A (alphanumeric) | No | Defaults to the Interest rate basis as defined for the Contract currency in CURRENCY Application. Following Interest Keys are supported in Bill module expect "S" Special for Interest method Discount and Yield added. Key Days/Denominator A 360/360 A1 360/360* A2 360/360* B 366/360 C 366/366 D 360/366 E 366/365 F 360/365 F1 360/365* F2 360/365* S Special For Discount to Yield Interest method C &amp; D Interest Basis not Allowed. For illustrations of all supported INTEREST.BASIS types and for types marked * see: INTEREST.BASIS Validation Rules: 1-2 type A (alphanumeric) characters. (Optional input. Default = CURRENCY table value) Once authorised this field cannot be changed. |
| 18 | `BL.BILL.INT.KEY` | `BlBill_IntKey` | TField | No | This is an optional input field. The default mechanism (see below) will happen as per below automatically along with other fields ( INT.SPREAD,INT.RATE). Default Mechanism 1. The system would first look for an appropriate BTTC record and take the values defined therein. Then it would use the values defined in appropriate BGC record for modification/override purposes. Finally it could use the values from BL.PARAMETER. It is possible that some of the fields in BTTC or BGC records are not defined (null values). As a general rule, the system would skip these fields and use the values defined at the next level of parameter records. 2. If Bill-Type of all Bill Registers attached to a Bill Contract are same then the system would first look for a BTTC record with key "DISCOUNT*NNNN" where "NNNN" is the value of the common Bill-Type. If there is no such record then the system would look for a record with key "DISCOUNT". 3. If Bill-Type of all Bill Registers attached to a Bill Contract is not the same, then the system would only look for a BTTC record with key DISCOUNT. 4. The system would first update the Contract Group field in Bills Contract; it would determine which is the first Contract Group defined in APPL.GEN.CONDITION file (Record Key: BL)which satisfy the Bill Contract conditions. If no contract group defined therein satisfies the Bill Contract conditions, the system would not update the Contract Group field in the Bills Contract. 5. The BGC file could have records with key values of either "C-Customer No." or a valid Bill Contract Group. In BGC, the system would first look for a record with key "C-Customer No.". If the record does not exist, then the system would look for a record with key equal to the Contract Group field in the Bills Contract. 6. The values in BGC record fields could in some cases override the BTTC values are in some cases could modify the BTTC values. It is possible that both BTTC and BGC records are not available or that the values for some fields are not defined in these records. In such cases, the values from the BL.PARAMETER file would be used by the system to default the values. Validation Rules: Valid BASIC.INTEREST or PERIODIC.INTEREST key is allowed To specify PERIODIC.INTEREST key, prefix the "P" with the interest key. (Example: For periodic interest key "01" ,specify it as "P01") Interest Key is allowed only when the Interest Rate is NULL. |
| 19 | `BL.BILL.INT.SPREAD` | `BlBill_IntSpread` | TField | No | This is an optional input field. The default mechanism (see below) will happen as per below automatically along with other fields ( INT.KEY,INT.RATE). Default Mechanism 1. The system would first look for an appropriate BTTC record and take the values defined therein. Then it would use the values defined in appropriate BGC record for modification/override purposes. Finally it could use the values from BL.PARAMETER. It is possible that some of the fields in BTTC or BGC records are not defined (null values). As a general rule, the system would skip these fields and use the values defined at the next level of parameter records. 2. If Bill-Type of all Bill Registers attached to a Bill Contract are same then the system would first look for a BTTC record with key "DISCOUNT*NNNN" where "NNNN" is the value of the common Bill-Type. If there is no such record then the system would look for a record with key "DISCOUNT". 3. If Bill-Type of all Bill Registers attached to a Bill Contract is not the same, then the system would only look for a BTTC record with key DISCOUNT. 4. The system would first update the Contract Group field in Bills Contract; it would determine which is the first Contract Group defined in APPL.GEN.CONDITION file (Record Key: BL)which satisfy the Bill Contract conditions. If no contract group defined therein satisfies the Bill Contract conditions, the system would not update the Contract Group field in the Bills Contract. 5. The BGC file could have records with key values of either "C-Customer No." or a valid Bill Contract Group. In BGC, the system would first look for a record with key "C-Customer No.". If the record does not exist, then the system would look for a record with key equal to the Contract Group field in the Bills Contract. 6. The values in BGC record fields could in some cases override the BTTC values are in some cases could modify the BTTC values. It is possible that both BTTC and BGC records are not available or that the values for some fields are not defined in these records. In such cases, the values from the BL.PARAMETER file would be used by the system to default the values. Validation Rules: Interest Spread is allowed if the Interest Rate is NULL |
| 20 | `BL.BILL.INT.RATE` | `BlBill_IntRate` | TField | No | This is an optional input field. The default mechanism (see below) will happen as per below automatically along with other fields ( INT.SPREAD, INT.KEY). Default Mechanism 1. The system would first look for an appropriate BTTC record and take the values defined therein. Then it would use the values defined in appropriate BGC record for modification/override purposes. Finally it could use the values from BL.PARAMETER. It is possible that some of the fields in BTTC or BGC records are not defined (null values). As a general rule, the system would skip these fields and use the values defined at the next level of parameter records. 2. If Bill-Type of all Bill Registers attached to a Bill Contract are same then the system would first look for a BTTC record with key "DISCOUNT*NNNN" where "NNNN" is the value of the common Bill-Type. If there is no such record then the system would look for a record with key "DISCOUNT". 3. If Bill-Type of all Bill Registers attached to a Bill Contract is not the same, then the system would only look for a BTTC record with key DISCOUNT. 4. The system would first update the Contract Group field in Bills Contract; it would determine which is the first Contract Group defined in APPL.GEN.CONDITION file (Record Key: BL)which satisfy the Bill Contract conditions. If no contract group defined therein satisfies the Bill Contract conditions, the system would not update the Contract Group field in the Bills Contract. 5. The BGC file could have records with key values of either "C-Customer No." or a valid Bill Contract Group. In BGC, the system would first look for a record with key "C-Customer No.". If the record does not exist, then the system would look for a record with key equal to the Contract Group field in the Bills Contract. 6. The values in BGC record fields could in some cases override the BTTC values are in some cases could modify the BTTC values. It is possible that both BTTC and BGC records are not available or that the values for some fields are not defined in these records. In such cases, the values from the BL.PARAMETER file would be used by the system to default the values. Validation Rules: Interest rate is allowed if the Interest key and Interest spread is NULL |
| 21 | `BL.BILL.EFF.INT.RATE` | `BlBill_EffIntRate` | TField |  | This is the final Interest Rate arrived by the system to calculated the Discount Amount. The system arrives Effective Interest Rate by adding the Interest rate as per Interest Key or Interest Rate plus spread and Interest Differential, if any defined in the appropriate BGC record for the Customer / Group. Validation Rules: No Input field. Cannot be Less than or Equal to ZERO. |
| 22 | `BL.BILL.INT.METHOD` | `BlBill_IntMethod` | TField | Conditional | Optional with values "Discount" or "Discount to Yield" or "Yield Added". Default value is "Discount". See user guide to know the calculation details. Validation Rules: If Interest Method is Yield added - Interest basis C &amp; D not Allowed. No input field once authorised. For "Yield added" method Interest Calculation Frequency is mandatory. |
| 23 | `BL.BILL.INT.CALC.FREQ` | `BlBill_IntCalcFreq` | TField | Yes | If the Interest Method is "Yield Added", Interest calculation frequency will defined here. Validation Rules: Standard T24 Interest frequency format. Daily/Monthly Mandatory for "YIELD ADDED" Interest Method. For other Interest method -No input field. |
| 24 | `BL.BILL.TOT.DISC.AMT` | `BlBill_TotDiscAmt` | TField |  | This is a no input field which is filled automatically when a bill is discounted. The amount defaulted in this column is the total discount on the Bill Contract. The discounted amount is equivalent to the interest charged on the principal of the Bill at the specified rate of interest for the period of maturity of the Bill at the defined Interest Basis. Eg. Bill Amount US $ 100000; Tenor of the Bill 60 days; Grace Days 3C;Int Basis B(366/360) Rate of Interest is 10% The Total Disc Amount is calculated as follows: US$ 100000 x 10/100 x63/360 = US$ 1750. |
| 25 | `BL.BILL.DRAWDOWN.ACCT` | `BlBill_DrawdownAcct` | TField | No | This is the account number to which the principal Amount of BL.BILL contract would be credited. This is an optional field. If not entered, default would be the first account in the Currency of Contract for the Contract Customer as per ACCOUNT.PARAMETER definition. System would issue override message if an account belonging to another customer is entered. In case the underlying Contract Customer (eg. Customer.id in MONEYMARKET,Counterparty in FOREX, etc.) has a record in CUSTOMER.SSI application, then the account number in this field gets defaulted as per the definition for that Customer from CUSTOMER.SSI. For more information on the usage of this feature please refer to CUSTOMER.SSI application. Validation Rules: Optional input. Defaults to account in contract currency for the BL.BILL customer.. Must be a valid ACCOUNT number or mnemonic. The system will check for account posting restrictions and require an override if necessary. The drawdown account must be denominated in the contract Currency. Once authorised , accounts cannot be changed. |
| 26 | `BL.BILL.PRIN.LIQ.ACCT` | `BlBill_PrinLiqAcct` | TField | No | Indicates the account number where entries are made for the reimbursement of the principal at liquidation. Based on ACCOUNT.PARAMETER definition, system defaults the accounts. In case the underlying Contract Customer (eg. Customer.id in MONEYMARKET,Counterparty in FOREX, etc.) has a record in CUSTOMER.SSI application, then the account number in this field gets defaulted as per the definition for that Customer from CUSTOMER.SSI. For more information on the usage of this feature please refer to CUSTOMER.SSI application. Validation Rules: 1-14 numeric characters-relating to a Customer Account, a Nostro Account. or 3-10 alphanumeric account mnemonic. (Optional input. Default value = Drawdown account.) Must be a valid ACCOUNT number or mnemonic. The system will check for posting restrictions and require an override if necessary. The principal liquidation account must be denominated in the contract currency Once authorised, this field cannot be changed. |
| 27 | `BL.BILL.INT.LIQ.ACCT` | `BlBill_IntLiqAcct` | TField | No | Contains the account number where entries are made for the reimbursement of the Interest (together with any taxes applicable to that interest) at liquidation. The defaulting process looks firstly at the SEC.ACC.MASTER record using the portfolio no. If no default found uses the CUSTOMER.CCY.ACCT record and finally if still no default found takes details from the AGENCY record and NOSTRO.ACCOUNT table. These settings can be overriden by using ACCOUNT.PARAMETER to set the account default method. In a case where interest is subject to taxes, such taxes will always remain associated with their interest amount and will always be liquidated through the same account. Thus it is not possible to liquidate interest and ocrresponding taxes through two different accounts or in two different currencies. Input in this field will be necessary only when the idsposal of the interest is different from the disposal of the principal. This field cannot be changes once BL contract is authorised. Validation Rules: 1-14 numeric characters-relating to a Customer Account or a Nostro Account or Internal Account. 3-10 type A alphanumeric account mnemonic. (Optional input. Default value = PRIN.LIQ.ACCT). The system will check for account posting restrictions and will require an override if necessary. Must be a valid ACCOUNT number or mnemonic. The Interest liquidation account must be denominated in the contract currency. |
| 28 | `BL.BILL.CHG.LIQ.ACCT` | `BlBill_ChgLiqAcct` | TField | No | Represents the account to which the fees/charges will be debited. This field indicates the account which will be debited with any charges applicable to a Bills contract. Fees and Charges are always expressed in the contract currency. In case the underlying Contract Customer (eg. Customer.id in MONEYMARKET,Counterparty in FOREX, etc.) has a record in CUSTOMER.SSI application, then the account number in this field gets defaulted as per the definition for that Customer from CUSTOMER.SSI. For more information on the usage of this feature please refer to CUSTOMER.SSI application. Validation Rules: 1-14 numeric characters-relating to a Customer Account or a Nostro or a Internal Account. or 3-10 alphanumeric account mnemonic. (Optional input. Default = Principal liquidation account.) The system will check for account posting restrictions and require an override if necessary. Must be an account denominated in the contract currency. |
| 29 | `BL.BILL.GRACE.DAYS` | `BlBill_GraceDays` | TField | No | This is an optional field with the Format nnC or nnW where nn is a number from 1 to 99. An input of 0 is also allowed. "C" stands for Calendar days and "W" for Working Days. If no value is entered, then the value would default as explained below which is a defaulting mechanism. If BGC value is defined (not null) then BGC value would be defaulted. If BTTC value is defined (not null) and BGC value is not defined (null), then BTTC value would be defaulted. If both BGC and BTTC values were not defined (null), then the Grace Days defined in the BL.PARAMETER file would be defaulted. Discount Interest will be calculated and charged upfront based on actual no of day Plus Grace days as specified here. Validation Rules: Value of 1-99 allowed prefixed by "C" or "W" |
| 30 | `BL.BILL.MARGIN` | `BlBill_Margin` | TField |  | For future use |
| 31 | `BL.BILL.CONSOLIDATE.ENTRY` | `BlBill_ConsolidateEntry` | TField |  | This is an YES or NO field. When more than one Statement Entry appear in an Application, it results in multiple entries in the statement of the customer. To make the sum of all the multiple entries to appear as one entry in the Statement, we define "YES" in this field. If "NO" is keyed in, the multiple entries will appear as it is in the statements. Validation Rules: Yes or No Defaults from the Consolidate.entry field as defined in BL.PARAEMTER. |
| 32 | `BL.BILL.LIQ.MODE` | `BlBill_LiqMode` | TField | Conditional | Optional with values AUTOMATIC, SEMI-AUTOMATIC or MANUAL. Default would be No value. If a value is entered in this field, Liquidation Mode field in all the Bill Registers attached to the Bill, would be updated to that value. (See PD user guide for more information) Validation Rules: Mandatory if the consolidate entry is set to "YES" Once authorised cannot be changed. |
| 33 | `BL.BILL.BR.NO` | `BlBill_BrNo` |  |  |  |
| 34 | `BL.BILL.BR.COLL.STATUS` | `BlBill_BrCollStatus` |  |  |  |
| 35 | `BL.BILL.BR.STATUS.DATE` | `BlBill_BrStatusDate` |  |  |  |
| 36 | `BL.BILL.BB.NO` | `BlBill_BbNo` |  |  |  |
| 37 | `BL.BILL.BB.COLL.STATUS` | `BlBill_BbCollStatus` |  |  |  |
| 38 | `BL.BILL.BB.BR.NO` | `BlBill_BbBrNo` |  |  |  |
| 39 | `BL.BILL.COLL.STATUS` | `BlBill_CollStatus` |  |  |  |
| 40 | `BL.BILL.COL.STA.DAT` | `BlBill_ColStaDat` |  |  |  |
| 41 | `BL.BILL.WAIVE.CHG` | `BlBill_WaiveChg` | TField | Yes | This is a mandatory field which will determine whether the commission/charge to be charged or to be waived. If entered as "Y" then the BR/BB/BL id, charge related fields in BL will not be defaulted and commission/charges will not be collected. Validation Rules: Valid values "Yes" or "No" |
| 42 | `BL.BILL.BR.BB.ID` | `BlBill_BrBbId` |  |  |  |
| 43 | `BL.BILL.CHG.CODE` | `BlBill_ChgCode` |  |  |  |
| 44 | `BL.BILL.CHG.CCY` | `BlBill_ChgCcy` |  |  |  |
| 45 | `BL.BILL.CHG.AMT` | `BlBill_ChgAmt` |  |  |  |
| 46 | `BL.BILL.CHG.AMT.LCY` | `BlBill_ChgAmtLcy` |  |  |  |
| 47 | `BL.BILL.CHG.DATE` | `BlBill_ChgDate` |  |  |  |
| 48 | `BL.BILL.TAX.CODE` | `BlBill_TaxCode` |  |  |  |
| 49 | `BL.BILL.TAX.AMT` | `BlBill_TaxAmt` |  |  |  |
| 50 | `BL.BILL.TAX.AMT.LCY` | `BlBill_TaxAmtLcy` |  |  |  |
| 51 | `BL.BILL.DISC.AMT` | `BlBill_DiscAmt` |  |  |  |
| 52 | `BL.BILL.TAX.INT.AMT` | `BlBill_TaxIntAmt` |  |  |  |
| 53 | `BL.BILL.TOT.CHG.AMT` | `BlBill_TotChgAmt` | TField |  | This is a no input field. The total charges charged for this particular contract will reflect in this column. |
| 54 | `BL.BILL.TOT.TAX.CHG.AMT` | `BlBill_TotTaxChgAmt` | TField |  | This is a no input field. The total tax on charges amount collected for this particular contract will reflect in this field. |
| 55 | `BL.BILL.TAX.INTEREST.KEY` | `BlBill_TaxInterestKey` | TField | No | Represents a reference on the TAX table which will indicate the rate to be used for the computation of Interest taxation. This field can be defaulted if the TAX.INTEREST.TYPE field is used. See the help text for CONTRACT.GRP and TAX.INTEREST.TYPE for further information. Interest taxation in all instances is charged on the relevant interest payment dates. No accrual will be performed. Validation Rules: 2 numeric characters. (Optional input. Default = no tax.) Must be a valid reference on the TAX table. Once authorised this field cannot be changed. |
| 56 | `BL.BILL.TAX.INTEREST.TYPE` | `BlBill_TaxInterestType` | TField | No | Using this field together with the CONTRACT.GRP field it is possible to determine a particular tax code from conditions based on: General tax type as defined in the TAX.TYPE.CONDITION application Type of customer as defined in the CUSTOMER.CHARGES application Type of contract as defined in the CONTRACT.GRP field. The CONTRACT.GRP code is determined every time the contract is changed by evaluating the conditions defined in the BILLS record of the APPL.GEN.CONDITION application. (see APPL.GEN.CONDTION for more information). The value in this field must equate to a record in the TAX.TYPE.CONDITION application which contains tax codes related to customer tax group and contract group. Whenever interest tax is recalculated the correct tax code will be determined and the TAX.INTEREST.KEY and TOT.INT.TAX fields updated accordingly. If the calculated tax amount changes between sending a payment and the end of the relevant tax period then an exception will be raised during the end of day indicating the different amounts. It will be stored in EXCEPTION.LOG.HIST. Validation Rules: Optional input, must be present as a record on TAX.TYPE.CONDITION. The actual TAX.TYPE.CONDITION key value will be prefixed by the "*" character (asterisk). |
| 57 | `BL.BILL.CONTRACT.GRP` | `BlBill_ContractGrp` | TField |  | Allows particular groups of contracts to be identified. The conditions which define a particular group are defined using the APPL.GEN.CONDITION for BL Application. Each time the contract is changed the conditions are re-evaluated which can result in a new CONTRACT.GRP code. This code can be defined in the TAX.TYPE.CONDITION application to refer to a particular tax code, in conjunction with the relevant customer group condition. If the CONTRACT.GRP changes then this may well result in new codes being allocated to the relevant TAX.PRINCIPAL.KEY, TAX.INTEREST.KEY and TAX.COMMISSION.KEY fields being updated. Validation Rules: 5 character alphanumeric. No input field and updated by the system. Insert text here Validation Rules: Rule 1 Rule 2 |
| 58 | `BL.BILL.TOT.TAX.INT.AMT` | `BlBill_TotTaxIntAmt` | TField |  | The total of all the taxes pertaining to the Interest will get populated in this field |
| 59 | `BL.BILL.ACCT.OFFICER` | `BlBill_AcctOfficer` | TField | No | Identifies the account officer responsible for the relationship with the CPARTY. This information will be used by the Management Information System (MIS) for the determination of the Customer Profitability Analysis. This field has been provided to enable the user to override at transaction level the CPARTY default account officer defined in the CUSTOMER file. Validation Rules: 1-4 digit numeric field. (Optional input; Default value = Account Officer specified on the CPARTY's customer file.) The Account Officer Code must appear on the DEPT.ACCT.OFFICER table. |
| 60 | `BL.BILL.DEALER.DESK` | `BlBill_DealerDesk` | TField | No | Identifies the dealer desk position which needs to be updated by the deal being created. The dealer desk code is held on the position record so that the exchange position can be displayed at dealer desk level. Each deal on the system will be allocated a two-digit dealer desk code. This code will be used to maintain position at the dealer desk level. Validation Rules: 2 alphanumeric character dealer code in the range A-Z, a-z, plus 0-9. Optional input; Default = dealer desk defined on the user profile. The code entered must appear on the DEALER.DESK table. When entering an internal deal, the dealer must be different from the one entered in the counter party field. The field cannot be changed once the deal has been authorised. |
| 61 | `BL.BILL.OVERALL.COLL.STAT` | `BlBill_OverallCollStat` | TField |  | BL.BILL-BL.BILL OVERALL.COLL.STAT This field used to define the overall Collection Status of the Bill. This is user friendly option through which all BR/BB collection status can be changed instead of changing one by one. For example : If 10 BR's attached to a BL cleared and instead of changing the status of each BR, overall collection status can be changed to "Cleared". Similarly if 2 BR's returned out of 10 BR's, mark the individual BR's collection status as "Returned" and change the overall status to "Cleared" Once authorise the collection status it cannot be changed again. |
| 62 | `BL.BILL.AMEND.DATE` | `BlBill_AmendDate` | TField |  | This is a no input field. If there is an amendment made to this contract like adding one more BR or deleting a BR from the BL Contract, the date on which the amendment will get populated here automatically. Validation Rules: Defaults to system date |
| 63 | `BL.BILL.AMD.VAL.DATE` | `BlBill_AmdValDate` | TField |  | This is a no input field. If there is an amendment made to this contract like adding one more BR or deleting a BR from the BL Contract, the value date of the amendment will get populated here automatically. Validation Rules: Defaults to system date |
| 64 | `BL.BILL.AMD.EXP.DATE` | `BlBill_AmdExpDate` | TField |  | This is a no input field. If there is an amendment made to this contract like adding one more BR or deleting a BR from the BL Contract, and if there is any change in the expiry date of the contract, this amended date amendment will get populated here automatically. In an existing contract, if the newly added BR or BB has a new expiry date which is greater than the existing expiry date reflecting in the contract, the new expiry date will reflect here. On the contrary, if the newly added BR or BB has a an expiry date which lesser than or equal to the existing expiry date reflecting in the contract, there will not be be any change in the expiry date populated here. |
| 65 | `BL.BILL.ACTION` | `BlBill_Action` | TField |  | This Field specifies the action to be performed on an invoice or BL.REGISTER or BL.BATCH record Validation Rules: Options available are DISBURSE , SETTLE User Input Field |
| 66 | `BL.BILL.DISB.ALLOT.TYPE` | `BlBill_DisbAllotType` | TField |  | This Field specifies how the disbursement amount to be allotted between registers when disbursement is done against batch Validation Rules: Options available are PRORATA , FIFO Default Value is PRORATA |
| 67 | `BL.BILL.EXCHANGE.RATE` | `BlBill_ExchangeRate` | TField |  | This Field specifies the exchange rate when cross currencies are involved It is the rate between the drawdown account currency and BL.BILL currency during disbursement It is the rate between the Principal Liquidation account currency and BL.BILL currency during settlement Validation Rules: Standard T24 Rate Field Input allowed only when cross currencies are involved |
| 68 | `BL.BILL.EXCHANGE.SPREAD` | `BlBill_ExchangeSpread` | TField |  | This Field specifies the customer spread to be applied on the exchange rate if exists Validation Rules: Standard T24 Rate Field Input allowed only when cross currencies are involved Defaulted from BL.GROUP.CONDITION if not inputted by the user |
| 69 | `BL.BILL.CUSTOMER.RATE` | `BlBill_CustomerRate` | TField |  | This Field specifies the effective rate after applying EXCHANGE.RATE and EXCHANGE.SPREAD Validation Rules: Standard T24 Rate Field Updated only when cross currencies are involved System maintained field |
| 70 | `BL.BILL.MKT.EXCH.PRFT` | `BlBill_MktExchPrft` | TField |  |  |
| 71 | `BL.BILL.AMD.BR.NO` | `BlBill_AmdBrNo` |  |  |  |
| 72 | `BL.BILL.AMD.AMOUNT` | `BlBill_AmdAmount` |  |  |  |
| 73 | `BL.BILL.RESERVED27` | `BlBill_Reserved27` |  |  |  |
| 74 | `BL.BILL.RESERVED26` | `BlBill_Reserved26` |  |  |  |
| 75 | `BL.BILL.RESERVED25` | `BlBill_Reserved25` |  |  |  |
| 76 | `BL.BILL.REM.BR.NO` | `BlBill_RemBrNo` |  |  |  |
| 77 | `BL.BILL.REM.BB.NO` | `BlBill_RemBbNo` |  |  |  |
| 78 | `BL.BILL.REM.BB.BR.NO` | `BlBill_RemBbBrNo` |  |  |  |
| 79 | `BL.BILL.ADD.BR.NO` | `BlBill_AddBrNo` |  |  |  |
| 80 | `BL.BILL.ADD.BB.NO` | `BlBill_AddBbNo` |  |  |  |
| 81 | `BL.BILL.ADD.BB.BR.NO` | `BlBill_AddBbBrNo` |  |  |  |
| 82 | `BL.BILL.RESERVED24` | `BlBill_Reserved24` |  |  |  |
| 83 | `BL.BILL.RESERVED23` | `BlBill_Reserved23` |  |  |  |
| 84 | `BL.BILL.RESERVED22` | `BlBill_Reserved22` |  |  |  |
| 85 | `BL.BILL.RESERVED21` | `BlBill_Reserved21` |  |  |  |
| 86 | `BL.BILL.RESERVED20` | `BlBill_Reserved20` |  |  |  |
| 87 | `BL.BILL.RESERVED19` | `BlBill_Reserved19` |  |  |  |
| 88 | `BL.BILL.RESERVED18` | `BlBill_Reserved18` |  |  |  |
| 89 | `BL.BILL.RESERVED17` | `BlBill_Reserved17` |  |  |  |
| 90 | `BL.BILL.RESERVED16` | `BlBill_Reserved16` |  |  |  |
| 91 | `BL.BILL.RESERVED15` | `BlBill_Reserved15` |  |  |  |
| 92 | `BL.BILL.OTHER.BANK.CHG` | `BlBill_OtherBankChg` | TField |  | This Field specifies the charges deducted by the other bank out of the settlement amount towards their charges Validation Rules: Standard T24 Amount Field Specified in the currency of BL.BILL User Input Field Applicable only during settlements It can not exceed the settlement amount |
| 93 | `BL.BILL.RESERVED13` | `BlBill_Reserved13` | TField |  |  |
| 94 | `BL.BILL.RESERVED12` | `BlBill_Reserved12` | TField |  |  |
| 95 | `BL.BILL.RESERVED11` | `BlBill_Reserved11` | TField |  |  |
| 96 | `BL.BILL.BR.BL.ID` | `BlBill_BrBlId` |  |  |  |
| 97 | `BL.BILL.ACTIVITY.CODE` | `BlBill_ActivityCode` |  |  |  |
| 98 | `BL.BILL.ACTIVITY.DATE` | `BlBill_ActivityDate` |  |  |  |
| 99 | `BL.BILL.MSG.TYPE` | `BlBill_MsgType` |  |  |  |
| 100 | `BL.BILL.MSG.CLASS` | `BlBill_MsgClass` |  |  |  |
| 101 | `BL.BILL.OVR.CARRIER` | `BlBill_OvrCarrier` |  |  |  |
| 102 | `BL.BILL.SEND.MSG` | `BlBill_SendMsg` |  |  |  |
| 103 | `BL.BILL.MSG.DATE` | `BlBill_MsgDate` |  |  |  |
| 104 | `BL.BILL.DELIVERY.REF` | `BlBill_DeliveryRef` |  |  |  |
| 105 | `BL.BILL.IAS.CLASSIFICATION` | `BlBill_IasClassification` | TField |  | This field holds the classification of contract based on IFRS standard. Validation Rules: Should be a valid record ID in IAS.CLASSIFICATION file. This field cannot be amended after authorization. Input allowed for discounted trade bills only |
| 106 | `BL.BILL.IAS.SUBTYPE` | `BlBill_IasSubtype` | TField |  | This field holds the Sub type for the corresponding classification defined in IAS.CLASSIFICATION field. Validation Rules: Should be a valid sub type for the corresponding classification defined in IAS.CLASSIFICATION file. The field cannot be amended after authorization. Input allowed for discounted trade bills only |
| 107 | `BL.BILL.MARKET.KEY` | `BlBill_MarketKey` | TField | Yes | Market rate to calculate the NPV based on future cashflow information. Validation Rules: The field can either be a Fixed rate or PERIODIC.INTEREST key with prefix P. Input is mandatory if IAS.CLASSIFICATION field is entered. Input allowed for discounted trade bills only |
| 108 | `BL.BILL.MARKET.MARGIN` | `BlBill_MarketMargin` | TField | No | Margin to calculate the NPV based on future cashflow information. Validation Rules: The field can either be positive or negative. Input is optional Input allowed for discounted trade bills only |
| 109 | `BL.BILL.RESERVED.6` | `BlBill_Reserved6` | TField |  |  |
| 110 | `BL.BILL.RESERVED.5` | `BlBill_Reserved5` | TField |  |  |
| 111 | `BL.BILL.RESERVED.4` | `BlBill_Reserved4` | TField |  |  |
| 112 | `BL.BILL.RESERVED.3` | `BlBill_Reserved3` | TField |  |  |
| 113 | `BL.BILL.RESERVED.2` | `BlBill_Reserved2` | TField |  |  |
| 114 | `BL.BILL.RESERVED.1` | `BlBill_Reserved1` | TField |  |  |
| 115 | `BL.BILL.CONTRACT.STATUS` | `BlBill_ContractStatus` | TField |  | This field shows the BL.BILL contract status. This field is updated by the system based on the activity of the BL contract. Validation Rules: No Input field Valid values "CUR" or "LIQ" |
| 116 | `BL.BILL.LOCAL.REF` | `BlBill_LocalRef` |  |  |  |
| 117 | `BL.BILL.STMT.NO` | `BlBill_StmtNo` |  |  |  |
| 118 | `BL.BILL.OVERRIDE` | `BlBill_Override` |  |  |  |
| 119 | `BL.BILL.RECORD.STATUS` | `BlBill_RecordStatus` | String |  |  |
| 120 | `BL.BILL.CURR.NO` | `BlBill_CurrNo` | String |  |  |
| 121 | `BL.BILL.INPUTTER` | `BlBill_Inputter` |  |  |  |
| 122 | `BL.BILL.DATE.TIME` | `BlBill_DateTime` |  |  |  |
| 123 | `BL.BILL.AUTHORISER` | `BlBill_Authoriser` | String |  |  |
| 124 | `BL.BILL.CO.CODE` | `BlBill_CoCode` | String |  |  |
| 125 | `BL.BILL.DEPT.CODE` | `BlBill_DeptCode` | String |  |  |
| 126 | `BL.BILL.AUDITOR.CODE` | `BlBill_AuditorCode` | String |  |  |
| 127 | `BL.BILL.AUDIT.DATE.TIME` | `BlBill_AuditDateTime` | String |  |  |
