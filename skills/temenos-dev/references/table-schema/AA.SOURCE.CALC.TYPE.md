# AA.SOURCE.CALC.TYPE — Table Schema

> Source: `INSERTS/I_F.AA.SOURCE.CALC.TYPE` in `AA_Framework.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `AA.SRC.DESCRIPTION` | `AaSourceCalcType_Description` |  |  |  |
| 2 | `AA.SRC.SOURCE.TYPE` | `AaSourceCalcType_SourceType` | TField | Yes | Base amount for calculation will be based on either a BALANCE, ACTIVITY or PROPERTY. BALANCE - Base amount of a property ACTIVITY - Transaction amount in AA.ARRANGEMENT.ACTIVITY PROPERTY - Currently not supported Validation Rules: 1. Mandatory Field. 2. Valid Inputs are BALANCE, PROPERTY and ACTIVITY. |
| 3 | `AA.SRC.BALANCE.TYPE` | `AaSourceCalcType_BalanceType` | TField |  | This field will determine if Debit balance amomt is required or Credit balance amount is required for calculation or both are required. It will accept only values -DEBIT,CREDIT,BOTH |
| 4 | `AA.SRC.CALC.TYPE` | `AaSourceCalcType_CalcType` | TField |  | The following balance amounts can be determined on top of which calculations can be carried out. DAILY - Calculated on Daily balances AVERAGE - Reserved for future use HIGHEST - Reserved for future use LOWEST - Reserved for future use COUNT - Reserved for future use TXN.AMOUNT - Calculated on AA.ARRANGEMENT.ACTIVITY Amount( can be used for activity charges) ROUTINE - If Calculation type is routine then a valid routine should be attached in ROUTINE field of BALANCE.CALC.TYPE record BILL.AMOUNT - To specify that the bill amount should be the source calculation amount when an activity is performed. Option allowed only when the source type is "ACTIVITY" |
| 5 | `AA.SRC.CALC.ROUTINE` | `AaSourceCalcType_CalcRoutine` | TField |  | This field enables the user to define a routine to calculate balance amount. Input in this field is allowed when the field CALC.TYPE is equal to ROUTINE. This field must be a valid EB.API id. To enable a java component implementation the EB.API record must have a source type of METHOD and implement an interface defined in the EB.API record HOOK.AA.SOURCE.CALC.TYPE.CALC.RTN. See the EB.API record HOOK.AA.SOURCE.CALC.TYPE.CALC.RTN for the full list of supported interfaces, initially AA.CalculationHook.calculateSourceBalances(). |
| 6 | `AA.SRC.CALC.START.DAY` | `AaSourceCalcType_CalcStartDay` | TField |  | If a bank wants to compute interest based on balances on specific days of the calendar month, then this field could be utilized. For example, if the bank decides to give interest on lowest balance between 15th and 30th of the month, then this field should be stated as 15. Valid entry is a number between 1 and 31. |
| 7 | `AA.SRC.CALC.END.DAY` | `AaSourceCalcType_CalcEndDay` | TField |  | If a bank wants to compute interest based on balances on specific days of the calendar month, then this field could be utilized. For example, if the bank decides to give interest on lowest balance between 15th and 30th of the month, then this field should be stated as 30. Valid entry is a number between 1 and 31. |
| 8 | `AA.SRC.MID.ACCOUNT.OPEN` | `AaSourceCalcType_MidAccountOpen` | TField | No | This field denotes whether the interest needs to be calculated for the month, if the arrangement was opened after the specified Calculation Start Day for the MINIMUM interest type. If set to NO, then no interest will be applied if the account was opened after the Calculation Start Day. Setting the field to BLANK will allow the interest according to the minimum balance criteria Validation Rules: Optional input. BLANK or NO. Can only be set to NO if the BALANCE.TYPE is set to MINIMUM and for Monthly calculation Default = BLANK |
| 9 | `AA.SRC.MID.ACCOUNT.CLOSE` | `AaSourceCalcType_MidAccountClose` | TField | No | This field denotes whether the interest needs to be calculated for the month, if the arrangement was closed on or before the specified Calculation End Day. If set to NO, then no interest will be applied if the account was closed on or before the Calculation End Day. Setting the field to BLANK will allow the interest according to the minimum balance criteria. Here interest will not be paid even if closure happened on the Calc End Day becaue the End of day balance will become 0. Validation Rules: Optional input. BLANK or NO. Can only be set to NO if the BALANCE.TYPE is set to MINIMUM and for Monthly calculation Default = BLANK |
| 10 | `AA.SRC.CONTEXT.NAME` | `AaSourceCalcType_ContextName` | TField | Yes | Context Name to be associated with Source Calc Type, when SOURCE.CALC.TYPE has value "CONTEXT" and SOURCE.TYPE has value "ACTIVITY". Corresponding Context Value specified in the AAA will be used as source balance for the corresponding context name specified here. Validation Rules: Valid value from AA.CONTEXT.TYPE. Mandatory when CALC.TYPE has value as "CONTEXT" |
| 11 | `AA.SRC.ATTRIBUTE.PROP.CLASS` | `AaSourceCalcType_AttributePropClass` | TField | Yes | Holds the property class to be used for picking the calculation source value. Indicates the Propertyclass which has the field with value to be used as source balance at that instant. Validation Rules: Value in this field is mandatory when SOURCE.CALC.TYPE has value "ATTRIBUTE" and SOURCE.TYPE has value "PROPERTY". Valid id from AA.PROPERTY.CLASS. |
| 12 | `AA.SRC.ATTRIBUTE.PROP.FIELD` | `AaSourceCalcType_AttributePropField` | TField |  | It holds value of FIELD.NAME from the application belongs to value in AttrPropClass. Value in this field will be used for source balance during charge calculation. Eg: TERM or AMOUNT fields from TERM.AMOUNT property. It also possible to attach a user routine to determine complex field values. The user routine needs to start with &quot;@&quot; |
| 13 | `AA.SRC.RESERVED.3` | `AaSourceCalcType_Reserved3` |  |  |  |
| 14 | `AA.SRC.RESERVED.2` | `AaSourceCalcType_Reserved2` |  |  |  |
| 15 | `AA.SRC.RESERVED.1` | `AaSourceCalcType_Reserved1` | TField |  |  |
| 16 | `AA.SRC.RECORD.STATUS` | `AaSourceCalcType_RecordStatus` | String |  |  |
| 17 | `AA.SRC.CURR.NO` | `AaSourceCalcType_CurrNo` | String |  |  |
| 18 | `AA.SRC.INPUTTER` | `AaSourceCalcType_Inputter` |  |  |  |
| 19 | `AA.SRC.DATE.TIME` | `AaSourceCalcType_DateTime` |  |  |  |
| 20 | `AA.SRC.AUTHORISER` | `AaSourceCalcType_Authoriser` | String |  |  |
| 21 | `AA.SRC.CO.CODE` | `AaSourceCalcType_CoCode` | String |  |  |
| 22 | `AA.SRC.DEPT.CODE` | `AaSourceCalcType_DeptCode` | String |  |  |
| 23 | `AA.SRC.AUDITOR.CODE` | `AaSourceCalcType_AuditorCode` | String |  |  |
| 24 | `AA.SRC.AUDIT.DATE.TIME` | `AaSourceCalcType_AuditDateTime` | String |  |  |
