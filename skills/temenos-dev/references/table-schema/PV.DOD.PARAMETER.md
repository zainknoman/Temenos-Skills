# PV.DOD.PARAMETER — Table Schema

> Source: `INSERTS/I_F.PV.DOD.PARAMETER` in `PV_DodRules.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `PV.DOD.ABS.THRESHOLD.CCY` | `PvDodParameter_AbsThresholdCcy` | TField | Yes | This field holds the currency in which the ABS.THREHSOLD.AMT is defined. In order to perform materiality test and to establish whether the past due amount is material or not, it is necessary to define the currency and the limit amount. Validation Rules: Valid Currency record. Mandatory Field. |
| 2 | `PV.DOD.OBLIGOR.CLASSIFICATION` | `PvDodParameter_ObligorClassification` |  |  |  |
| 3 | `PV.DOD.ABS.THRESHOLD.AMT` | `PvDodParameter_AbsThresholdAmt` |  |  |  |
| 4 | `PV.DOD.REL.THRESHOLD.PERC` | `PvDodParameter_RelThresholdPerc` |  |  |  |
| 5 | `PV.DOD.RETAIL.DEFAULT.CALC` | `PvDodParameter_RetailDefaultCalc` | TField |  | This field signifies the level at which the DPD default assessment (materiality threshold tests) is applied for the obligors classified as Retail. Validation Rules: Possible values - Facility and Obligor Facility option indicates default assessment will be done at asset level. Obligor option indicates default assessment will be done at obligor level. This field value is defaulted to the obligor level, when no option is chosen by the user. |
| 6 | `PV.DOD.DEFAULT.DPD.COUNT` | `PvDodParameter_DefaultDpdCount` | TField | Yes | This field holds the number of days, the materiality threshold tests breach consecutively, to identify an obligor or asset is in default. The DPD default flag is set, when the DPD count of an obligor or asset crosses the threshold defined in this field. Validation Rules: Numeric value of maximum 3 digits. Mandatory Field. |
| 7 | `PV.DOD.PROB.DEFAULT.MONITOR` | `PvDodParameter_ProbDefaultMonitor` | TField |  | This field provides an option for the user to clear the probation default flag manually or automatically upon completion of the probation period. When the automatic option is chosen, the probation default status is cleared automatically by the system, on the date the probation period is completed. When the manual option is chosen, the probation default status is expected to be cleared by the user manually. Until it is cleared, the obligor is still considered to be in default status, due to presence of a probation default flag. Validation Rules: Possible values are Automatic and Manual. This field is defaulted to automatic, when no option is chosen by the user, but probation periods are parameterized. |
| 8 | `PV.DOD.STD.PROB.PERIOD` | `PvDodParameter_StdProbPeriod` | TField | No | The field holds the standard probation period to be applied for an obligor before moving the obligor to the performing status. Validation Rules: The format is nnnD/M/Y - where n is number, D is days, M is months and Y is Years. The input is in calendar days. e.g. 100D, 6M, 2Y. Accepts up to 5 digits followed by 1 alpha character only for days(D) Accepts up to 3 digits followed by 1 alpha character other than days(D) Optional Input. |
| 9 | `PV.DOD.DR.PROB.PERIOD` | `PvDodParameter_DrProbPeriod` | TField | No | This field holds the longer probation period to be applied for an obligor, who has undergone distressed restructuring, before moving the obligor to the performing status. The EBA regulation suggests a minimum period of 12 months. Validation Rules: The format is nnnD/M/Y - where n is number, D is days, M is months and Y is Years. The input is in calendar days. e.g. 100D, 6M, 2Y. Accepts up to 5 digits followed by 1 alpha character only for days(D) Accepts up to 3 digits followed by 1 alpha character other than days(D) Optional Input. |
| 10 | `PV.DOD.UTP.INDICATOR` | `PvDodParameter_UtpIndicator` |  |  |  |
| 11 | `PV.DOD.UTP.INDICATOR.THRESHOLD.PERC` | `PvDodParameter_UtpIndicatorThresholdPerc` |  |  |  |
| 12 | `PV.DOD.EXPOSURE.EXCL.BAL.TYPE` | `PvDodParameter_ExposureExclBalType` | TField |  | This field holds the record id from the AC.BALANCE.TYPE which contains the balance types to be excluded from theAsset's exposure amount calculation based on the details held in the EB.CONTRACT.BALANCE. The system automatically considers all the non-contingent asset types as a part of the exposure amount calculation and excludes those that are mentioned here. Validation Rules: Valid AC.BALANCE.TYPE record. The user must create and configure the AC.BALANCE.TYPE record. Once the AC.BALANCE.TYPE is attached in parameter record, it is not recommended to change virtual balance types within AC.BALANCE.TYPE record. If any new balance types has to be excluded from calculation, then define the new and all old virtual balance types in another AC.BALANCE.TYPE record. And then attach this newly created AC.BALANCE.TYPE record in this field. |
| 13 | `PV.DOD.PAST.DUE.EXCL.BAL.TYPE` | `PvDodParameter_PastDueExclBalType` | TField |  | This field holds the record id from the AC.BALANCE.TYPE which contains the balance types to excluded from theAsset's past due calculation based on the details held in EB.CONTRACT.BALANCE.. By default, all non contingent asset types are part of past due amount. The system automatically considers all the non-contingent asset types as a part of the past due amount calculation and excludes those that are mentioned here. Validation Rules: Valid AC.BALANCE.TYPE record. The user must create and configure the AC.BALANCE.TYPE record. Once the AC.BALANCE.TYPE is attached in parameter record, it is not recommended to change virtual balance types within AC.BALANCE.TYPE record. If any new balance types has to be excluded from calculation, then define the new and all old virtual balance types in another AC.BALANCE.TYPE record. And then attach this newly created AC.BALANCE.TYPE record in this field. |
| 14 | `PV.DOD.DR.PROB.START.DATE` | `PvDodParameter_DrProbStartDate` | TField |  | This field controls what value has to be updated in the field DISTRESSED.RESTRUCTURING.DATE of PV.DOD.CUSTOMER.DETS. Validation Rules: Options allowed - PROBATION.DEFAULT.DATE and DR.DATE. This field is defaulted as PROBATION.DEFAULT.DATE. PROBATION.DEFAULT.DATE - Assigns Today date as value to DISTRESSED.RESTRUCTURING.DATE field of PV.DOD.CUSTOMER.DETS, when DR(Distressed Restructuring) UTP is removed. DR.DATE - Assigns DISTRESSED.RESTRUCTURING.DATE field of PV.DOD.CUSTOMER.DETS with default date corresponding to the DR UTP, when it is being removed from the PV.DOD.CUSTOMER.DETS. |
| 15 | `PV.DOD.DOD.INFO.API` | `PvDodParameter_DodInfoApi` | TField |  | Allows user to input a API which returns DOD(Definition of Default) related information from external system to T24. The client could be using the DOD functionality in his previous system. Then he could have a DOD related information for the assets or obligor. If he expects those DoD information to be loaded into T24 applications, then DOD.INFO.API has to be configured. The name of any BASIC subroutine or a java class name is entered here. This field specifies the routine that needs to be triggered when client does upgrade or migration process to DoD functionality. To enable java implementations an EB.API id with a source type of METHOD which implements an interface is defined in the EB.API record PV.DOD.PARAM.INFO.API.HOOK. Validation Rules: Valid record from EB.API table. In case of obligor, either DaysPastDue(DPD) and UnlikelinessToPay(UTP) indicator details or probation details can be returned from API but not both. |
| 16 | `PV.DOD.RESERVED.8` | `PvDodParameter_Reserved8` | TField |  |  |
| 17 | `PV.DOD.RESERVED.7` | `PvDodParameter_Reserved7` | TField |  |  |
| 18 | `PV.DOD.RESERVED.6` | `PvDodParameter_Reserved6` | TField |  |  |
| 19 | `PV.DOD.RESERVED.5` | `PvDodParameter_Reserved5` | TField |  |  |
| 20 | `PV.DOD.RESERVED.4` | `PvDodParameter_Reserved4` | TField |  |  |
| 21 | `PV.DOD.RESERVED.3` | `PvDodParameter_Reserved3` | TField |  |  |
| 22 | `PV.DOD.RESERVED.2` | `PvDodParameter_Reserved2` | TField |  |  |
| 23 | `PV.DOD.RESERVED.1` | `PvDodParameter_Reserved1` | TField |  |  |
| 24 | `PV.DOD.LOCAL.REF` | `PvDodParameter_LocalRef` |  |  |  |
| 25 | `PV.DOD.OVERRIDE` | `PvDodParameter_Override` |  |  |  |
| 26 | `PV.DOD.RECORD.STATUS` | `PvDodParameter_RecordStatus` | String |  |  |
| 27 | `PV.DOD.CURR.NO` | `PvDodParameter_CurrNo` | String |  |  |
| 28 | `PV.DOD.INPUTTER` | `PvDodParameter_Inputter` |  |  |  |
| 29 | `PV.DOD.DATE.TIME` | `PvDodParameter_DateTime` |  |  |  |
| 30 | `PV.DOD.AUTHORISER` | `PvDodParameter_Authoriser` | String |  |  |
| 31 | `PV.DOD.CO.CODE` | `PvDodParameter_CoCode` | String |  |  |
| 32 | `PV.DOD.DEPT.CODE` | `PvDodParameter_DeptCode` | String |  |  |
| 33 | `PV.DOD.AUDITOR.CODE` | `PvDodParameter_AuditorCode` | String |  |  |
| 34 | `PV.DOD.AUDIT.DATE.TIME` | `PvDodParameter_AuditDateTime` | String |  |  |
