# EB.ENC.PARAMETER — Table Schema

> Source: `INSERTS/I_F.EB.ENC.PARAMETER` in `EB_Security.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `EB.ENC.DESCRIPTION` | `EbEncParameter_Description` |  |  |  |
| 2 | `EB.ENC.ENCRYPT.APPL` | `EbEncParameter_EncryptAppl` |  |  |  |
| 3 | `EB.ENC.ENCRYPT.RTN` | `EbEncParameter_EncryptRtn` | TField | Yes | EB.API Hook routine to be called for encrypting the fields (Arguments: File name, RecordId and the Record, a dynamic Array) before committing the record to the disk. For more information on the routine format, please refer to the USER guide. Validations: 1. Mandatory field. 2. The routine should have an entry defined in EB.API |
| 4 | `EB.ENC.DECRYPT.RTN` | `EbEncParameter_DecryptRtn` |  |  |  |
| 5 | `EB.ENC.ENQ.DECRYPT.RTN` | `EbEncParameter_EnqDecryptRtn` | TField | Yes | EB.API HOOK routine for decrypting the field's data from an Enquiry. To be called per field at Conversion level Syntax: DECRYPT &lt;&lt;Field Name&gt;&gt; DECRYPT is a standard text to inform T24 that this field is sensitive and the routine attached in the main parameter table to be called. DECRYPT is a standard text to inform T24 that this field is sensitive and the routine attached in the main parameter table to be called. Field Name must be a valid field name in the enquiry For more information on the routine format, please refer to the USER guide. Validations: 1. Mandatory field. 2. The routine should have an entry defined in EB.API |
| 6 | `EB.ENC.APPL.ENC.FLD.RTN` | `EbEncParameter_ApplEncFldRtn` | TField | Yes | A EB.API Hook routine to be called for returning the list of fields per application which are encrypted. Note: The applications list returned by this routine should match the APPLICATIONS mentioned in the m/v ENCRYPT.APPL. If specified then the field APPL.MSK.CHARS becomes mandatory. Validations: 1. Non-mandatory field. 2. The routine should have an entry defined in EB.API |
| 7 | `EB.ENC.APPL.MSK.CHARS` | `EbEncParameter_ApplMskChars` | TField | Yes | The value to be used for masking the encrypted characters for a non-privileged user. Note: NULL or Blank values will not be replaced. If this field is left NULL then the actual encrypted data will be displayed. For more information on the routine format, please refer to the USER guide. Validations: 1. Non-mandatory field. 2. Max length of 35 characters 3. If the field APPL.ENC.FLD.RTN is specified then the field APPL.MSK.CHARS becomes mandatory. |
| 8 | `EB.ENC.ENQ.MSK.CHARS` | `EbEncParameter_EnqMskChars` | TField | Yes | The value to be used for masking the encrypted field's value (for a non-privileged user) Note: This is for the local API routine mentioned in ENQ.DECRYPT.RTN to make use of. CORE does not handle this for ENQUIRY. Validations: 1. Non-mandatory field. 2. Max length of 35 characters |
| 9 | `EB.ENC.DE.ENCRYPT.RTN` | `EbEncParameter_DeEncryptRtn` | TField | Yes | EB.API hook routine to be invoked for encrypting sensitive tags in swift delivery message towards the end of formatting process. Arguments passed will be the Delivery reference and message contents (F.DE.O.MSG.SWIFT contents). For more information on the routine format, please refer to the USER guide. Validations: 1. Non Mandatory field. 2. The routine should have an entry defined in EB.API |
| 10 | `EB.ENC.DE.DECRYPT.RTN` | `EbEncParameter_DeDecryptRtn` | TField | Yes | EB.API hook routine to be invoked for decrypting swift delivery message before starting formatting process. Arguments passed will be the Delivery reference and message contents (F.DE.O.MSG or F.DE.I.MSG contents). For more information on the routine format, please refer to the USER guide. Validations: 1. Non Mandatory field. 2. The routine should have an entry defined in EB.API |
| 11 | `EB.ENC.PROCESS.ENRI.RTN` | `EbEncParameter_ProcessEnriRtn` | TField | Yes | EB.API hook routine to be invoked for processing the enrichment if the field to be used to return an enrichment value is categorised as sensitive field. This API will be invoked only if the field used to return an enrichment value is sensitive. Accepts two Parameter; 1. Flag to decide User permission 2. Enrichment Value This API is responsible to check whether the user is allowed to view the enrichment as plain text. Based on user permission API should return decrypted enrichment or the Flag to mask the enrichment Validations: 1. Non Mandatory field. 2. The routine should have an entry defined in EB.API |
| 12 | `EB.ENC.SENSITIVE.FIELDS.NOINPUT` | `EbEncParameter_SensitiveFieldsNoinput` | TField |  | It mark all sensitive fields as NOINPUT if is set to Y Once set to Y, the field can be unset anytime, and there will be no restrictions enforced on it being removed. When a non privilege user logs in and opens any record for amendment, depending on the defined sensitive fields, they'll be marked as NOINPUT |
| 13 | `EB.ENC.ENABLE.ENQ.FLD.ENCRYPT` | `EbEncParameter_EnableEnqFldEncrypt` | TField |  | It decrypts the sensitive fields if enquiry is launched from privileged user Once set to Y, the field can be unset anytime, and there will be no restrictions enforced on it being removed. When an enquiry is launched, the runtime for enquiry will refer to value of this new field in EB.ENC.PARAMETER, and if this is set, then it will automatically decrypt the value in the field for display, if the field is one of the sensitive fields for the application |
| 14 | `EB.ENC.LOCAL.REF` | `EbEncParameter_LocalRef` |  |  |  |
| 15 | `EB.ENC.RECORD.STATUS` | `EbEncParameter_RecordStatus` | String |  |  |
| 16 | `EB.ENC.CURR.NO` | `EbEncParameter_CurrNo` | String |  |  |
| 17 | `EB.ENC.INPUTTER` | `EbEncParameter_Inputter` |  |  |  |
| 18 | `EB.ENC.DATE.TIME` | `EbEncParameter_DateTime` |  |  |  |
| 19 | `EB.ENC.AUTHORISER` | `EbEncParameter_Authoriser` | String |  |  |
| 20 | `EB.ENC.CO.CODE` | `EbEncParameter_CoCode` | String |  |  |
| 21 | `EB.ENC.DEPT.CODE` | `EbEncParameter_DeptCode` | String |  |  |
| 22 | `EB.ENC.AUDITOR.CODE` | `EbEncParameter_AuditorCode` | String |  |  |
| 23 | `EB.ENC.AUDIT.DATE.TIME` | `EbEncParameter_AuditDateTime` | String |  |  |
| 24 | `EB.ENC.ENCRYPT.FIELD` | `EbEncParameter_EncryptField` |  |  |  |
| 25 | `EB.ENC.FORCE.MASKING` | `EbEncParameter_ForceMasking` | TField |  | It enables for masking if encryption and decryption routines are not defined It will mask the contents of sensitive fields if the application is launched from non privileged user |
