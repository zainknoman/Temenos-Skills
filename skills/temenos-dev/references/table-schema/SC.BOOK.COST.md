# SC.BOOK.COST — Table Schema

> Source: `INSERTS/I_F.SC.BOOK.COST` in `SC_ScoSecurityPositionUpdate.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `SCCOST.SECURITY.ACCOUNT` | `ScBookCost_SecurityAccount` | TField | Yes | You should enter the id of the portfolio for which you wish to amend a particular security cost price Validation Rules: This is a mandatory field Must be a valid portfolio, existing on the SEC.ACC.MASTER file. This cannot be a dealer book. |
| 2 | `SCCOST.SECURITY.NUMBER` | `ScBookCost_SecurityNumber` | TField | Yes | You should enter in this field the id of the security for which you wish to amend the book cost Validation Rules: This is a mandatory field Must exist on the SECURITY.MASTER file |
| 3 | `SCCOST.DEPOSITORY` | `ScBookCost_Depository` | TField | Yes | You should enter the depository at which the security is held Validation Rules: This is a mandatory field Must exist on the CUSTOMER.SECURITY file and have a CUSTOMER.TYPE of "DEPOSITORY" |
| 4 | `SCCOST.NOMINEE.CODE` | `ScBookCost_NomineeCode` | TField | No | In this field you should enter the nominee code in which name the security is registered. This is an optional field but if the security for which you wish to amend the book cost is held in a nominee name and you do not enter it, the System will report the position does not exist. Validation Rules: Optional field Must exist on the NOMINEE.CODE file |
| 5 | `SCCOST.MATURITY.DATE` | `ScBookCost_MaturityDate` | TField | No | If the particular security for which you wish to amend the book cost is a Kassenobligationen (KO) type security then you should enter the maturity date to assist location of the correct position. Validation Rules: Optional field Input only allowed if the security is a KO type. These are identified by the underlying SUB.ASSET.TYPE file KASSENOBLIGATIONEN field having been set to "Y" |
| 6 | `SCCOST.INTEREST.RATE` | `ScBookCost_InterestRate` | TField | No | If the particular security for which you wish to amend the book cost is a Kassenobligationen (KO) type security then you should enter the interest rate to assist location of the correct position.Validation Rule. Validation Rules: Optional field Input only allowed if the security is a KO type. These are identified by the underlying SUB.ASSET.TYPE file KASSENOBLIGATIONEN field having been set to "Y" |
| 7 | `SCCOST.TRANSACTION` | `ScBookCost_Transaction` | TField | Yes | You should now enter an applicable transaction code to specify whether the amendment to the underlying book cost is a reduction or an increase. Validation Rules: Mandatory field Must exist on the SC.TRANS.NAME file |
| 8 | `SCCOST.BOOK.COST.SEC.CCY` | `ScBookCost_BookCostSecCcy` | TField | No | You may enter in the security currency the amount by which you require to adjust the book cost. The fugure you supply will be an absolute amount. Whether the cost is to or decreased depends upon the SECURITY.TRANS code you specify in the TRANSACTION field. This is an optional field but at least one of this field, BOOK.COST.REF.CCY and BOOK.COST.BSE.CCY must be populated to cause the others to be calculated automatically using the appropriate exchange rate from the currency files. If you do not wish this field to be updated, you should enter a "0" (zero). Validation Rules: Standard amount, including "0" (zero) if no change required Optional field |
| 9 | `SCCOST.BOOK.COST.REF.CCY` | `ScBookCost_BookCostRefCcy` | TField | No | You may enter in the portfolio reference currency the amount by which you require to adjust the book cost. The fugure you supply will be an absolute amount. Whether the cost is to or decreased depends upon the SECURITY.TRANS code you specify in the TRANSACTION field. This is an optional field but at least one of this field, BOOK.COST.SEC.CCY and BOOK.COST.BSE.CCY must be populated to cause the others to be calculated automatically using the appropriate exchange rate from the currency files. If you do not wish this field to be updated, you should enter a "0" (zero). Validation Rules: Standard amount field Optional |
| 10 | `SCCOST.BOOK.COST.BSE.CCY` | `ScBookCost_BookCostBseCcy` | TField | No | You may enter in the base currency the amount by which you require to adjust the book cost. The fugure you supply will be an absolute amount. Whether the cost is to or decreased depends upon the SECURITY.TRANS code you specify in the TRANSACTION field. This is an optional field but at least one of this field, BOOK.COST.SEC.CCY and BOOK.COST.REF.CCY must be populated to cause the others to be calculated automatically using the appropriate exchange rate from the currency files. If you do not wish this field to be updated, you should enter a "0" (zero). Validation Rules: Standard amount field Optional |
| 11 | `SCCOST.TRADE.DATE` | `ScBookCost_TradeDate` | TField |  | You should enter in this field the date on which the decision was made to amend the underlying book cost. If left blank, will default to the current system date. Validation Rules: Must be a valid date Must be less than or equal to the VALUE.DATE |
| 12 | `SCCOST.VALUE.DATE` | `ScBookCost_ValueDate` | TField |  | This field should contain the value date from which the book cost adjustment is to apply. If left blank, will default to today's date. Validation Rules: Must be a valid date Must be equal to or greater than the TRADE.DATE |
| 13 | `SCCOST.SUB.ACCOUNT` | `ScBookCost_SubAccount` | TField | No | Specifies the sub account for depository. SUB.ACCOUNT must be defined in the CUSTOMER.SECURITY record of the respective DEPOSITORY. Validation Rules: 1-16 numeric characters Must be defined in CUSTOMER.SECURITY record for the depository Optional field. |
| 14 | `SCCOST.NARRATIVE` | `ScBookCost_Narrative` |  |  |  |
| 15 | `SCCOST.LOCAL.REF` | `ScBookCost_LocalRef` |  |  |  |
| 16 | `SCCOST.OVERRIDE` | `ScBookCost_Override` |  |  |  |
| 17 | `SCCOST.RECORD.STATUS` | `ScBookCost_RecordStatus` | String |  |  |
| 18 | `SCCOST.CURR.NO` | `ScBookCost_CurrNo` | String |  |  |
| 19 | `SCCOST.INPUTTER` | `ScBookCost_Inputter` |  |  |  |
| 20 | `SCCOST.DATE.TIME` | `ScBookCost_DateTime` |  |  |  |
| 21 | `SCCOST.AUTHORISER` | `ScBookCost_Authoriser` | String |  |  |
| 22 | `SCCOST.CO.CODE` | `ScBookCost_CoCode` | String |  |  |
| 23 | `SCCOST.DEPT.CODE` | `ScBookCost_DeptCode` | String |  |  |
| 24 | `SCCOST.AUDITOR.CODE` | `ScBookCost_AuditorCode` | String |  |  |
| 25 | `SCCOST.AUDIT.DATE.TIME` | `ScBookCost_AuditDateTime` | String |  |  |
| 26 | `SCCOST.CG.BASE.UPDATE` | `ScBookCost_CgBaseUpdate` | TField |  |  |
