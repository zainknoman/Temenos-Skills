# TFS.TRANSACTION — Table Schema

> Source: `INSERTS/I_F.TFS.TRANSACTION` in `TT_TellerFinancialService.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `TFS.TXN.DESCRIPTION` | `TfsTransaction_Description` |  |  |  |
| 2 | `TFS.TXN.CONSOL.NARR` | `TfsTransaction_ConsolNarr` |  |  |  |
| 3 | `TFS.TXN.CONSOL.EXCLUDE` | `TfsTransaction_ConsolExclude` | TField | No | Even if Consolidation is enabled, it is possible to specify a particular transaction type to be excluded from Consolidation by setting this field to YES. TFS will simply not consider any Legs that have this TFS Transaction type when it consolidates the legs. These TFS legs would directly hit the Customer account. Validation Rules: ================= Optional Input. Allowed Values: YES or NO Default Value: NO If the implementation uses T24 Cheque Collection process to manage the life of the cheque (configured via CQ.PARAMETER), then this field needs to be set to YES for those TFS.TRANSACTION records used for recording Collection or Clearing Cheques. This is because we need the CHEQUE.COLLECTION record to have the Primary Account and not the Washthru Account. |
| 4 | `TFS.TXN.INTERFACE.TO` | `TfsTransaction_InterfaceTo` | TField | Yes | This field forms part of the Interface parameters. Defines the underlying modules that TFS can interface to. Input in this field will be validated against, at the time of setting up TFS.TRANSACTION. Validation Rules: ================= Allowed Values: FT, TT and DC. Mandatory Input. |
| 5 | `TFS.TXN.INTERFACE.AS` | `TfsTransaction_InterfaceAs` | TField | Yes | This field forms part of the Interface parameters. This is used to map the transaction type of the underlying module. A transaction which charges the underlying must not be used. Validation Rules: Mandatory Input if INTERFACE.TO is FT or TT. Otherwise Input not allowed. If INTERFACE.TO is FT, then input in this field must be a valid record in FT.TXN.TYPE.CONDITION. If INTERFACE.TO is TT, then input in this field must be a valid record in TELLER.TRANSACTION. |
| 6 | `TFS.TXN.OFS.VERSION` | `TfsTransaction_OfsVersion` | TField | No | This field forms part of the Interface Parameters. This field holds the Version that needs to be used when interfacing with the underyling application defined in the associated INTERFACE.TO field. The value in this field overrides the setup in the corresponding field in TFS.PARAMETER. If not setup here, then TFS will use the setup defined in TFS.PARAMETER. Validation Rules: ================= Optional Input. Input must be a valid record in VERSION table. If the associated INTERFACE.TO is FT, then this should be a valid version for FUNDS.TRANSFER Application If the associated INTERFACE.TO is TT, then this should be a valid version for TELLER Application If the associated INTERFACE.TO is DC, then this should be a valid version for DATA.CAPTURE Application. |
| 7 | `TFS.TXN.SURROGATE.AC` | `TfsTransaction_SurrogateAc` | TField | No | In the normal course of operations, a teller might be required to do a Cash deposit, Cheque deposit, A/C to A/C transfer between two accounts of the Customer. However, except for the A/C to A/C transfer, the other two transactions above require input in only one of the Account fields, by the user. The internal accounts corresponding to the Till or Cheque suspense are hidden from the user/screen. In TELLER.FINANCIAL.SERVICES (TFS), all these transactions mentioned above will be done on the same screen. However it is not possible to hide a particular field for one multi-value set and show it for another multi-value set (each multi-value set refers to one transaction type - Cash deposit, Cheque Deposit, Transfer etc). To overcome this constraint, TFS has what is called as a 'Surrogate Account' - a temporary place holder and this is the only field that will be displayed on screen. For transaction types like Cash deposit or Cheque deposit, the Surrogate Account field will be automatically defaulted by TFS with the Primary Account - indicating that that is the account to which the transaction will be posted. For account to account transfer, it needs to assume that one side of the transaction is the Primary Account and needs to require the user to input the other account in the Surrogate Account field. In order for it to recognise that a transaction type requires input of another account by the user, this field SURROGATE.AC in TFS.TRANSACTION is used. It tells TFS whether the account input in Surrogate Account field on TFS screen should be treated as the Debit or Credit account of the corresponding TFS Leg. Validation Rules: Optional Input. Allowed Values: ACCOUNT.DR, ACCOUNT.CR or NULL. Recommendation: ================ Instead of having one transaction type as 'Account Transfer', there will be two transaction types 'FROM' and 'TO' (although, both pointing to the same underlying module and its transaction code). The transaction type FROM will have this field set to ACCOUNT.DR and the transaction type TO will have this field set to ACCOUNT.CR |
| 8 | `TFS.TXN.TXN.REV.DC.CR` | `TfsTransaction_TxnRevDcCr` | TField | Yes | This field forms part of DATA.CAPTURE Related parameters. When trying to reverse a TFS leg which has the underlying as TELLER or FUNDS.TRANSFER in History, TFS would undo the entries by creating offsetting DATA.CAPTURE transactions. It will attempt to use the same TRANSACTION codes as in the accounting entries used by the original underlying TELLER or FUNDS.TRANSFER albeit with the REVERSAL.MARK in DC set to Y. However if those transaction codes are not enabled for use in DATA.CAPTURE module (DATA.CAPTURE field in TRANSACTION table must be set to Y), then it needs a default alternate code that it can use for the Credit Reversal Entry. Setup in this field overrides the setup in the corresponding field in TFS.PARAMETER, but only when this transaction type is used. If left blank, then TFS uses the setup defined in TFS.PARAMETER. Validation Rules: ================= Mandatory Input. Input must be a valid record in TRANSACTION table. The DEBIT.CREDIT.IND in TRANSACTION must be set to CREDIT. |
| 9 | `TFS.TXN.TXN.REV.DC.DR` | `TfsTransaction_TxnRevDcDr` | TField | Yes | This field forms part of DATA.CAPTURE Related parameters. When trying to reverse a TFS leg which has the underlying as TELLER or FUNDS.TRANSFER in History, TFS would undo the entries by creating offsetting DATA.CAPTURE transactions. It will attempt to use the same TRANSACTION codes as in the accounting entries used by the original underlying TELLER or FUNDS.TRANSFER albeit with the REVERSAL.MARK in DC set to Y. However if those transaction codes are not enabled for use in DATA.CAPTURE module (DATA.CAPTURE field in TRANSACTION table must be set to Y), then it needs a default alternate code that it can use for the Debit Reversal Entry. Setup in this field overrides the setup in the corresponding field in TFS.PARAMETER, but only when this transaction type is used. If left blank, then TFS uses the setup defined in TFS.PARAMETER. Validation Rules: ================= Mandatory Input. Input must be a valid record in TRANSACTION table. The DEBIT.CREDIT.IND in TRANSACTION must be set to DEBIT. |
| 10 | `TFS.TXN.TXN.CCY.TO.DR.CCY` | `TfsTransaction_TxnCcyToDrCcy` | TField |  | This field forms part of the Currency Conversion Parameters. The CURRENCY field in TELLER.FINANCIAL.SERVICES is the Currency of the 'Transaction' itself and need not be either the Debit or the Credit Currency. A hypothetical example would be a Customer wanting to transfer USD 100 from his/her GBP account to his/her EUR Account. This means that there is a 3rd currency involved. This field allows to define the rate preferences on how the Debit Currency equivalent should be arrived at from the Transaction Currency. Value in this field overrides the setup in the corresponding field in TFS.PARAMETER, but only when this transaction type is used. If left blank, then TFS will use the setup defined in TFS.PARAMETER. Validation Rules: ================= Allowed Input: BUY.SELL, MID.REVAL Default Value: NULL |
| 11 | `TFS.TXN.TXN.CCY.TO.CR.CCY` | `TfsTransaction_TxnCcyToCrCcy` | TField |  | This field forms part of the Currency Conversion Parameters. The CURRENCY field in TELLER.FINANCIAL.SERVICES is the Currency of the 'Transaction' itself and need not be either the Debit or the Credit Currency. A hypothetical example would be a Customer wanting to transfer USD 100 from his/her GBP account to his/her EUR Account. This means that there is a 3rd currency involved. This field allows to define the rate preferences on how the Credit Currency equivalent should be arrived at from the Transaction Currency. Value in this field overrides the setup in the corresponding field in TFS.PARAMETER, but only when this transaction type is used. If left blank, then TFS will use the setup defined in TFS.PARAMETER. Validation Rules: ================= Allowed Input: BUY.SELL, MID.REVAL Default Value: NULL |
| 12 | `TFS.TXN.BUY.FCY.SELL.LCY` | `TfsTransaction_BuyFcySellLcy` | TField |  | This field forms part of the Currency Conversion parameters for this transaction type. In a TFS transaction, if in a Leg, the debit side is Foreign Currency and the Credit Side is Local Currency, the system needs to arrive at a deal rate. This field allows to define the rate preferences for such a transaction - only when this transaction type is used in TFS. Value in this field overrides the setup in the corresponding field in TFS.PARAMETER. If not setup here, then TFS will use the preferences defined in TFS.PARAMETER. Validation Rules: Allowed Values: BUY.SELL, MID.REVAL |
| 13 | `TFS.TXN.BUY.LCY.SELL.FCY` | `TfsTransaction_BuyLcySellFcy` | TField |  | This field forms part of the Currency Conversion parameters for this transaction type. In a TFS transaction, if in a Leg, the debit side is Local Currency and the Credit Side is Foreign Currency, the system needs to arrive at a deal rate. This field allows to define the rate preferences for such a transaction - only when this transaction type is used in TFS. Value in this field overrides the setup in the corresponding field in TFS.PARAMETER. If not setup here, then TFS will use the preferences defined in TFS.PARAMETER. Validation Rules: Allowed Values: BUY.SELL, MID.REVAL |
| 14 | `TFS.TXN.BUY.FCY.SELL.FCY` | `TfsTransaction_BuyFcySellFcy` | TField |  | This field forms part of the Currency Conversion parameters for this transaction type. In a TFS transaction, if in a Leg, the debit side is Foreign Currency and the Credit Side is also Foreign Currency, the system needs to arrive at a deal rate. This field allows to define the rate preferences for such a transaction - only when this transaction type is used in TFS. Value in this field overrides the setup in the corresponding field in TFS.PARAMETER. If not setup here, then TFS will use the preferences defined in TFS.PARAMETER. Validation Rules: Allowed Values: BUY.SELL, MID.REVAL |
| 15 | `TFS.TXN.MKT.EXCH.PROFIT` | `TfsTransaction_MktExchProfit` | TField |  | This field forms part of the Currency Conversion Parameters. When cross currency transactions are done, TELLER application calculates market exchange profit. This field allows to define if market exchange profit should be calculated. Value in this field overrides the setup in the corresponding field in TFS.PARAMETER. If not setup here, then TFS will use the setup defined in TFS.PARAMETER. Validation Rules: ================= Allowed Values: YES or NO. Default Value: Blank - so that TFS inherits the setup defined in TFS.PARAMETER. |
| 16 | `TFS.TXN.RESERVED.16` | `TfsTransaction_Reserved16` | TField |  |  |
| 17 | `TFS.TXN.RESERVED.17` | `TfsTransaction_Reserved17` | TField |  |  |
| 18 | `TFS.TXN.RESERVED.18` | `TfsTransaction_Reserved18` | TField |  |  |
| 19 | `TFS.TXN.RESERVED.19` | `TfsTransaction_Reserved19` | TField |  |  |
| 20 | `TFS.TXN.RESERVED.20` | `TfsTransaction_Reserved20` | TField |  |  |
| 21 | `TFS.TXN.EXC.FROM.TXN.LIMIT` | `TfsTransaction_ExcFromTxnLimit` | TField | No | The TFS.TRANSACTION type that needs to be excluded from transaction limit validation(where the system will validate the transaction amount against a pre-set limit and raise an override if the transaction breaches the limit), this field can be set to YES. Validation Rules: ================= Optional input. Allowed Values: YES or NULL Default Value: NULL |
| 22 | `TFS.TXN.LCY.LIMIT` | `TfsTransaction_LcyLimit` | TField | No | This is the same as in TFS.PARAMETER.If this field is set at the TFS.TRANSACTION level, then it will take precedence over the setup in TFS.PARAMETER, but only when this TFS Transaction type is used. Validation Rules: ================= Optional input. |
| 23 | `TFS.TXN.LIMIT.CCY` | `TfsTransaction_LimitCcy` |  |  |  |
| 24 | `TFS.TXN.CCY.LIMIT` | `TfsTransaction_CcyLimit` |  |  |  |
| 25 | `TFS.TXN.CR.TXN.CODE` | `TfsTransaction_CrTxnCode` | TField | Yes | If this transaction type corresonds to DC, then this field is used to define the transaction code to be used when creating the Credit leg of DC. Validation Rules: Mandatory input if the underlying (INTERFACE.TO) is DC. Otherwise, Input not allowed. Input must be a valid record in TRANSACTION table. The field DEBIT.CREDIT.IND in TRANSACTION must be set to CREDIT. |
| 26 | `TFS.TXN.DR.TXN.CODE` | `TfsTransaction_DrTxnCode` | TField | Yes | If this transaction type corresonds to DC, then this field is used to define the transaction code to be used when creating the Debit leg of DC. Validation Rules: Mandatory input if the underlying (INTERFACE.TO) is DC. Otherwise, Input not allowed. Input must be a valid record in TRANSACTION table. The field DEBIT.CREDIT.IND in TRANSACTION must be set to DEBIT. |
| 27 | `TFS.TXN.CR.ALLOWED.AC` | `TfsTransaction_CrAllowedAc` | TField |  | Noinput field. Used by System during run-time. |
| 28 | `TFS.TXN.DR.ALLOWED.AC` | `TfsTransaction_DrAllowedAc` | TField |  | Noinput field. Used by System during run-time. |
| 29 | `TFS.TXN.CR.ALLOWED.CCY` | `TfsTransaction_CrAllowedCcy` | TField |  | Noinput field. Used by System during run-time. |
| 30 | `TFS.TXN.DR.ALLOWED.CCY` | `TfsTransaction_DrAllowedCcy` | TField |  | Noinput field. Used by System during run-time. |
| 31 | `TFS.TXN.CR.CATEG` | `TfsTransaction_CrCateg` | TField |  | Noinput field. Used by System during run-time. |
| 32 | `TFS.TXN.DR.CATEG` | `TfsTransaction_DrCateg` | TField |  | Noinput field. Used by System during run-time. |
| 33 | `TFS.TXN.DR.CURR.MKT` | `TfsTransaction_DrCurrMkt` | TField |  | Noinput field. Used by System during run-time. |
| 34 | `TFS.TXN.CR.CURR.MKT` | `TfsTransaction_CrCurrMkt` | TField |  | Noinput field. Used by System during run-time. |
| 35 | `TFS.TXN.DENOM.FILTER` | `TfsTransaction_DenomFilter` | TField |  | Noinput field. Used by System during run-time. |
| 36 | `TFS.TXN.CR.DENOM.TYPE` | `TfsTransaction_CrDenomType` | TField |  | Noinput field. Used by System during run-time. |
| 37 | `TFS.TXN.DR.DENOM.TYPE` | `TfsTransaction_DrDenomType` | TField |  | Noinput field. Used by System during run-time. |
| 38 | `TFS.TXN.CHARGE` | `TfsTransaction_Charge` | TField |  | Noinput field. Used by System during run-time. |
| 39 | `TFS.TXN.COMMISSION` | `TfsTransaction_Commission` | TField |  | Noinput field. Used by System during run-time. |
| 40 | `TFS.TXN.TT.SIDE.1` | `TfsTransaction_TtSide1` | TField |  | Noinput field. Used by System during run-time. |
| 41 | `TFS.TXN.AUTO.DENOMINATE` | `TfsTransaction_AutoDenominate` | TField |  | Noinput field. Used by System during run-time. |
| 42 | `TFS.TXN.RESERVED.42` | `TfsTransaction_Reserved42` | TField |  |  |
| 43 | `TFS.TXN.RESERVED.43` | `TfsTransaction_Reserved43` | TField |  |  |
| 44 | `TFS.TXN.RESERVED.44` | `TfsTransaction_Reserved44` | TField |  |  |
| 45 | `TFS.TXN.RESERVED.45` | `TfsTransaction_Reserved45` | TField |  |  |
| 46 | `TFS.TXN.RESERVED.46` | `TfsTransaction_Reserved46` | TField |  |  |
| 47 | `TFS.TXN.RESERVED.47` | `TfsTransaction_Reserved47` | TField |  |  |
| 48 | `TFS.TXN.RESERVED.48` | `TfsTransaction_Reserved48` | TField |  |  |
| 49 | `TFS.TXN.RESERVED.49` | `TfsTransaction_Reserved49` | TField |  |  |
| 50 | `TFS.TXN.RESERVED.50` | `TfsTransaction_Reserved50` | TField |  |  |
| 51 | `TFS.TXN.RESERVED.51` | `TfsTransaction_Reserved51` | TField |  |  |
| 52 | `TFS.TXN.LOCAL.REF` | `TfsTransaction_LocalRef` |  |  |  |
| 53 | `TFS.TXN.OVERRIDE` | `TfsTransaction_Override` |  |  |  |
| 54 | `TFS.TXN.RECORD.STATUS` | `TfsTransaction_RecordStatus` | String |  |  |
| 55 | `TFS.TXN.CURR.NO` | `TfsTransaction_CurrNo` | String |  |  |
| 56 | `TFS.TXN.INPUTTER` | `TfsTransaction_Inputter` |  |  |  |
| 57 | `TFS.TXN.DATE.TIME` | `TfsTransaction_DateTime` |  |  |  |
| 58 | `TFS.TXN.AUTHORISER` | `TfsTransaction_Authoriser` | String |  |  |
| 59 | `TFS.TXN.CO.CODE` | `TfsTransaction_CoCode` | String |  |  |
| 60 | `TFS.TXN.DEPT.CODE` | `TfsTransaction_DeptCode` | String |  |  |
| 61 | `TFS.TXN.AUDITOR.CODE` | `TfsTransaction_AuditorCode` | String |  |  |
| 62 | `TFS.TXN.AUDIT.DATE.TIME` | `TfsTransaction_AuditDateTime` | String |  |  |
