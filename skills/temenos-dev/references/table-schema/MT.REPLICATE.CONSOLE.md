# MT.REPLICATE.CONSOLE — Table Schema

> Source: `INSERTS/I_F.MT.REPLICATE.CONSOLE` in `MT_Framework.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `MT.REP.TABLE.NAME` | `MtReplicateConsole_TableName` | TField | Yes | Specifies the valid T24 table name for which the data record need to be replicated from master to tenant. Validation Rules: Valid entry in PGM.FILE (valid application) Allowed maximum of 40 characters Mandatory field |
| 2 | `MT.REP.RECORD.ID` | `MtReplicateConsole_RecordId` | TField | Yes | Specifies the valid record in master system for the TABLE.NAME value. Validation Rules: Valid values are: Valid entry in t24 table given in TABLE.NAME field Allowed maximum of 50 characters Mandatory field |
| 3 | `MT.REP.TENANT.ID` | `MtReplicateConsole_TenantId` | TField | Yes | This field is to define valid tenant id for which the record from master need to be replicated. The tenant id can be acceptable only when the tenant type group matches with the TENANT.TYPE.CHECK given in this replicate console Validation Rules: Valid entry in MT.TENANT table. Mandatory if no TENANT.GROUP given Allowed maximum of 35 characters. |
| 4 | `MT.REP.TENANT.GROUP` | `MtReplicateConsole_TenantGroup` | TField | Yes | This field is to define valid tenant group for which the record from master need to be replicated. The generic tenant group record (TENANT.GROUP.LEVEL = GENERIC) only can be allowed. Validation Rules: Allowed maximum of 35 characters Valid entry in MT.TENANT.GROUP table Mandatory if no TENANT.ID given |
| 5 | `MT.REP.TARGET.RECORD.ID` | `MtReplicateConsole_TargetRecordId` | TField |  | This field is to define the target record id for the record being replicated to tenant system. For UPDATE request type, it is expected to have same id as RECORD.ID value Though it is expected that the data record to be sent will be the RECORD.ID from the Master system, it is also possible to export the record with a different target id only for request type is FULL or blank. If this is set then the record created or replaced at the tenant system will have the TARGET.RECORD.ID but the content of the RECORD.ID data. Validation Rules: Allowed maximum of 50 characters For REQUEST.TYPE = UPDATE, a. The value defaulted from RECORD.ID , b.Cannot have different target id than RECORD.ID value For other REQUEST.TYPE, it is expected to input target id in the valid format for an application given in TABLE.NAME field. |
| 6 | `MT.REP.TARGET.COMPANY.ID` | `MtReplicateConsole_TargetCompanyId` | TField |  | Specifies the company id from tenant for which the record being replicated from master, this is applicable where a single tenant is defined as the recipient (not for tenant group setup). The MT Administrator must ensure that the company id of tenant is correct.. Validation Rules: Maximum of 9 characters allowed. Input allowed only when TENANT.ID given (not available if TENANT.GROUP has value). |
| 7 | `MT.REP.TENANT.TYPE.CHECK` | `MtReplicateConsole_TenantTypeCheck` | TField |  | This field is used as a safety check to help eliminate human error in executing the message build process. The user must enter the destination system type (LIVE or NON-LIVE) this will be checked against TENANT.TYPE.GROUP (from MT.TENANT.TYPE of MT.TENANT) or TENANT.GROUP.TYPE(from MT.TENANT.GROUP) values of respective tenants to make sure that the data replication is targeted for the correct type of tenants. Validation Rules: Acceptable values are: 1. LIVE - Indicates the t24 instance(tenant system) used as LIVE or PRODUCTION. 2. NON-LIVE - Indicates the t24 instance(tenant system) used for other purpose like DEMO, UAT, TRAINING etc. |
| 8 | `MT.REP.PARAM.ID` | `MtReplicateConsole_ParamId` | TField |  | Used to specify a customised MT system parameter record to override the message format values from DEFAULT parameter record or being defaulted by system. Validation Rules: Maximum of 35 characters allowed. Must be a valid entry in MT.SYSTEM.PARAMETER table |
| 9 | `MT.REP.OFS.PGM.VERSION` | `MtReplicateConsole_OfsPgmVersion` | TField |  | Specifies the pgm version for the TABLE.NAME version (suffix of version id) can be used in OFS message formed during MT.REPLICATE.CONSOLE authorisation which will be part of generated event XML. For example ACCOUNT.PARAMETER,CHECK is a valid version in the tenant system, then the suffix after comma is the value to be used for OFS.PGM.VERSION. The MT Administrator must ensure the value is the correct suffix of valid version in the tenant system. Validation Rules: Maximum of 35 characters allowed/ Takes precedence over the OFS.PGM.VERSION given in MT.TENANT table |
| 10 | `MT.REP.OPERATION.MODE` | `MtReplicateConsole_OperationMode` | TField |  | Specifies the mode of operation for which the MT.REPLICATE.CONSOLE is being used. Only a positive value of EXECUTE will allow the generation of messages, and as a safety mechanism an override will also be given to ensure the correct mode has been used. Validation Rules: Acceptable values are: 1. MAINTAIN :- Indicates the MT.REPLICATE.CONSOLE record is being created or maintained and no event generation will happen. 2. EXECUTE :- Indicates the MT.REPLICATE.CONSOLE record authorisation can proceed with event generation. 3. Blank :- Treated the same as MAINTAIN. 8 characters. |
| 11 | `MT.REP.MESSAGE.COUNT` | `MtReplicateConsole_MessageCount` | TField |  | Specifies the number of events generated (or to be generated) during MT.REPLICATE.CONSOLE authorisation. This field gets updated during validation stage of MT.REPLICATE.CONSOLE in advance if there is no error identified. Value gets cleared when this console record opened for amendment or further processing. Validation Rules: Allowed maximum of 35 characters System updated field |
| 12 | `MT.REP.TENANT.COUNT` | `MtReplicateConsole_TenantCount` | TField |  | Specifies the number of active tenants involved in current console process for which the event generation will or has already occur(red). This field gets updated during validation stage of MT.REPLICATE.CONSOLE in advance if there is no error identified. Value gets cleared when this console record opened for amendment or further processing. Validation Rules: Allowed maximum of 35 characters System generated field |
| 13 | `MT.REP.TENANT.LIST` | `MtReplicateConsole_TenantList` |  |  |  |
| 14 | `MT.REP.PROCESS.STATUS` | `MtReplicateConsole_ProcessStatus` | TField |  | Specifies the status of current console process. This field gets updated during validation stage of MT.REPLICATE.CONSOLE in advance if there is no error identified. Value gets cleared when this console record opened up for amendment or further process. Validation Rules: System updated values are: 1. VAL - indicates that the console has been created and event not planned for event generation. 2. SENT - indicates that the console process has been completed and event also generated via integration framework. Allowed maximum of 35 characters System generated field. |
| 15 | `MT.REP.REQUEST.TYPE` | `MtReplicateConsole_RequestType` | TField | Yes | Specifies the type of request for which the MT.REPLICATE.CONSOLE is being created. The OFS message will be constructed either for: 1. Full record creation or replacement in tenant system. 2. Amendment of an existing record in tenant system. Validation Rules: Valid values are: FULL or blank :- Indicates the MT.REPLICATE.CONSOLE record is being created in the master system to perform full record creation or replacement in the tenant system, hence the OFS message will be formed for the full record of TABLE.NAME read with RECORD.ID in master system. UPDATE :- Indicates the MT.REPLICATE.CONSOLE record is to do an amendment or update of an existing record in the tenant system. The UPDATE.FIELDS is mandatory if REQUEST.TYPE=UPDATE. |
| 16 | `MT.REP.UPDATE.FIELDS` | `MtReplicateConsole_UpdateFields` |  |  |  |
| 17 | `MT.REP.RESERVED.8` | `MtReplicateConsole_Reserved8` | TField |  |  |
| 18 | `MT.REP.RESERVED.7` | `MtReplicateConsole_Reserved7` | TField |  |  |
| 19 | `MT.REP.RESERVED.6` | `MtReplicateConsole_Reserved6` | TField |  |  |
| 20 | `MT.REP.RESERVED.5` | `MtReplicateConsole_Reserved5` | TField |  |  |
| 21 | `MT.REP.RESERVED.4` | `MtReplicateConsole_Reserved4` | TField |  |  |
| 22 | `MT.REP.RESERVED.3` | `MtReplicateConsole_Reserved3` | TField |  |  |
| 23 | `MT.REP.RESERVED.2` | `MtReplicateConsole_Reserved2` | TField |  |  |
| 24 | `MT.REP.RESERVED.1` | `MtReplicateConsole_Reserved1` | TField |  |  |
| 25 | `MT.REP.LOCAL.REF` | `MtReplicateConsole_LocalRef` |  |  |  |
| 26 | `MT.REP.OVERRIDE` | `MtReplicateConsole_Override` |  |  |  |
| 27 | `MT.REP.RECORD.STATUS` | `MtReplicateConsole_RecordStatus` | String |  |  |
| 28 | `MT.REP.CURR.NO` | `MtReplicateConsole_CurrNo` | String |  |  |
| 29 | `MT.REP.INPUTTER` | `MtReplicateConsole_Inputter` |  |  |  |
| 30 | `MT.REP.DATE.TIME` | `MtReplicateConsole_DateTime` |  |  |  |
| 31 | `MT.REP.AUTHORISER` | `MtReplicateConsole_Authoriser` | String |  |  |
| 32 | `MT.REP.CO.CODE` | `MtReplicateConsole_CoCode` | String |  |  |
| 33 | `MT.REP.DEPT.CODE` | `MtReplicateConsole_DeptCode` | String |  |  |
| 34 | `MT.REP.AUDITOR.CODE` | `MtReplicateConsole_AuditorCode` | String |  |  |
| 35 | `MT.REP.AUDIT.DATE.TIME` | `MtReplicateConsole_AuditDateTime` | String |  |  |
