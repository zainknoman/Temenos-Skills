# AA.PAYMENT.RULE.TYPE — Table Schema

> Source: `INSERTS/I_F.AA.PAYMENT.RULE.TYPE` in `AA_PaymentRules.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `AA.PR.DESCRIPTION` | `AaPaymentRuleType_Description` |  |  |  |
| 2 | `AA.PR.PAYMENT.RULE.TYPE` | `AaPaymentRuleType_PaymentRuleType` | TField |  | This field is used to specify how the payment rules will apply to an arrangement. This can have any of the below five options. Current: Payment will be applied on arrangement&apos;s current balances. (e.g. current principal, accrued interest, etc.). This will settle the current balances and doesn�t look at application order. Input to application order will not be allowed for Current Balance repayments. Bill Property: For product with bills and due amounts, payment to the due amounts will be Property wise(e.g. all Principal amounts followed by all Interest amounts, etc.) Bill Date: For product with bills and due amounts, payment to the due amounts will be bill date wise (e.g., all amount on bill 1 followed by all amounts of bill 2, etc.). Pay.In.Advance : Indicates the amount should go into the UNC balance. Advance: If chosen, the payment amount will be applied only on the current balances of Interest, account properties and advance due balances for the charge properties During this settlement, System will raise future bills in advance and these bills will either be in &quot;SETTLED&quot; or &quot;ADVANCED&quot; status. Advance field should be set only for the &quot;Payment&quot; Bill types and valid only for the &quot;DUE&quot; Payment method. Valid for future schedules if the properties belong to the system calculated payment types CONSTANT, PROGRESSIVE and ACTUAL/OTHER (Any charges). Also valid for any bills in Issued status(even for LINEAR/INTEREST.ONLY type). Otherwise, the definition will be void and the repayment amount will be passed to the remainder activity. |
| 3 | `AA.PR.CALC.ROUTINE` | `AaPaymentRuleType_CalcRoutine` | TField |  | This field enables the user to define a routine to indicate how much should be settled to the UNC balance. Input in this field is allowed when the field PAYMENT.RULE.TYPE is equal to PAY.IN.ADVANCE. The remaining amount, if any, would behave in the similar way as excess payment and would follow the REMAINDER.ACTIVITY route. This field must be a valid EB.API id. To enable a java component implementation the EB.API record must have a source type of METHOD and implement an interface defined in the EB.API record HOOK.AA.PAYMENT.TYPE.CALC.ROUTINE. See the EB.API record HOOK.AA.PAYMENT.TYPE.CALC.ROUTINE for the full list of supported interfaces, initially AA.CalculationHook.calculateUncSettledAmount(). |
| 4 | `AA.PR.PAYMENT.METHOD` | `AaPaymentRuleType_PaymentMethod` | TField | Yes | Indicates the Payment is Due from the Customer or is it Payable to the customer. ALLOWED OPTIONS - DUE, PAY Mandatory field Cannot be modified after authorisation |
| 5 | `AA.PR.BILL.TYPE` | `AaPaymentRuleType_BillType` |  |  |  |
| 6 | `AA.PR.VD.CALC.API` | `AaPaymentRuleType_VdCalcApi` | TField | No | This field allows an API to be attached to modify the value date of accounting entry for a deposit funding so that payments during 'grace' period could be managed. API has 4 arguments in total, This field also allows an API to be attached to provide whether repayment required for Particular bill or Not,eventhough we configured in Payment Rules Condition IN � Arrangement reference IN � Bill reference which is being processed for payment IN � Bill details record(of the Bill reference) being processed for payment Out � New Value date to be used in Accounting,Pass "Notrequired" in 2nd Param If dont want to settle a Particular bill Validation Rules Should be a valid entry in EB.API record Optional field |
| 7 | `AA.PR.FWD.BILL.SETTLE` | `AaPaymentRuleType_FwdBillSettle` | TField |  | When an early repayment of a bill component has to be included for updating delinquency amount of subsequent bills (as there is no outstanding bill whose delinquency amount could be reduced), this attribute should be configured to YES. It should be noted that this attribute will be meaningful only when the overdue condition has the required setup for delinquency processing. When the option 'YES' is selected, on making an early repayment, the repayment amount will be updated in activity balances. During subsequent make due processing of any scheduled bill, system will reduce the delinquency amount to the extent of repayment amount. Any ageing processing will continue to happen based on the outstanding delinquency amount. In case the outstanding delinquency amount is 0 or within tolerance, ageing status of the bill will be marked as SETTLED during make due activity of the bill, When the option is not selected, during early repayment, no details will be captured in activity balances. During subsequent make due processing of any scheduled bill, both the outstanding total amount and outstanding delinquency amount will have the same values. Allowed Options - YES or Null |
| 8 | `AA.PR.BILL.STATUS` | `AaPaymentRuleType_BillStatus` | TField | No | This field indicates that the payment is to settle only the Overdue Bills. It is an optional Field. Allowed options - Aging and Null. Validations: Input is allowed only for the BILL.DATE and BILL.PROPERTY Payment Rule Type. |
| 9 | `AA.PR.RECORD.STATUS` | `AaPaymentRuleType_RecordStatus` | String |  |  |
| 10 | `AA.PR.CURR.NO` | `AaPaymentRuleType_CurrNo` | String |  |  |
| 11 | `AA.PR.INPUTTER` | `AaPaymentRuleType_Inputter` |  |  |  |
| 12 | `AA.PR.DATE.TIME` | `AaPaymentRuleType_DateTime` |  |  |  |
| 13 | `AA.PR.AUTHORISER` | `AaPaymentRuleType_Authoriser` | String |  |  |
| 14 | `AA.PR.CO.CODE` | `AaPaymentRuleType_CoCode` | String |  |  |
| 15 | `AA.PR.DEPT.CODE` | `AaPaymentRuleType_DeptCode` | String |  |  |
| 16 | `AA.PR.AUDITOR.CODE` | `AaPaymentRuleType_AuditorCode` | String |  |  |
| 17 | `AA.PR.AUDIT.DATE.TIME` | `AaPaymentRuleType_AuditDateTime` | String |  |  |
| 18 | `AA.PR.PAYMENT.RULE.ACTIVITY` | `AaPaymentRuleType_PaymentRuleActivity` | TField |  | To capture the repayment rules activity that will be triggered on the arrangements under a Facility. When a combined payment is made at Facility, payment priority will trigger the repayment rules activity defined in this field to the arrangement under Facility. Underlying arrangement will make the payment based on the repayment rule defined in the individual arrangement. Any remainder amount will be allocated to another drawing arrangement. |
| 19 | `AA.PR.TAX.INCLUSIVE` | `AaPaymentRuleType_TaxInclusive` | TField |  | This field indicates if tax to be collected. Allowed values are Yes/No/Null. Yes � Tax will be calculated and collected at the time of repayment of accrued interest and advance payment. No/Null � Existing feature. Tax will not be calculated during repayment i.e Tax will be calculated during Make Due. Validation Rules: 1. No change field 2. Allowed only for Lending Product Line |
| 20 | `AA.PR.MAKE.DUE` | `AaPaymentRuleType_MakeDue` | TField |  | This field indicates advance repayment and ACC balances should post make due entries for Interest and Tax (if applicable). Allowed values are Yes/No/Null. Yes � Post due entries No/Null � Due entries will not posted Validation Rules: 1. No change field 2. Make due should be Yes if Tax inclusive is set 3. Allowed only for Lending Product Line |
| 21 | `AA.PR.ADVANCE.SETTLE.DATE` | `AaPaymentRuleType_AdvanceSettleDate` | TField |  | Allowed values are Null and Due. When an advance payment rule type has Advance settlement date set to Due, the advance payment received should be parked in a new asset type &quot;ADV&lt;ACCOUNT&gt;&quot;. This advance payment should be settled as advance repayments to the bills on the respective due dates. |
