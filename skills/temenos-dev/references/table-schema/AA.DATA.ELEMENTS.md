# AA.DATA.ELEMENTS — Table Schema

> Source: `INSERTS/I_F.AA.DATA.ELEMENTS` in `AA_Framework.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `AA.DESCRIPTION` | `AaDataElements_Description` |  |  |  |
| 2 | `AA.TYPE` | `AaDataElements_Type` | TField | Yes | This field defines if the data element is a field or a balance. Allowed values : - FIELD - BALANCE* - PROPERTY *BALANCE is currently not in scope of development. This value is introduced for future use. Refer section 7. - When the "TYPE" is 'Field' then 'Data Source' is mandatory. - When the "TYPE" is 'Balance' then 'Data Source' can only be ARRANGEMENT. Error to be thrown in case user chooses a different option stating "SOURCE should be ARRANGEMENT when TYPE is BALANCE". - When "TYPE" is 'Property' then, no other input is required in the table. When Pricing Grid is defined, this should be an Interest property. Mandatory field. |
| 3 | `AA.DATA.SOURCE` | `AaDataElements_DataSource` | TField | Yes | This field defines the source from where the data element is retrieved. It allows one of the following option: - ARRANGEMENT - CUSTOMER - COLLATERAL CUSTOMER, COLLATERAL are applications from where a data can be directly referenced. In case its an ARRANGEMENT, the data can be referenced from an existing arrangement or refer to INTEREST or ACCOUNT Property Class. Conditional Mandatory field. |
| 4 | `AA.ARRANGEMENT.LEVEL` | `AaDataElements_ArrangementLevel` | TField | Yes | This field is allowed for input only when DATA.SOURCE field is chosen as ARRANGEMENT. At runtime the system either retrieves the data value from the arrangement being processed or from its parent. Allowed Values: - Self - Parent In case this is left blank (NULL), system should default the value as 'Self' arrangement during validation. Conditional Mandatory field. |
| 5 | `AA.ARRANGEMENT.SOURCE` | `AaDataElements_ArrangementSource` | TField | Yes | This field is allowed for input only when DATA.SOURCE field is ARRANGEMENT. - Allows only 2 property class � INTEREST and ACCOUNT. Required if a source field will be specified. Conditional Mandatory field. |
| 6 | `AA.RESERVED.1` | `AaDataElements_Reserved1` | TField |  |  |
| 7 | `AA.FIELD` | `AaDataElements_Field` | TField | Yes | - Valid field name in Transact in one of the applications; CUSTOMER, COLLATERAL or the ARRANGEMENT. - This field indicates the source where the data is located. - When the TYPE is 'FIELD', then the actual field is validated for its presence against the respective Standard Selection tables. In case of an ARRANGEMENT, the field is validated against the ARRANGEMENT SOURCE mentioned (i.e.) either the AA.ARRANGEMENT table or respective AA.ARR.PROPERTY. Mandatory field. |
| 8 | `AA.FIELD.RULE` | `AaDataElements_FieldRule` | TField | No | Rules to specify the multi-value/sub-value position to compare if the field belong to an associated sub-value/multi-value. Optional field. |
| 9 | `AA.BALANCE` | `AaDataElements_Balance` | TField |  | Balance to be retrieved. Refers to a valid AA balance type when FIELD is set to 'Balance'. |
| 10 | `AA.DATATYPE` | `AaDataElements_Datatype` | TField | Yes | - The DATA TYPE should be defaulted by referencing the SOURCE application. - A system updated field indicating the IN2 type of the field. - This is required to allow the UX to understand what type of field to display to the user for input. - It is allowed for modification by the user. User input to be retained if defaulted value is modified. When DataCheckFile or OptionList is input then Datatype is a mandatory field. When Datatype is input then it must be valid datatype present for the field. When a valid datatype from EB.TABLE.DATA.TYPE is input then it must be validated against the datatype available for the field. |
| 11 | `AA.DATA.CHECK.FILE` | `AaDataElements_DataCheckFile` | TField | No | Valid checkfile available in Transact. - If the Field is linked to a checkfile, this system updated field will contain the table name. - The UX can then pre-fetch valid values to present to the user.(when grid is being defined) Optional field. If system defaults the DataCheckFile then it will be restricted to change. When check file is input then it must match the datatype input. When check file is of type VIRTUAL then the datatype must only be IN2A. |
| 12 | `AA.DATA.SELECTION` | `AaDataElements_DataSelection` | TField | Yes | - This field is mandatory when DATA.CHECKFILE is input. - If a Data Checkfile is specified, the user can supply selection criteria so the UX can filter the list of IDs to present to the user. - System to validate, if the field name mentioned is a valid one under the checkfile. - The field name and should be separated by an operand and then followed by the value. - If the format is incorrect then an error message should be displayed. - For ex: SECTOR == "2000" - There can only be one condition indicated in this field for now. The format of the field is "Field name" OPERAND "Value" Allowed operands are == and != Conditional Mandatory field. |
| 13 | `AA.CATCH.ALL` | `AaDataElements_CatchAll` | TField | Conditional | This field defines whether a catch all "value" can be specified in a pricing grid and if so whether it is Mandatory or Optional for a grid definition. Allowed Values: - No - Mandatory - Optional Value 'No' indicates that default values cannot be defined in pricing grid. Catch All is not allowed when FIELD is 'PROPERTY'. Mandatory field. |
| 14 | `AA.CATCH.ALL.VALUE` | `AaDataElements_CatchAllValue` | TField | Conditional | A multi-language field to create a meaningful "label" for the catch all value. This will exclusively be used by the UX. - Value is mandatory when 'CATCH.ALL' is Mandatory and Optional. Conditional Mandatory field. |
| 15 | `AA.OVERRIDE` | `AaDataElements_Override` |  |  |  |
| 16 | `AA.RECORD.STATUS` | `AaDataElements_RecordStatus` | String |  |  |
| 17 | `AA.CURR.NO` | `AaDataElements_CurrNo` | String |  |  |
| 18 | `AA.INPUTTER` | `AaDataElements_Inputter` |  |  |  |
| 19 | `AA.DATE.TIME` | `AaDataElements_DateTime` |  |  |  |
| 20 | `AA.AUTHORISER` | `AaDataElements_Authoriser` | String |  |  |
| 21 | `AA.CO.CODE` | `AaDataElements_CoCode` | String |  |  |
| 22 | `AA.DEPT.CODE` | `AaDataElements_DeptCode` | String |  |  |
| 23 | `AA.AUDITOR.CODE` | `AaDataElements_AuditorCode` | String |  |  |
| 24 | `AA.AUDIT.DATE.TIME` | `AaDataElements_AuditDateTime` | String |  |  |
| 25 | `AA.USED.PROP.CLASS` | `AaDataElements_UsedPropClass` |  |  |  |
| 26 | `AA.EVALUATION.TYPE` | `AaDataElements_EvaluationType` | TField |  | Field to define the method in which the criterion values are evaluated in the pricing grid. This is to identify whether the data element value should be considered as a single value or range of values while performing Level/Band calculations. Range � Applicable for all numeric and amount based values which includes Tier Amounts with Band or Level, financial ratios, etc. Equal � This is for exact matching of the criterion values. Equal will be the default value of this field. If no value is given, system will consider the option "EQUAL" for exact matching. Validation Rules: 1. To specify which comparison type should be used for grid evaluation. 2. Only RANGE and EQUAL supported currently. 3. The default value is EQUAL |
| 27 | `AA.TIER.EXCLUSIVE` | `AaDataElements_TierExclusive` | TField |  |  |
| 28 | `AA.LEVEL.TYPE` | `AaDataElements_LevelType` | TField |  | Field to specify the level of the Parent arrangement. This is defined along with the field ARRANGEMENT.LEVEL. The Parent arrangement could be a Facility or a Deal. Allowed Values are 1. Deal - The criterion values in the pricing grid refers to the values at deal level. 2. Facility - The criterion values in the pricing grid refers to the values at facility level. Validation Rules: 1. Allowed only if ARRANGEMENT.LEVEL is PARENT. 2. Default Value - Facility Example: If UTLCOMMITMENT is one of the criterion in pricing grid to determine the margin, the LEVEL.TYPE � Deal, will enable the system to consider the total aggregate utilization at the deal level to determine the margin for the drawing arrangement. |
| 29 | `AA.DATA.ROUTINE` | `AaDataElements_DataRoutine` | TField |  | Field to specify a routine which will be used to fetch data element�s value. An EB.API Id with a source type HOOK which implements an interface defined in the EB.API record AA.DATA.ELEMENTS.DATA.ROUTINE.HOOK. The field supports the Calculation.getDataElementValue() method. The Calculation class is in the com.temenos.t24.api.hook.arrangement package which is in AA_CalculationHook.jar shipped with T24. Validation Rules: Should be a valid record in EB.API. Example: A sample routine will be released from core AA.LOCAL.GET.UTILISED.RATIO which will return the Drawdown in % as on the give effective date. This routine will have the logic as, Drawdown % = (Loan Utilised Amount / Total Loan Amount) * 100 Loan Utilised Amount will also include the Disbursement Bills (if any) in issued status as a given effective date. |
