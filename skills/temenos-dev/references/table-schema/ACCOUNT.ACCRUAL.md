# ACCOUNT.ACCRUAL — Table Schema

> Source: `INSERTS/I_F.ACCOUNT.ACCRUAL` in `IC_Config.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `AC.ACR.MTH.END.UPTO.DAY` | `AccountAccrual_MthEndUptoDay` | TField | Yes | Specifies the day of the month on which monthly Interest Accrual processes will take place. If the day specified falls on a non-working day, the Accrual will take place on the previous working day. The accrual calculation will include the month end to date if LAST.DAY.INCLUSIVE is set to Y, otherwise the calculation will not include this date. Validation Rules: 1-2 numeric characters in the range 1 to 31. At present only 31 is allowed. Mandatory input. Cannot be changed. |
| 2 | `AC.ACR.LAST.DAY.INCLUSIVE` | `AccountAccrual_LastDayInclusive` | TField | Yes | Specifies whether interest calculations for application are inclusive or exclusive of the capitalisation date. Assuming MTH.END.UPTO.DAY (field 1) contains 20 and an Account also has Interest Capitalisation on 20th of the month, Monthend Accrual processing and interest application will both take place on the 20th of each month (or on the previous working day if 20th is not a working day). If this field contains Y, the amount booked to the Customer's Account will include interest calculated on balances with value up to and including 20th. The entry booked to the Account will have a value date of 21st. If this field contains NO, the amount booked to the Customer's Account will only include interest calculated on balances with value up to and including the last calender day before 20th. The entry booked to the Account will have a value date of the day after the last balance included. Validation Rules: Y (inclusive), N or NO (exclusive). Mandatory input. Cannot be changed once a customer account has been opened in the company. |
| 3 | `AC.ACR.EXCLUSIVE.TYPE` | `AccountAccrual_ExclusiveType` | TField |  | No helptext available. |
| 4 | `AC.ACR.APP.POSTING.DAY` | `AccountAccrual_AppPostingDay` | TField | Yes | Specifies whether interest is to appear on a Customer statement on the date it is calculated or on the following day. If NEXT is specified, details for producing the interest entries are written in a temporary file. The entries are booked at the start of end-of-day processing on the next working day, by the program IC.POST.NEXT. The Profit and Loss entries are booked on application (Capitalisation) day, regardless of the contents of this field. Validation Rules: NEXT or TODAY or any abbreviation thereof. (Mandatory input.) Cannot be changed. |
| 5 | `AC.ACR.INCLUDE.CHARGES` | `AccountAccrual_IncludeCharges` | TField | Yes | Specifies whether Account charges are to be accrued every month end, or only booked to Profit and Loss when they are applied. Account charges may be applied monthly, quarterly or six-monthly as specified in the Company table, always at the end of a calendar month. Validation Rules: Y (Yes), N or NO. (Mandatory input.) |
| 6 | `AC.ACR.DEFLT.ACCR.TYPE` | `AccountAccrual_DefltAccrType` |  |  |  |
| 7 | `AC.ACR.DEFLT.ACCR.FQU` | `AccountAccrual_DefltAccrFqu` |  |  |  |
| 8 | `AC.ACR.ACCRUAL.GRP` | `AccountAccrual_AccrualGrp` |  |  |  |
| 9 | `AC.ACR.GRP.ACCR.TYP` | `AccountAccrual_GrpAccrTyp` |  |  |  |
| 10 | `AC.ACR.GRP.ACCR.FQU` | `AccountAccrual_GrpAccrFqu` |  |  |  |
| 11 | `AC.ACR.ACCRUAL.CAT` | `AccountAccrual_AccrualCat` |  |  |  |
| 12 | `AC.ACR.CATEG.TYPE` | `AccountAccrual_CategType` |  |  |  |
| 13 | `AC.ACR.CAT.ACCR.FQU` | `AccountAccrual_CatAccrFqu` |  |  |  |
| 14 | `AC.ACR.CONVERT.FCY.TAX` | `AccountAccrual_ConvertFcyTax` | TField | No | This field is for future use, it specifies whether or not Tax calculated in foreign currency should be converted to Local Currency before it is booked to a Tax Account. Validation Rules: Y or N(o) or Nill (Must be NO at present.) (Optional input. No input means Tax is booked in the currency in which it is calculated). |
| 15 | `AC.ACR.AUTO.SETTLE` | `AccountAccrual_AutoSettle` | TField |  | Specifies whether the system must automatically raise the accounting transactions to move the IC P&amp;L to xxxxxSP when an account is suspended and then move the xxxxxSP to P&amp;L when the suspense is removed. If the indicator on ACCT.GROUP.CONDITION is not set, then the indicator on ACCOUNT.ACCRUAL is checked. Valid Option : 'YES', 'NO' or Null |
| 16 | `AC.ACR.VAL.DATE.ADJUST` | `AccountAccrual_ValDateAdjust` | TField |  | Can be used to adjust the number of calendar days forward that the value date of an account balance can be adjusted. For example certain countries require that interest accruals take place on the opening balance of an account rather than the closing balance. Setting this field to 1 will have this effect. |
| 17 | `AC.ACR.WRITE.ACCR.ACCT` | `AccountAccrual_WriteAccrAcct` | TField | No | Optional input. YES indicates that the information only ACCR.ACCT.XX for accounts accruals are written out. Where XX indicates DR, DR2, CR, CR2, and CH NO or null indicattes that the records are not written. NB: The INFO.ACCT.XX application can be used to view accrual information for an individual account. |
| 18 | `AC.ACR.COMP.ACC.RTN` | `AccountAccrual_CompAccRtn` | TField |  | Identifies a local routine that can be called to adjust the compensation account data. This routine will be called with 1 multi valued argument delimited by field marks. The argument data is as follows:- Field 1 - the lead acount number i.e. the account being compensated to, this is for information only Field 2 - the start date of the calculation period, this can be adjusted if required Field 3 - The end date of the calculation period, this is for information only Field 4 - Indicates the type of processing taking palce, and is information only, "S" = capitalisation, "A" = accrual, "C" = back value correction, "I" = information only, no accounting entries are posted nb: this can be used via the INFO.ACCT.XX routines for testing purposes where XX indicates the type of interest calculation DR,DR2, CR, CR2, CH Field 5 - the current list of compensation accounts delimited by value marks marks, this can be adjusted by the local routine Field6 - associated with field5 this can be returned from the local routine and can contain the start date when the balances for the relevant compensation account in field5 are included in the compensation group balances Field7 - associated with field5 this can be returned from the local routine and can contain the end date when the balances for the relevant compensation account in field5 are excluded from the compensation group balances An example follows of how this local routien could be used to adjust the calculation of the compensation group balances. Acount 100002 was compensated to account 10001 from the second of the month to the tenth of the month. The calcuation period is the 1st to the 31st of the month. Account 100002 has a balance of 1000 for the whole period. Account 100001 has a balance of 2000 for the whole period. The calculation takes place on the 2nd of the month and is projected to the end of the month. Normally the compensation group balance will be 3000 for the whole month. However if the local routine is called the compensation balances can be calculated as follows 1st 2000 2nd 3000 10th 2000 nb: the above example will require a local development to store the start and end dates of the compensation group memberships. |
| 19 | `AC.ACR.RESERVED1` | `AccountAccrual_Reserved1` | TField |  |  |
| 20 | `AC.ACR.RECORD.STATUS` | `AccountAccrual_RecordStatus` | String |  |  |
| 21 | `AC.ACR.CURR.NO` | `AccountAccrual_CurrNo` | String |  |  |
| 22 | `AC.ACR.INPUTTER` | `AccountAccrual_Inputter` |  |  |  |
| 23 | `AC.ACR.DATE.TIME` | `AccountAccrual_DateTime` |  |  |  |
| 24 | `AC.ACR.AUTHORISER` | `AccountAccrual_Authoriser` | String |  |  |
| 25 | `AC.ACR.CO.CODE` | `AccountAccrual_CoCode` | String |  |  |
| 26 | `AC.ACR.DEPT.CODE` | `AccountAccrual_DeptCode` | String |  |  |
| 27 | `AC.ACR.AUDITOR.CODE` | `AccountAccrual_AuditorCode` | String |  |  |
| 28 | `AC.ACR.AUDIT.DATE.TIME` | `AccountAccrual_AuditDateTime` | String |  |  |
