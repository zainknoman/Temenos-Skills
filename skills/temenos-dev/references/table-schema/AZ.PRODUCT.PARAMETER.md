# AZ.PRODUCT.PARAMETER — Table Schema

> Source: `INSERTS/I_F.AZ.PRODUCT.PARAMETER` in `AZ_Config.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `AZ.APP.DESCRIPTION` | `AzProductParameter_Description` |  |  |  |
| 2 | `AZ.APP.LOAN.DEPOSIT` | `AzProductParameter_LoanDeposit` | TField | Yes | Indicates the type of product being defined. One of the following options is available for selection: DEPOSIT LOAN Deposit products have balances greater than zero and earn credit interest as per the rate of interest, defined by the Bank Loan products begin with zero balances and have negative balances. The negative balances incur debit interest as per the rate of interest defined. Mandatory input |
| 3 | `AZ.APP.ALLOWED.CATEG` | `AzProductParameter_AllowedCateg` | TField |  | The default category to which the All in One product is allocated. It is ensured by the system that all AZ accounts opened under this product, belong to the category entered here. The category code is used to classify financial transactions in T24 according to the type of business operations or product type. The first two digits represents the highest level classification and next three digits represents sub classification which provides a clear definition of the profit and loss or the product type. The use of these codes with personal customer characteristics (like Residence, Status, Sector code etc.,) will enable the Banks to produce balance sheets and returns reflecting a coordinated and structured view of its operation, by directing business transactions to their appropriate place in the report. Validation Rules 5-7 Numeric Codes It must be an existing code in the CATEGORY table For customer Accounts, it must be in the range of 1000 to 9999 The category is defaulted to AZ.ACCOUNT from the corresponding ACCOUNT record and validated against the category code defined here. |
| 4 | `AZ.APP.DR.TXN.CODE` | `AzProductParameter_DrTxnCode` | TField | Yes | Defines the Transaction Code to be used on the Debit entry generated by AZ. The Transaction Code of the entry is used to determine the references and narratives to be printed on statements. The Debit/Credit indicators within the Transaction Code table are not used to verify this input. The transaction code defined here should be used or duplicated in any other field. If the ACCT.SYNC field is "Y" -then the transaction code defined here might be used as a default for the other leg of the transaction that it not affecting the loan / deposit when the transaction is triggered from AZ.ACCOUNT. To exemplify, In Credit Card type of loan, if the loan repayment takes place. Dr. will be to the internal/customer/nominated a/c and credit to the loan a/c. As debit side of the transaction is not affecting the loan (debit to a customer a/c)- the transaction code will be one inscribed in this field. Validation Rules 1-3 numeric character Transaction code. (Mandatory input.) Must be an existing code in Transaction Table. |
| 5 | `AZ.APP.CR.TXN.CODE` | `AzProductParameter_CrTxnCode` | TField | Yes | Defines the Transaction Code to be used on the Credit entry generated by AZ. The Transaction Code of the entry is used to determine the references and narratives to be printed on statements. The Debit/Credit indicators within the Transaction Code table are not used to verify this input. If the ACCT.SYNC field is "Y" -then the transaction code defined here might be used as a default for the other leg of the transaction that it not affecting the loan. The transaction code defined here should be used or duplicated in any other field. See example in DR.TXN.CODE. Validation Rules 1-3 numeric character Transaction code. (Mandatory input.) Must be an existing code in Transaction Table. |
| 6 | `AZ.APP.MINIMUM.TERM` | `AzProductParameter_MinimumTerm` | TField |  | This field defines the Minimum Term required for the AZ Loan or Deposit accounts created under this product. Validation Rules 2 Numeric and 1 Alpha Character Permitted values for Alpha Characters: D - Number of days M - Number of months Y - Number of years Must be less than or equal to Maximum Term Negative values not permitted |
| 7 | `AZ.APP.MAXIMUM.TERM` | `AzProductParameter_MaximumTerm` | TField |  | This field defines the Maximum Term allowed for the AZ Loan or Deposit accounts created under this product. Validation Rules 2 Numeric and 1 Alpha Character Permitted values for Alpha Characters: D - Number of days M - Number of months Y - Number of years Must be greater than or equal to Minimum Term Negative values not permitted |
| 8 | `AZ.APP.MINIMUM.AMOUNT` | `AzProductParameter_MinimumAmount` | TField | No | The minimum amount of Deposit or Loan for this product is defined under this field. While opening accounts under this product, the amount of the account is validated against the amount defined here, for minimum value. Validation Rules 1 to 20 type AMT (standard amount format) Characters (Optional Input) Must be less than or equal to Maximum Amount Negative values not permitted |
| 9 | `AZ.APP.MAXIMUM.AMOUNT` | `AzProductParameter_MaximumAmount` | TField | No | The maximum amount of Deposit or Loan for this product is defined here. While opening accounts under this product, the amount of the account is validated against the amount defined here, for maximum value. Validation Rules 1 to 20 type AMT (standard amount format) Characters (Optional Input) Must be greater than or equal to Minimum Amount Negative values not permitted |
| 10 | `AZ.APP.INT.BASIS` | `AzProductParameter_IntBasis` | TField | No | Defines the method of determining the number of days used for debit interest calculations. In each of the options specified below the number on the left represents the number of days basis for multiplying Can accept the following types: Key G is used as interest basis for the weekly frequency defined in loan account schedule. For illustrations of all supported INTEREST.BASIS INTEREST.BASIS Validation Rules Optional input. If no value is defined for this field in AZ.PRODUCT.PARAMETER, then for all the AZ.ACCOUNTs relating to this product, the interest basis will be picked up by the system automatically based on the currency of the account, by referring the CURRENCY table. characters of value A, B, C, D, E, F, or g |
| 11 | `AZ.APP.RATE.PERIOD` | `AzProductParameter_RatePeriod` | TField |  | Input in this field implies that the product is a Fixed Floating type. A Fixed Floating product initially takes a FIXED rate of interest and then moves to floating rate of interest till maturity based on the PERIOD input in the field RATE.PERIOD. Values may be input in the following format, i.e. nnD or nnM or nny The floating rate is prompted by 'R' schedule without which a FIXED FLOATING deposit or loan acct cannot be committed or authorized. The rates of interest both FIXED and FLOATING are defaulted from APP. A combination of the following interest is permitted for input, (1) BASIC.INTEREST (BI) and FIXED.RATE (2) BASIC.INTEREST (BI) and PERIODIC INTEREST (PI) To illustrate, In AZ.PRODUCT.PARAMETER the RATE.PERIOD is input as 03M. The Fixed Floating Deposit Start date is 05.04.2003 and maturity date is 04.04.2004. Interest is FIXED at 20% (field INT.FIXED.RATE) and for floating BI rate with RATE.KEY as 01 RATE.SPREAD as 2% RATE.OPERAND as ADD [Assume that the BI rate on the date of R schedule is 6.75%] System takes FIXED rate of interest at 20% from 05.04.2003 to 05.07.2003 And FLOATING rate at 8.75% (6.75% + 2%) from 06.07.2003 till 04.04.2004 Thus RATE.PERIOD is the field that differentiates a FIXED-FLOATING contract from other types of contracts. Based on the input in this field 'R' schedule is triggered to bring FLOATING rate in to operation. VALIDATIONS (1) Two numeric and One alphanumeric input allowed. (2) Permitted values in RATE.PERIOD nn&gt;D, nn&gt;M and nn&gt;Y (3) Fixed and Floating interest rates are defaulted from APP- user has the option to change the FIXED rate of interest directly at account level and FLOATING rate by changing the INTEREST. RATE value in BI table. (4) The change in Rate. key value can be gauged from the ACI record only (5) Spread cannot be changed it is fixed. (6) For fixed floating a combination of BI + PI, BI + FIXED is allowed but not PI AND FIXED. (7) Not applicable for credit cards |
| 12 | `AZ.APP.RATE.KEY` | `AzProductParameter_RateKey` | TField | No | Contains the linking reference number of the appropriate interest rate, which will be used for interest computation purposes. The key indicates the rate to be used for interest computation purposes on Loans and Deposit accounts. The key can take the form of an entry on the BASIC RATE TEXT and consequently BASIC INTEREST table. A rate change on the BASIC.INTEREST rate table will automatically reschedule the loan / deposit accounts that are linked to table, on the respective dates or on the dates defined as per the interest revision notice periods. This process will be done during COB. Validation Rules 1-2 numeric characters. (Optional input.) Should have been defined in BASIC.RATE.TEXT and in BASIC.INTEREST table |
| 13 | `AZ.APP.RATE.SPREAD` | `AzProductParameter_RateSpread` | TField |  | Indicates the spread or margin of interest, which has to be applied to the basic interest rate defined in BASIC.INTEREST ( linked through RATE.KEY) of the loan account, to arrive the variable interest. Validation Rules 1-5 numeric characters of rate type (example 3.25, 99.99) A margin, which will result in a negative interest rate is prohibited. Cannot be greater than 100 This field is available for input only if RATE.KEY is defined |
| 14 | `AZ.APP.RATE.OPERAND` | `AzProductParameter_RateOperand` | TField |  | Determines the method by which the basic interest rate is to be adjusted by the RATE.SPREAD to arrive at the Variable interest rate for this loan product. This field specifies whether the Spread Rate is to be added to, subtracted from or multiplied by the basic interest rate to arrive at the final variable interest rate. If Multiply is specified the final rate is (100 + M) percent of the effective interest rate , where M is the Margin Rate percentage. Validation Rules Valid values are ADD, SUBTRACT or MULTIPLY. This field is available for input only when RATE.KEY is defined |
| 15 | `AZ.APP.RATE.PERCENT` | `AzProductParameter_RatePercent` | TField |  | Defines the percentage of outstanding amount up to which the Variable interest rate is applicable. For the rest of the percentage amount, fixed interest rate will be applicable. Example: If 60 is defined here, variable interest will be applied on 60% of the outstanding amount and Fixed interest will be applied on the remaining 40% of the outstanding amount. The only EXCEPTION is FIXED-FLOATING type of deposit or loan-which takes only 100 as input in this field. Validation Rules 1-5 numeric rate type like 60, 45.50 etc. Maximum value 100. No negative values will be accepted. In case of FIXED-FLOATING type -this field has to be input with the value 100. In case this is defined then any change to the BASIC.INTEREST will not be applicable. |
| 16 | `AZ.APP.INT.FIXED.RATE` | `AzProductParameter_IntFixedRate` | TField |  | Indicates the annual fixed interest rate applicable for the loan / deposit accounts opened under this Product. The rate of interest is defaulted to the AZ account from here. However, the interest rate can be modified at the account level also. Validation Rules 1- 5 numeric characters of rate type, like 18.50. |
| 17 | `AZ.APP.PERIODIC.RATE.KEY` | `AzProductParameter_PeriodicRateKey` | TField |  | A reference number entered here as a key, links to an interest rate defined in the PERIODIC.INTEREST table. Interest rates are defined in PERIODIC.INTEREST table for various periods. The Periodic Rate key is used for Deposit Products to apply interest, based on the period of the deposit. When the Product is FIXED.FLOATING type - this PI key will be allowed to be used in combination with RATE.KEY in field no.12. Validation Rules Input is not allowed, if RATE.KEY is already defined Permitted- if the RATE.PERIOD contains any VALUE. Allowed only for deposit products. |
| 18 | `AZ.APP.PI.METHOD` | `AzProductParameter_PiMethod` | TField |  | Specifies the method that has to be used for selecting the interest rate from Periodic Interest Table, for Deposit accounts, corresponding to the period of deposit. Allowed values are Previous, Next or Interpolate Validation Rules Input is allowed only if Periodic rate key is defined |
| 19 | `AZ.APP.DRAWDOWN.TYPE` | `AzProductParameter_DrawdownType` | TField |  | Defines a type of drawdown for this product, which specifies the method of withdrawing the Principal amount, from the loan accounts of this product. The various types of drawdowns are: 1. Full Drawdown: The entire loan amount has to be withdrawn at a time. No partial withdrawals are allowed 2. Partial and No interest: Partial or full withdrawal of the loan amount is permitted from the loan account. Interest and Principal repayment need not be done during the drawdown period. 3. Partial and interest: The drawdown of loan amount can be done in partial or in full. During the drawdown period repayment towards interest has to be made. Otherwise it will be treated as arrears. If a nominated account is defined, in all the options, the system will automatically transfer the drawdown amount on the drawdown date to the nominated account. If no nominated account is defined and if the drawdown is preferred through the AZ account itself, then a teller default will be created and the amount can be drawn through teller using the teller default feature. Validation Rules Valid for Loan products only. Valid numbers are 1,2,3. Default is 1 (Full Drawdown) Must be 1 for CREDIT CARD type of loans. |
| 20 | `AZ.APP.MULTI` | `AzProductParameter_Multi` | TField |  | This field defines if the sub accounts are to be created during credit or debit over the AZ.ACCOUNT. Input allowed only if ACCT.SYNC is set to 'Y'. Input allowed only for CREDIT CARD type of loan product and FIXED or SAVINGS PLAN type of deposit products. If 'YES' were input then subject to, other parameter set up, every debit entry using DRAWDOWN.TXN code in a CREDIT CARD kind of set up would create a SUB acct and every credit entry using DEPOSIT.CR txn code or B schedule in the deposit will create a SUB acct. VALIDATIONS Permitted values either 'YES' or '' Once input the values cannot be changed . |
| 21 | `AZ.APP.ACCT.SYNC` | `AzProductParameter_AcctSync` | TField |  | Allowed values are 'YES' and ''. If 'YES' then the working balance in a/c and the principal in AZ.ACCOUNT created will be in sync. This can be achieved by using specific transaction codes for specific AZ related events. (For more details on transaction codes and events, refer the help text for various fields like DRAWDOWN.TXN). If the input is 'NULL', the two balances might not be in harmony. If 'YES', then an entry over the AZ.ACCOUNT should contain a valid txn code (apart from DR.TXN.CODE and CR.TXN.CODE) defined in its APP so that it knows how to process the entry within AZ. VALIDATIONS Permitted values either 'YES' or ''. Once input the values cannot be changed. |
| 22 | `AZ.APP.INT.ONLY` | `AzProductParameter_IntOnly` | TField |  | This field is used to define whether 'interest only repayment' is allowed for this product or not. If Y (Yes) is defined, accounts opened under this product shall be able to have Interest Only repayment facility If N (No) is defined, accounts of this product shall not be able to have the Interest Only repayment facility. For loan accounts with repayment type 'Non Redemption', no validation will be done with respect to this field. Validation Rules The permitted values are 'Y' or 'N' Valid only for Loan products Default is 'N' |
| 23 | `AZ.APP.MAX.INSTL.INT.ONLY` | `AzProductParameter_MaxInstlIntOnly` | TField |  | Total period of time that the interest only repayment could be defined for a loan during the life time of a loan account. If input is given in numbers it is treated as number of days. If input is given prefixing P, it is treated as number of installments. Example: If input is given as 60, it implies that the total number of Interest Only repayment period cannot exceed 60 days during the life of the loan account. If input is given as P60, it implies that the total number Interest only repayment period cannot exceed 60 installments (of any frequency like daily, monthly) during the life of the loan account. Note: For loan accounts with repayment type 'Non Redemption', no validation will be done with respect to this field. Validation Rules Value for this field will be accepted only if 'INT ONLY' field is defined as Y Valid only for Loan Product Cannot have negative values |
| 24 | `AZ.APP.PART.REDEMPTION` | `AzProductParameter_PartRedemption` | TField |  | Defines whether a portion of the funds available in the fixed Deposit can be allowed for redemption, before the maturity date of the deposit. If 'Y' is defined the accounts opened under this deposit product will have the facility of part redemption. If 'N' is defined, part redemption facility will not be available for deposit accounts opened under this product Allowed only form FIXED deposit products. |
| 25 | `AZ.APP.PI.TABLE.TO.USE` | `AzProductParameter_PiTableToUse` | TField |  | In the case of early redemption of a deposit, either partial or full, the eligible interest rate will be based on the period for which the redeemed amount was in force. To find this eligible interest rate, the system has to refer either the current periodic interest table or the periodic interest table that corresponds to the opening date of the deposit or the lowest of these two rates or trigger a routine to get the interest rate. Allowed input are 'CURRENT' / 'OPENING' / 'LOWEST' / 'OTHERS' |
| 26 | `AZ.APP.PRE.CLOSE.RTN` | `AzProductParameter_PreCloseRtn` | TField |  | A local routine with a valid PGM.FILE entry with PGM.TYPE set to 'S' can be attached in this field. This routine will be triggered when a part withdrawal or preclose of a deposit happens. If it is a multi deposit then this routine will be triggered for every sub account during preclosure / part withdrawal. This field is allowed only if PI.TABLE.TO.USE is set to OTHERS. The routine to be triggered will have to be input as 'LOCAL.ROUTINE(INTEREST.RATE)'. Where INTEREST.RATE is the rate that is returned from the routine. This rate will be used instead of EARLY.RED.MARGIN if any given. |
| 27 | `AZ.APP.EARLY.RED.MARGIN` | `AzProductParameter_EarlyRedMargin` | TField |  | Defines the margin rate of interest that has to be deducted from the eligible rate arrived for early redemption of deposit amount. For example if margin is defined as 2, and for early redemption of a deposit the eligible interest was arrived as 9% after referring the interest table, then the final interest paid for this early redemption will be 7% Validation Rules Numeric Value. |
| 28 | `AZ.APP.APPLY.PENALTY` | `AzProductParameter_ApplyPenalty` | TField |  | Identifies whether or not penalty is to be applied to accounts of this product. If 'Y' is defined, for loan products, penal interest will be applied on arrears automatically by the system. If 'N' is defined penalty will not be applied automatically. The user has to apply manually, if required. This field is not applicable for DEPOSIT type, as the rate of interest for redemption will be automatically calculated by the system based on the value provided in other fields like, PI.TABLE.TO.USE, PI.METHOD. Validation Rules Valid options are 'Y', 'N'. Default is 'N'. |
| 29 | `AZ.APP.GRACE.PERIOD` | `AzProductParameter_GracePeriod` | TField | No | Defines the period in number of days, until the expiry of which the loan accounts under this product, shall not be considered to be in arrears. If the account continues to be in arrears status, even after the expiry of the grace period, penalty interest will be applied on the arrears, as per penalty parameters defined. Validation Rules Optional field 1-999 numeric Cannot have negative values. |
| 30 | `AZ.APP.PENAL.OPERAND` | `AzProductParameter_PenalOperand` | TField |  | Determines the method by which the effective interest of the loan product (either a key based variable interest or fixed interest or a combination of both) is to be adjusted by the Penal margin to arrive at the penal interest rate. This field specifies whether the Margin Rate is to be added to, subtracted from or multiplied by the effective rate to arrive at the penal interest rate. If Multiply is specified the final rate is (100 + M) percent of the effective interest rate , where M is the Margin Rate percentage. Validation Rules Valid values are ADD, SUBTRACT or MULTIPLY. This field is available for input only when APPLY.PENALTY is y |
| 31 | `AZ.APP.PENAL.MARGIN` | `AzProductParameter_PenalMargin` | TField |  | Indicates the margin or spread of penalty, which has to be applied to the effective interest (variable interest or fixed interest or the combination of both) of the loan account, to arrive at the penal interest. Validation Rules 1-5 numeric characters of rate type (example 3.25, 99.99) A margin, which will result in a negative interest rate is prohibited. Cannot be greater than 100%. This field is available for input only if APPLY.PENALTY is defined as Y and PENAL.OPERAND is not Null. |
| 32 | `AZ.APP.PENAL.RATE` | `AzProductParameter_PenalRate` | TField |  | Defines the penal rate of interest that can be applied on the value defined under the field PENALTY.ON. At present, the penalty interest rate is applied on the arrears amount only. When PENAL.OPERAND and PENAL.MARGIN are not defined, then this field is allowed for input by user. Otherwise, where the operand and margin values are defined, the penal rate will be calculated automatically by the system from the effective interest. If APPLY.PENALTY is set to 'Y' and PENAL.MARGIN, PENAL.OPERAND and PENAL.RATE are null the system would default the EFFECTIVE.INT.RATE into this field. The penal interest rate entered here is defaulted to AZ loan account. However, at account level it can be modified, in case of need. Validation Rules Rate type, numeric value Maximum value 100 Negative values not accepted. |
| 33 | `AZ.APP.PENALTY.ON` | `AzProductParameter_PenaltyOn` | TField |  | Identifies the basis for applying the penalty calculation. For Loan products the following values apply: 1. Arrears 2. Balance Owing 3. Credit limit For Deposit products, the following values apply: 4. Interest days 5.Fixed Amount 6. Redeemed amount At present, only penalty processing on arrears alone will be done. Other values are reserved for future development. Validation Rules Input allowed only if APPLY PENALTY is defined as 'Y' Negative values not permitted |
| 34 | `AZ.APP.PD.LINK.TO.AZ` | `AzProductParameter_PdLinkToAz` | TField |  | This field can be input as either 'YES' or 'NO' or ''. If this field is YES-an, attempt to repay an amount, less than the amount raised by way of schedules after repay date will create, PD. In this case APPLY.PENALTY must be set to 'N'.If PD is not installed, then the funds will be automatically debited from repay account. If this field is NO then, the amount still payable will continue to remain in the account (i.e., the principal will not be reduced) and AZ.OVERDUES will be updated. Allowed only if ACCT.SYNC is set to 'YES'. Not allowed for CREDIT CARD type of loan products. If this field is '' then, FT on hold will be created for the insufficient funds. VALIDATIONS YES or NO or ''. Value in this field should be same as that of ACCT.SYNC for loans Once input the values cannot be changed. Partial payment will raise PD only if repay date is past for credit cards. |
| 35 | `AZ.APP.CARD.TYPE` | `AzProductParameter_CardType` | TField |  | Input in this field should be a valid id in CARD.TYPE application. When input the APP id will be populated in the attached CARD.TYPE. VALIDATIONS Any valid card type id can be input. Input allowed only for CREDIT CARD type of loans. |
| 36 | `AZ.APP.MAX.BACK.DATE` | `AzProductParameter_MaxBackDate` | TField |  | Defines the maximum number of days, earlier than the current business date, up to which the value date of the accounts, defined under this product, can go back. However, the maximum back value date defined under DATES table of T24, will override the maximum back date arrived by the number of days defined here. Validation Rules 1-3 digit Numeric value Cannot have negative values |
| 37 | `AZ.APP.POSTING.RESTRICT` | `AzProductParameter_PostingRestrict` | TField | No | Any valid code entered in this field, defines the restrictions for posting entries in AZ accounts, opened under this product. This field can be effectively used in AZ Loan products, for achieving certain restrictions like, not allowing redraw facility.Temenos recommends the user to define the Restriction at APP and not in the underlying account Validation Rules Optional input 1 to 4 numeric characters Posting Restriction code, based on EB.OBJECT It must be an existing code in the POSTING.RESTRICT table Posting restriction codes used in ACCOUNT, like 99, for marking Closure are not valid under this module, as All in One account closure is handled in a different way. |
| 38 | `AZ.APP.MIN.CREDIT.BAL` | `AzProductParameter_MinCreditBal` | TField |  | Defines the minimum credit balance that has to be held in accounts of this product, at any time. Validation Rules Amount field Negative value not permitted Applicable for credit balance type of accounts |
| 39 | `AZ.APP.OFFSET.ALLOWED` | `AzProductParameter_OffsetAllowed` | TField |  | For future use. |
| 40 | `AZ.APP.FORWARD.ACC.MNTHS` | `AzProductParameter_ForwardAccMnths` | TField |  | For future use. |
| 41 | `AZ.APP.REPAYMENT.TYPE` | `AzProductParameter_RepaymentType` | TField |  | Defines the Repayment type of the All in One Accounts opened under this product. This type determines the way in which the loan / deposit account is to be repaid. The repayment types available for loan accounts are: 1. ANNUITY This type of loan has a fixed periodic repayment amount (the 'annuity') consisting of two variable components of principal and interest. The repayment amount is calculated at the start of the contract so that the principal will be fully repaid after the required number of repayments. Initially, repayments consist mostly of interest. However, as the contract progresses, the size of the principal component increases as the accrued interest reduces. In All in One loan account, for defining a zero repayment at the start of the loan, for a fixed period, Annuity type of repayment can be defined in the schedule with amount as Zero. 2. LINEAR Linear loans also have an interest and a principal component to their periodic repayments. In this case, however, the principal repayment amount remains fixed over the lifetime of the contract. The interest element reduces as the accrued interest on the reducing balance decreases. This leads to a repayment scheme whereby the total periodic repayment reduces over the lifetime of the contract. 3. NONRED In NONRED type of loans, there is no regular repayment of principal, but only interest. The redemption of Principal will be done on maturity date of the loan. In All in One account loans, the validations relating to Interest Only and Maximum Period interest only will not happen for this type of loan. 4. OTHER Non structured repayments are handled under this type. Repayment of any amount on any dates are allowed, provided the total repayment amount is equal to principal. 5.CREDIT CARD This type of Loan repayment type was added from G14.0.00. For this repayment type, the schedules are NO. When the repayment type is Credit Card the regular schedules are discarded, sub a/cs are created and new types of schedules like CI, CC are generated automatically. Repayment types available for Deposit accounts are: FIXED Deposit amount is accepted in a fixed lump sum and repaid as fixed amount either with interest accumulated or not. SAVING PLAN Deposit amount is accepted in a periodic basis and the principal is increased in a recurring manner till the end of a fixed term. Validation Rules 1 to 12 Alpha characters. Input must be either 'ANNUITY', 'LINEAR', 'OTHERS', 'NONRED' or 'CREDIT CARD' for loan products and 'SAVINGS PLAN' or 'FIXED' for Deposit products. Once defined and record authorised cannot be changed. |
| 42 | `AZ.APP.CREDIT.AMT.MULTI` | `AzProductParameter_CreditAmtMulti` | TField |  | This field indicates if B schedules need to be validated to be in multiples of the CREDIT.AMOUNT defined in AZ.ACCOUNT. If 'YES' is set then the B schedule amount can be only in multiples of the the CREDIT.AMOUNT, if defined in AZ.ACCOUNT. Allowed values are 'YES' or ''. Allowed only for SAVINGS PLAN type of deposit products. Allowed only if ACCT.SYNC is set as 'YES'. |
| 43 | `AZ.APP.CR.INT.FQU` | `AzProductParameter_CrIntFqu` | TField |  | Defines the frequency for interest credit, for all deposit accounts opened under this product. Example If 'M01' is defined here, this frequency will be defaulted to the deposit account of this product and interest will be credited / capitalised every month from the start date. This is acheived by defaulting the 'I' schedule into the az.account during creation. If a liquidation account is defined in ACCOUNT then interest will be credited to that account as per this frequency. Otherwise the interest will be capitalised in the Deposit account itself If 'NM01' is defined here, then the interest will be paid out to the nominated account after capitalising it every month from the start date. This is acheived by defaulting the 'IN' schedule into the az.account during creation. This field is also used during deposit rollover. Based on the setup done here either a 'I' or 'IN' schedule will be created during rollover. If this is not defined then the 'IN' schedule will be created on maturity after rollover. Validation Rules Type of input allowed are: Dnn,Wnn, Mnn |
| 44 | `AZ.APP.CR.INT.TYPE` | `AzProductParameter_CrIntType` | TField |  | Defines the type by which the credit interest frequency will be processed for the deposit accounts opened under this product. This field is associated with CR.INT.FQU ( frequency for interest credit) Allowed types are: ANNIVERSARY / PERIOD.END If ANNIVERSARY is defined then the credit interest frequency will be based on the opening date. If PERIOD.END is defined then the credit interest will be based on the calendar end like month end. |
| 45 | `AZ.APP.CR.DEP.FQU` | `AzProductParameter_CrDepFqu` | TField |  | Defines the frequency of further credits to the deposit account. In the case of deposits like Savings Plan, Recurring Deposit, this field is use for defining the frequency of future credits expected. For example, if M01 is defined here, regular monthly credits to the account are expected. Validation Rules Type of input allowed are: D&lt;nn&gt;,W&lt;nn&gt;, M&lt;nn&gt;. This field and grace period field should have a rationale input i.e. Grace can't be Month. End while the deposit frequency is daily. This validation is valid from 14.2.00 with the introduction of period. end option in Grace period field. |
| 46 | `AZ.APP.RESCHED.TYPE` | `AzProductParameter_ReschedType` |  |  |  |
| 47 | `AZ.APP.RESCHED.NOTICE` | `AzProductParameter_ReschedNotice` |  |  |  |
| 48 | `AZ.APP.RATE.CHNG.EXIST` | `AzProductParameter_RateChngExist` | TField |  | This field can take "Y", "N" or "null". When the field is input as "Y" or "" any change in rate of interest i.e. through Basic.Interest key will be effective not only to new accounts but the existing accounts will also be taken at the changed rate-from the date of such change. If "N" is input then any change in rate of interest will be applicable only to the new a/cs whose draw downs are after the date of such change. |
| 49 | `AZ.APP.TERM.PRIORITY` | `AzProductParameter_TermPriority` | TField |  | Specifies whether the Term of the loan has to be prioritised and kept constant or not, in the case of Re scheduling. In AZ Loan accounts, the term of the loan or repayment amount may change due to certain factors like Principal increase, Principal decrease, change in Rate of interest, change in frequency etc. In such cases, it is possible to retain the original term and the maturity date as constant and change the repayment amount suitably, by specifying the Term Priority as 'Y'. If Term Priority is specified as 'N', then by keeping the repayment amount as constant the term will be changed by altering the maturity date of the loan. This field has to be opted by the user whenever a Rescheduling of the loan account is to be done. According to the value defined here, Rescheduling will be done and the previous schedule will be written to AZ.SCHEDULES.HIST. In case of annuity type of loans if the user defined annuity amount is greater than the system generated amount then the contract will be matured earlier than the actual maturity date even though this field is set to 'Y'. In case of OTHERS/NONRED type of loans, TERM.PRIORITY 'N' is not applicable as they may have uneven payments. Hence if AZ contract is of OTHERS/NONRED type of loans, then system shall ignore the value in this field and work as if TERM.PRIORITY is 'Y'. Validation Rules Valid value 'Y' or 'N' |
| 50 | `AZ.APP.RECALC.CURR.AMT` | `AzProductParameter_RecalcCurrAmt` | TField | No | Specifies whether the changes made to the loan account schedule, on account of re-scheduling, are to be effected from the current repayment date or from the next repayment date. If 'Y' is defined here the effect of re-scheduling will be given from the current repayment date and if 'N' is specified the effect will be from the next repayment date. This field will not be considered for future dated or back dated BI rate changes. Validation Rules Allowed values 'Y' or 'N' Optional input Not applicable for credit card loans |
| 51 | `AZ.APP.SINGLE.LIMIT` | `AzProductParameter_SingleLimit` | TField |  | This field specifies the value that should be set in the SINGLE.LIMIT field in the ACCOUNT that is attached to this APP. Input can be 'Y', 'N' or ''. If input as 'Y' then the acct that is attached to the product parameter should also have the field single limit marked as yes. If input as 'N' or '' then the acct that is attached to the product parameter cannot have the field single limit marked as 'Y'. In this case all the AZ.ACCOUNT that are linked to the same limit must have REDUCE.LIMIT set to be the same. VALIDATIONS Input can be either 'Y', 'N' or ''. |
| 52 | `AZ.APP.MATURITY.INSTR` | `AzProductParameter_MaturityInstr` | TField |  | Defines the maturity instruction for the deposit accounts opened under this product. If 'Automatic Rollover' is specified the deposit will be rolled over with the same terms as that of the original deposit. If 'Payment to Nominated Account' is defined here, on date of maturity the maturity amount will be credited to the nominated account specified in the deposit account. Partial Rollovers and rollover with additional amount are handled at account level. Validation Rules Permitted values : 'Automatic Rollover' / 'Payment to Nominated Account' Allowed only for FIXED type deposits. |
| 53 | `AZ.APP.ROLLOVER.ACCTNG` | `AzProductParameter_RolloverAcctng` | TField |  | This field specifies if accounting entries over the AZ.ACCOUNT need to be raised during rollover. This could be useful for passbook printing purposes. For example, Let us assume that the deposit is for 5000. If the MATURITY.INSTR is 'AUTOMATIC ROLLOVER' then during rollover the following entries will be generated. A. Nominated account is given, Case 1. Rollover amount - 5000 Dr AZ.ACCOUNT 5000 Cr Nominated a/c 5000 Cr AZ.ACCOUNT 5000 Dr Nominated a/c 5000 Case 2. Rollover amount - 6000 Dr AZ.ACCOUNT 5000 Cr Nominated a/c 5000 Cr AZ.ACCOUNT 6000 Dr Nominated a/c 6000 Case 3. Rollover amount - 4000 Dr AZ.ACCOUNT 5000 Cr Nominated a/c 5000 Cr AZ.ACCOUNT 4000 Dr Nominated a/c 4000 B. No nominated account given, Case 1. Rollover amount - 5000 Dr AZ.ACCOUNT 5000 Cr Internal a/c 5000 Cr AZ.ACCOUNT 5000 Dr Internal a/c 5000 Case 2. Rollover amount - 6000 Dr AZ.ACCOUNT 5000 Cr Internal a/c 5000 Cr AZ.ACCOUNT 6000 Dr Internal a/c 6000 Create a TELLER.DEFAULT crediting AZ.ACCOUNT for 1000. Case 3. Rollover amount - 4000 Dr AZ.ACCOUNT 5000 Cr Internal a/c 5000 Cr AZ.ACCOUNT 4000 Dr Internal a/c 4000 Create a TELLER.DEFAULT debiting Internal account for 1000. MATURITY.DR txn code will be used on the debit side and ROLLOVER.CR txn code will be used on the credit side entries to AZ.ACCOUNT. If nominated account is not provided then an internal account will be used. The category of the internal account need to be defined in ACCOUNT.CLASS application for 'AZROLLOVER' id. Allowed only if ACCT.SYNC is set to 'Y'. Allowed only for FIXED type deposits. |
| 54 | `AZ.APP.OD.CATEGORY` | `AzProductParameter_OdCategory` | TField |  | This field defines the category code to which the account needs to be changed, if the deposit is matured based on certain conditions, like, 1. MATURITY.INSTR and nominated account are not defined. 2. MATURITY.INSTR is not defined but nominated account is defined and FINAL.MATURITY in AZ.ACCOUNT is not defined. Should be a valid category defined in the CATEGORY table. Range allowed: 1 to 9999 |
| 55 | `AZ.APP.APPROPRIATE.TYPE` | `AzProductParameter_AppropriateType` | TField |  | This field decides how a repayment received should be apportioned among the various withdrawals made under a Credit Card account. Permitted inputs are (1) Proportionate and (2) FIFO [First in First Out]. TO ILLUSTRATE: - Suppose there are 3 draws through sub a/c Withdrawal 1 350000 Withdrawal 2 650000 Withdrawal 3 250000 TOTAL 1250000 Appropriation Type- Proportionate If the repayment received, is 125000 the set-off will be 125000*350000/1250000 Towards Drawl (1) -35000 ----I.e.- 125000*350000/1250000 Towards Drawl, (2) it is 65000--I.e.- 125000*650000/1250000 Finally toward drawl (3) 25000--I.e.- 125000*250000/1250000 Appropriation Type- FIFO If on the other hand the repayment received is, say 450000 and type is FIFO then: - First withdrawal of 350000 will be fully adjusted and then remaining amount of 100000 will be adjusted towards the O/s of second withdrawal. This way it will continue until the last drawl is met with fully. VALIDATIONS: Input can be either Proportionate or FIFO. Input is subject to change. Allowed only for CREDIT CARD type of products. |
| 56 | `AZ.APP.REVOLVING.RATIO` | `AzProductParameter_RevolvingRatio` | TField |  | Inputs in this field indicate that the product is a REVOLVING type. Input permitted is Numeric, which is applied to the total drawls made by the customer to arrive at the schedule repayment amounts by way of CC schedules. The ratio input in this field is defaulted to AZ main A/c. Input made can be altered at any time. VALIDATIONS Any numeric from 1-100 Allowed only for CREDIT CARD type of products. |
| 57 | `AZ.APP.HIGHEST.RANGE` | `AzProductParameter_HighestRange` |  |  |  |
| 58 | `AZ.APP.AMT.PERCENT` | `AzProductParameter_AmtPercent` |  |  |  |
| 59 | `AZ.APP.BONUS.PREMIUM` | `AzProductParameter_BonusPremium` |  |  |  |
| 60 | `AZ.APP.LATE.PYMT.FEE` | `AzProductParameter_LatePymtFee` |  |  |  |
| 61 | `AZ.APP.LIAB.TO.PENALTY` | `AzProductParameter_LiabToPenalty` | TField |  | Based on the value given under this field, the number of delayed installments liable to penalty will be arrived and corresponding charges defined under LATE.PMT.FEE will be charged. Numbers specified in this field refers to number of installments. For example, if 2 is defined here, the account will be liable for late fee after 2 installments are missed. This field is applicable for SAVINGS PLAN type of deposits. Validation Rules Numeric value ranging from 0 to 99 |
| 62 | `AZ.APP.PENALTY.COLL.AT` | `AzProductParameter_PenaltyCollAt` | TField |  | Defines when the charges defined in LATE.PYMT.FEE field in APP are to be collected when they are processed during COB. IMMEDIATE: The fee will be collected immediately while processing in COB. MATURITY: The fee will be carried forward till maturity of the deposit. This is applicable in the case of part withdrawal of deposit. NEXT-INSTALLMENT: In the case of part withdrawal of Savings Plan type, the fee will be adjusted in the next installment credited to the account. NEXT-INT-DUE: In the case of part withdrawal the fee will be adjusted in the next interest due for remaining deposit Validation Rules Valid values are, IMMEDIATE, MATURITY, NEXT-INSTALLMENT, NEXT-INT-DUe |
| 63 | `AZ.APP.PRE.CLOSURE.FEE` | `AzProductParameter_PreClosureFee` |  |  |  |
| 64 | `AZ.APP.CC.PR.GRACE.PERIOD` | `AzProductParameter_CcPrGracePeriod` | TField | Yes | For any repayment type if the GENERATION of CC schedules (principal repayment schedule) is to be postponed then this field is used. Input permitted in this field is Numeric and it should be read along with the frequency input in the repay date field of card issue application. When a card issue frequency is monthly and input made in this field is 3, it means for 3 months the generation of CC schedule is to be postponed, i.e., NO CC schedule is generated. If in card issue the frequency is weekly then it is to be interpreted that 'CC' schedules will not be generated for 3 weeks. For a Non Redemption type, input in this field is mandatory. This is for the reason that in Non Redemption type CI (interest) schedules are generated regularly and the Principal (CC schedules) repayment is made in one BULLET payment. VALIDATIONS Input allowed only if the Repayment Type is a Non redemption Allowed only for CREDIT CARD type of products. |
| 65 | `AZ.APP.DRAWDOWN.TXN` | `AzProductParameter_DrawdownTxn` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan side (Debit or Credit) are identified and categorized as TXN codes. Draw down Txn implies making a draw down on the loan a/c- where Debit will be to the Loan a/c and Credit will be to the Nominated a/c. When entered the system automatically identifies the transaction code as an EVENT of Draw down. Whenever, loan is drawn this TXN code is used to identify the event, if ACCT.SYNC.is set to 'Y'. VALIDATIONS Must be a valid id in transaction application. TXN codes are input to identify the EVENT i.e loan repay, Draw down etc Mandatory input if the ACCT. SYNC is set to "Y". |
| 66 | `AZ.APP.INT.ADJ.TXN` | `AzProductParameter_IntAdjTxn` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan/deposit side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used to signify that the entry is a interest adjustment entry.This is applicable only for Annuity contracts and if this entry is encountered, then the system shall debit AZINTADJ Suspense account instead of the underlying AZ Account. VALIDATIONS Must be a valid "ID" in transaction application Mandatory input if the ACCT. SYNC is set to "Y". Only debit transaction code allowed. Value date in the entry should be same as the bank date(TODAY). Applicable only for ANNUITY contracts with Reduce limit as 'NO'. |
| 67 | `AZ.APP.LOAN.REPAY` | `AzProductParameter_LoanRepay` |  |  |  |
| 68 | `AZ.APP.RESERVED.TXN.2` | `AzProductParameter_ReservedTxn2` | TField |  |  |
| 69 | `AZ.APP.LOAN.PRECLOSE` | `AzProductParameter_LoanPreclose` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan side (Debit or Credit) are identified and categorized as TXN codes. Loan Preclose implies closing down of the loan a/c well before the ORIGINAL maturity date- where Credit will be to the Loan a/c and Debit will be to the Repay a/c. When entered the system automatically identifies the transaction code as an EVENT of Loan Preclose. The SYSTEM record must be defined in AZ.SETTLEMENT.PRIORITY file. VALIDATIONS Must be a valid "ID" in transaction application. TXN codes are input to identify the EVENT i.e loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". |
| 70 | `AZ.APP.RESERVED.TXN.3` | `AzProductParameter_ReservedTxn3` | TField |  |  |
| 71 | `AZ.APP.MATURITY.TXN` | `AzProductParameter_MaturityTxn` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan side (Debit or Credit) are identified and categorized as TXN codes. Maturity txn implies complete and full closure of loan a/c- where Credit will be to the Loan a/c and Debit will be to the Repayment a/c. When entered the system automatically identifies the transaction code as an EVENT of Loan Maturity. When the loan is normally closed as per schedule of repayment then this TXN code is used on the credit side of the transaction. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". |
| 72 | `AZ.APP.CHARGE.DR.TXN` | `AzProductParameter_ChargeDrTxn` |  |  |  |
| 73 | `AZ.APP.INT.DR.CAP` | `AzProductParameter_IntDrCap` |  |  |  |
| 74 | `AZ.APP.TAX.ON.CHG.TXN` | `AzProductParameter_TaxOnChgTxn` |  |  |  |
| 75 | `AZ.APP.BONUS.CR` | `AzProductParameter_BonusCr` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the deposit side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used while raising the bonus entry in SAVINGS PLAN type of deposit. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". |
| 76 | `AZ.APP.DEPOSIT.CR` | `AzProductParameter_DepositCr` |  |  |  |
| 77 | `AZ.APP.DEP.DR.PART` | `AzProductParameter_DepDrPart` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the deposit side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used to signify that the entry is a part withdrawal of a deposit. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". |
| 78 | `AZ.APP.DEP.DR.PRE.CLOSURE` | `AzProductParameter_DepDrPreClosure` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the deposit side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used to signify that the entry is due to preclosure of a deposit. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". |
| 79 | `AZ.APP.DEP.DR.MATURITY` | `AzProductParameter_DepDrMaturity` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the deposit side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used to when raising the debit entry during normal deposit maturity. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". |
| 80 | `AZ.APP.ROLLOVER.CR` | `AzProductParameter_RolloverCr` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the deposit side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used when raising the credit entry during rollover of deposits. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". |
| 81 | `AZ.APP.INT.CR.CAP` | `AzProductParameter_IntCrCap` |  |  |  |
| 82 | `AZ.APP.PRINCIPAL.TXN` | `AzProductParameter_PrincipalTxn` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used to signify that the entry is a credit entry and a principal repayment to a loan. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". Not allowed for CREDIT CARD type of products. Debit transaction codes not allowed. |
| 83 | `AZ.APP.OD.PRINCIPAL.TXN` | `AzProductParameter_OdPrincipalTxn` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used to signify that the entry is a credit entry and a overdue principal repayment to a loan. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". Not allowed for CREDIT CARD type of products. Debit transaction codes not allowed. |
| 84 | `AZ.APP.OD.INTEREST.TXN` | `AzProductParameter_OdInterestTxn` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan side (Debit or Credit) are identified and categorized as TXN codes. This txn code is used to signify that the entry is a credit entry and a overdue interest repayment to a loan. VALIDATIONS Must be a valid "ID" in transaction application TXN codes are input to identify the EVENT i.e. loan repay, Draw down etc. Mandatory input if the ACCT. SYNC is set to "Y". Not allowed for CREDIT CARD type of products. Debit transaction code not allowed. |
| 85 | `AZ.APP.OD.CHARGE.TXN` | `AzProductParameter_OdChargeTxn` |  |  |  |
| 86 | `AZ.APP.TAX.ON.INT.TXN` | `AzProductParameter_TaxOnIntTxn` |  |  |  |
| 87 | `AZ.APP.INT.CORR.TXN` | `AzProductParameter_IntCorrTxn` |  |  |  |
| 88 | `AZ.APP.INT.SUS.TXN` | `AzProductParameter_IntSusTxn` | TField | Yes | Valid Transaction code should be entered. Txn codes that affect the loan/deposit side (Debit or Credit) are identified and categorized as TXN codes. It is applicable only for Annuity contracts . If ANNUITY.ADJUST is Yes at the AZ contract level and the ANNUITY amount defined is lesser than the interest for the period, then the system shall use this transaction code to debit AZINTADJ Suspense account &amp; credit AZ Account. VALIDATIONS Must be a valid "ID" in transaction application Mandatory input if the ACCT. SYNC is set to "Y". Only debit transaction code allowed. Applicable only for ANNUITY contracts . |
| 89 | `AZ.APP.RESERVED.TXN.5` | `AzProductParameter_ReservedTxn5` | TField |  |  |
| 90 | `AZ.APP.RESERVED.TXN.6` | `AzProductParameter_ReservedTxn6` | TField |  |  |
| 91 | `AZ.APP.RESERVED.TXN.7` | `AzProductParameter_ReservedTxn7` | TField |  |  |
| 92 | `AZ.APP.LOCAL.REF` | `AzProductParameter_LocalRef` |  |  |  |
| 93 | `AZ.APP.PROD.START.DATE` | `AzProductParameter_ProdStartDate` | TField |  | This field indicates the date from which the product is avaliable. If defined then it may not be possible to create AZ.ACCOUNT with value dates prior to this date. The defined date cannot be greater than the PRODUCT.END.DATE. During repayments through AZ.ACCOUNT the value in REPAY.V.DATE cannot be lesser than the value defined here, if any. Not applicable for sub accounts opened under SAVINGS PLAN type of products, in this case only the master account is validated and not the sub accounts. |
| 94 | `AZ.APP.PROD.END.DATE` | `AzProductParameter_ProdEndDate` | TField |  | This field indicates the date from which the product is not avaliable. If defined then it may not be possible to create AZ.ACCOUNT with value date after this date. The defined date cannot be lesser than the PRODUCT.START.DATE. During repayments through AZ.ACCOUNT the value in REPAY.V.DATE cannot be greater than the value defined here, if any. Not applicable for sub accounts opened under SAVINGS PLAN type of products, in this case only the master account is validated and not the sub accounts. |
| 95 | `AZ.APP.CREATE.PD.EOD` | `AzProductParameter_CreatePdEod` | TField |  | For CREDIT CARD type of products this field indicates if PD has to be created during COB on the schedule dates if the repay account has insufficient funds. If set as 'YES' then PD will be created during COB, else PD will have to created through a online payment beyond the schedule date. This field is valid only for CREDIT CARD type of products. This field is allowed only if PD.LINK.TO.AZ.is set to 'YES'. Allowed values are 'YES', 'NO', ''. 'NO' and '' are considered to be the same. For credit cards, system has to settles first by date wise and then by component wise. Hence related PD.PARAMETER should have REPAYMENT.METHOD as '3' and only the following components are settled by AZ and in the order REPAYMENT.ORDER as A1,TX,A2,PE,PS,IN,Pr |
| 96 | `AZ.APP.PASSBOOK.ID` | `AzProductParameter_PassbookId` | TField |  | This field specifies the id of TELLER.PASSBOOK that need to be used while printing passbooks for this product. This field provides a facility to define different passbook layouts for different products. The id must be a valid id in TELLER.PASSBOOK. If not defined then the id SYSTEM in TELLER.PASSBOOK will be used by default while printing passbooks. |
| 97 | `AZ.APP.BONUS.ON` | `AzProductParameter_BonusOn` | TField |  | "This field defines the base amount on which bonus is to be calculated for deposits of SAVINGS PLAN type. If defined as 'PRINCIPAL' then the bonus will be calcuated on the amount in the PRINCIPAL field in AZ.ACCOUNT. If defined as 'INTEREST' then the bonus will be calculated on the interest that has been capitalised so far. If '' then the bonus will be calculated on the WORKING.BALANCE of the account. If MULTI is set to 'YES' then the base amount will be the sum of the amounts of the valid sub accounts. Allowed values are 'PRINCIPAL', 'INTEREST' and ''. This field should be read along with BONUS.ON.ARREARS. |
| 98 | `AZ.APP.BONUS.ON.ARREARS` | `AzProductParameter_BonusOnArrears` | TField |  | This field defines if bonus is to calculated even if there are any arrears in the payment installment of deposit. If defined as '' then even of one installment is not paid then bonus will not be calculated. If defined as 'YES' then if the number of late payments exeeds the defined LIAB.TO.PENALTY then bonus will not be calculated. Allowed values are 'YES' and ''. |
| 99 | `AZ.APP.EXTEND.MAT.DATE` | `AzProductParameter_ExtendMatDate` | TField |  | Currently noinput field. For future use. |
| 100 | `AZ.APP.REDUCE.LIMIT` | `AzProductParameter_ReduceLimit` | TField |  | This field is used to provide a revolving limit functionlity in loan products. If defined as '' then on the scheduled dates the limit is reduced for P and A schedules and incase of N schedules where there is no interest liquadation account, which provides a non-revolving functionlity. If defined as 'NO' then on the scheduled dates the limit is not reduced which provides a revolving limit functionlity. In this case the PRINCIPAL in AZ.ACCOUNT is not reduced on schedule dates or when the principal is repaid. Not allowed for deposits and CREDIT CARD. 'NO' not allowed for ANNUITY type of loans. If set to 'NO' then value in DRAWDOWN field cannot be set to 1, i.e., full drawdown. Allowed values are 'NO' or ''. |
| 101 | `AZ.APP.LAST.DAY.INCL` | `AzProductParameter_LastDayIncl` | TField |  | This field determines whether the cap date is considered for interest calculation or not. Valid inputs are 'Y' or NULL. 1. If 'Y', the interest calculation, will consider the balance as of the Cap date and post the entries with the value date of cap date + 1. 2. If null, in the interest calculation, the interest will be calculated for the balance of cap date -1, and the entries will be posted with the value date of processing date. In case of online loan repayment resulting in interest capitalisation/collection , LAST.DAY.INCL is considered to be 'N' even if LAST.DAY.INCL is 'Y' in AZ/APP Level. Validation This field is a noinput field for ANNUITY type of repayment. |
| 102 | `AZ.APP.IRA.PROCESS` | `AzProductParameter_IraProcess` | TField |  | This is a switch for activating the IRA processing in AZ. |
| 103 | `AZ.APP.LOAN.INT.ADJUST` | `AzProductParameter_LoanIntAdjust` | TField |  | This field is used to decide whether interest adjustment is allowed or not in an ANNUITY or CREDIT-CARD type AZ loan account. Interest adjustment is a functionality by which interest payment could be lesser than the accrued interest amount and the remaining amount would be debited from suspense account of class AZINTADJ. The suspense account would be settled during the subsequent repayments. Interest adjustment is allowed only if this field is set to YES. Validation Rules : Holds YES or NO Input allowed only if ACCT.SYNC field is set to YES. |
| 104 | `AZ.APP.ROLL.MAT.WRK.DAY` | `AzProductParameter_RollMatWrkDay` | TField |  | This field indicates if the rolled over new maturity date should be a working day or not. If the field is Yes, during rollover of loans &amp; deposits, if the new maturity date happens to be on a holiday, then the system shall set the new maturity date to the first working day after the rolled over date. Assume that value date of AZ is 10th Aug 2005 &amp; maturity date is 15th Aug 2005 . Then during rollover, the system shall set Value date as 15th Aug &amp; new maturity date as 20th Aug. If the field is YES &amp; 20th Aug happens to be a holiday &amp; first working day after rollover is 22nd Aug , then new maturity date will be set to 22nd Aug. If the field is NO, then new maturity date will still be 20th Aug only. Allowed values - YES,NO. NO &amp; null are treated the same. |
| 105 | `AZ.APP.INT.CORR.IMMED` | `AzProductParameter_IntCorrImmed` | TField |  | Defines the way system should collect interest correction amount . Value NEXT is allowed only for Annuity contracts &amp; with ANNUITY.ADJUST as YES . When correction entries are raised due to back-dated drawdown or repayment, then the system collects the correction amount from AZINTADJ Suspense account immediately . Only during NEXT repayment( online/cob), the system shall collect above correction amount from repayment account. Default value is CURRENT and in this case, correction amount is collected immediately from the repayment account . Values - CURRENT,NEXt |
| 106 | `AZ.APP.INT.CORR.RTN` | `AzProductParameter_IntCorrRtn` | TField |  | A local hook routine triggered when interest correction entry is raised for Annuity contracts with Annuity adjustment as YES. It takes precedence over INT.CORR.IMMED. It should be called with the following arguments (ACC.ID,'',INT.CORR.IMMED) where ACC.ID is input argument &amp; holds the AZ Account ID currently being processed. Y.INT.CORR.IMMED is output returned from this subroutine &amp; should hold the values of CURRENT, NEXT only. |
| 107 | `AZ.APP.PD.LINK.MAIN.AZ` | `AzProductParameter_PdLinkMainAz` | TField |  | For Credit cards, this field signifies if PD should be created for Main master account or for sub account. Value of 'YES' signifies that PD shall be created for Main account. Otherwise PD will be created for sub account. Can hold values YES &amp; blank. |
| 108 | `AZ.APP.PAY.INT.AT.MAT` | `AzProductParameter_PayIntAtMat` | TField |  | This field is used to decide whether accrued interest has to be paid out or rolled over during rollover of a fixed deposit. Allowed values are YES or NO If 'YES' is defined, then during rollover, the accrued interest before rollover will be paid out .When final maturity is reached or maturity instruction is Payment to Nominated account, then interest will be paid to Nominated account. If 'NO' is defined, then interest will be rolled over along with the principal deposit amount by default. The amount of rolled over deposit can anyway be overridden by the rollover amount definition at AZ.ACCOUNT level |
| 109 | `AZ.APP.CREATE.TD.FOR.INT` | `AzProductParameter_CreateTdForInt` | TField |  | This field indicates whether to create TELLER.DEFAULT or not for the interest amount during part withdrawal(through TELLER) /Pre closure when till account is used as credit side account. . If this value is YES then a TELLER.DEFAULT record will be created when a TELLER transaction is processed debiting an AZ.ACCOUNT Fixed Deposit. This will only be created when no nominated/interest liquidation is specified on the AZ.ACCOUNT. If the field CREATE.TD.FOR.INT is NULL the interest will be credited to the credit side account irrespective of it being the till account. Validation Rules : Value of YES is permitted only for Deposits &amp; with Teller product installed in the company |
| 110 | `AZ.APP.COMPOUND.TYPE` | `AzProductParameter_CompoundType` | TField |  | This field accepts the frequency like DAILY,WEEKn, MONTHnn, Nnn to determine the compounding period in a year. Validation Rules : ALLOWED for fixed deposits only |
| 111 | `AZ.APP.CC.AMT.RTN` | `AzProductParameter_CcAmtRtn` | TField |  | This field holds a user defined API which calculates and updates the TYPE.CC amount in a CREDIT-CARD type AZ loan account created under this product. If exists, this API will be executed to calculate and update TYPE.CC amount field in AZ.SCHEDULES for both AZ sub account and AZ main account. Validation Rules : A valid key from EB.API file Input allowed only with repayment type as CREDIT-CARd |
| 112 | `AZ.APP.PAY.INT.AT.PARTRED` | `AzProductParameter_PayIntAtPartred` | TField |  | Field at the product level, to indicate if interest is to be paid to Settlement account immediately during part redemption. If set to NULL, then interest is paid out immediately to Settlement account during part redemption. If set to NO, then interest will be paid only during the next IN or on the maturity date. Validation Rules Allowed only for normal Fixed deposits. Default is null. |
| 113 | `AZ.APP.NONAZ.COLL.RED.FEE` | `AzProductParameter_NonazCollRedFee` | TField |  | Field to indicate if PRE.CLOSURE.FEE should be collected when part redemption or pre closure is done through TELLER/FT. When set to NULL, the fee is not collected when redemption is done through TELLER/FT. When set to YES, the fee is charged on the AZ.ACCOUNT or Settlement account based on the field CHG.FROM.RED.AMT in AZ.ACCOUNT. Validation Rules Only allowed for normal Fixed deposits. |
| 114 | `AZ.APP.IAS.CLASSIFICATION` | `AzProductParameter_IasClassification` | TField |  | This field holds the classification of contract based on IFRS standard.Input not allowed for Credit-card type of contractsInput not allowed when ACCT.SYNC is set to null |
| 115 | `AZ.APP.IAS.SUB.TYPE` | `AzProductParameter_IasSubType` | TField |  | This field holds the Sub type for the corresponding classification defined in IAS.CLASSIFICATION field. |
| 116 | `AZ.APP.CAP.INT.AT.SOD` | `AzProductParameter_CapIntAtSod` | TField |  | Field at the product level, to indicate if the Schedule on the Due date should be processed during the Start of Day or End of Day. If set to YES, then the Schedule is processed during the Start of Day. If set to NO, then the Schedule is processed during the End of Day. Validation rules: Accepted values YES or NO Allowed only for Fixed Deposits. Defaulted to NO |
| 117 | `AZ.APP.MAT.DATE.IN.LOCALREG` | `AzProductParameter_MatDateInLocalreg` | TField |  |  |
| 118 | `AZ.APP.RESERVED.6` | `AzProductParameter_Reserved6` | TField |  |  |
| 119 | `AZ.APP.RESERVED.5` | `AzProductParameter_Reserved5` | TField |  |  |
| 120 | `AZ.APP.RESERVED.4` | `AzProductParameter_Reserved4` | TField |  |  |
| 121 | `AZ.APP.RESERVED.3` | `AzProductParameter_Reserved3` | TField |  |  |
| 122 | `AZ.APP.RESERVED.2` | `AzProductParameter_Reserved2` | TField |  |  |
| 123 | `AZ.APP.RESERVED.1` | `AzProductParameter_Reserved1` | TField |  |  |
| 124 | `AZ.APP.OVERRIDE` | `AzProductParameter_Override` |  |  |  |
| 125 | `AZ.APP.RECORD.STATUS` | `AzProductParameter_RecordStatus` | String |  |  |
| 126 | `AZ.APP.CURR.NO` | `AzProductParameter_CurrNo` | String |  |  |
| 127 | `AZ.APP.INPUTTER` | `AzProductParameter_Inputter` |  |  |  |
| 128 | `AZ.APP.DATE.TIME` | `AzProductParameter_DateTime` |  |  |  |
| 129 | `AZ.APP.AUTHORISER` | `AzProductParameter_Authoriser` | String |  |  |
| 130 | `AZ.APP.CO.CODE` | `AzProductParameter_CoCode` | String |  |  |
| 131 | `AZ.APP.DEPT.CODE` | `AzProductParameter_DeptCode` | String |  |  |
| 132 | `AZ.APP.AUDITOR.CODE` | `AzProductParameter_AuditorCode` | String |  |  |
| 133 | `AZ.APP.AUDIT.DATE.TIME` | `AzProductParameter_AuditDateTime` | String |  |  |
