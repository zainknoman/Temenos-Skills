# REPO.TYPE — Table Schema

> Source: `INSERTS/I_F.REPO.TYPE` in `RP_Config.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `RP.TYP.DESCRIPTION` | `RepoType_Description` |  |  |  |
| 2 | `RP.TYP.PRODUCT.CATEGORY` | `RepoType_ProductCategory` | TField | Yes | This field is used to specify the default product category that will be used for the type of Repo contract to which this record refers. The product category is defaulted into the Repo contract, and is used to determine whether the cash side of the contract is a loan or a deposit. It is this information that broadly governs if the contract is a Repo or a Reverse Repo. Where the product category indicates a DEPOSIT, the repo type refers to a REPO contract. Where the product category indicates a LOAN, the repo type refers to a REVERSE REPO contract. This field is closely linked to the TRANSACTION.INDEX field that holds the text "LOAN" or "DEPOSIT" to clarify the product category. The TRANSACTION.INDEX and the product category must agree. Refer to the Rules. Validation Rules: Mandatory input unless DEAL.TYPE is BUY/SELL.BACK, SELL/BUY.BACK or INTERNAL A maximum of 5 characters may be entered. Values in the range 21200 to 21299 are allowed. For repo contracts, input must be in the range 21200 - 21249 and the TRANSACTION.INDEX must be DEPOSIT. For Reverse repo contracts, input must be in the range 21250 - 21299 and the TRANSACTION.INDEX must be LOAN. Must be the key to a valid entry on the CATEGORY file. |
| 3 | `RP.TYP.TRANSACTION.INDEX` | `RepoType_TransactionIndex` | TField | Yes | This field is linked to the PRODUCT.CATEGORY field and is used to clarify the type of repo contract, either as a repo or as a reverse repo. REPO contracts are cash DEPOSITS and a transfer out of securities. REVERSE REPO contracts are cash LOANS and a transfer in of securities. Unless the DEAL.TYPE is INTERNAL, BUY/SELL.BACK or SELL/BUY.BACK the this field must agree with the PRODUCT.CATEGORY field as follows ; For BUY/SELL.BACK TRANSACTION.INDEX will be automatically set as LOAN, for SELL/BUY.BACK it will be as DEPOSIT. Validation Rules: Mandatory input. A maximum of 7 characters may be entered. The following values are permitted: LOAN or DEPOSIT |
| 4 | `RP.TYP.MARGIN.PORT.SUFFIX` | `RepoType_MarginPortSuffix` | TField | Yes | The default suffix that will be applied counterparty's customer number to determine the default margin portfolio for use in the REPO contract. The suffix must be defined in the REPO.PARAMETER record (in the REPO.MARGIN.SUF field) before it can be referenced in a REPO.TYPE record. Where the default margin portfolio does not exist, the MARGIN.PORTFOLIO must be entered into the REPO contract. It is this portfolio that is used to determine if a margin call is required (or is to be expected) and as such should contain no other holdings except those from the REPO application. Validation Rules: Mandatory input unless CUSTOMER.REPO is Yes, or DEAL.TYPE is BUY/SELL.BACK, SELL/BUY.BACK or INTERNAL A maximum of 3 characters may be entered. |
| 5 | `RP.TYP.CUSTOMER.REPO` | `RepoType_CustomerRepo` | TField |  | This flag is used to trigger the Customer Repo processing within the REPO module. Where this flag is set to "Yes" contracts of this REPO.TYPE are considered to be performed on behalf of one of the banks clients. As such the cash side of the contract should be shown below the line. On the REPO contract itself, the BANK.PORTFOLIO field becomes no input. Instead the field CUST.PORTFOLIO is used to determine the customer's portfolio that will supply the securities. The SECURITY.TRANSFER performed under this type of contract is carried out Delivery Against Payment, and as such the cash side of the contract is considered to be notional, with an internal account being used for the accounting. Validation Rules: Only "Yes", "No" and blank are allowed. Blank is treated as "No" |
| 6 | `RP.TYP.ACCT.CATEGORY` | `RepoType_AcctCategory` | TField |  | For contracts that are "Customer" Repos, the accounts entered on the main REPO contract are internal accounts. This field is used to provide a default for this account. Validation Rules: Only allowed where the field CUSTOMER.REPO is set to "Yes" Must be a valid category in the range 10000 - 19999 |
| 7 | `RP.TYP.TRANSACTION.TYPE` | `RepoType_TransactionType` | TField |  | Identifies the transaction types to be used in the security transfers. Validation Rules: Must be a valid entry in SC.TRANS.TYPE If left blank, type TRO (debit stock) and TRI (credit stock) will be defaulted to the Repo contract and used in the security transfer records. |
| 8 | `RP.TYP.DEFAULT.DELIVERY` | `RepoType_DefaultDelivery` | TField | Yes | This sets the Delivery Instructions that will default automatically into the REPO Contract field DELIVERY.INSTR for this REPO.TYPE. If this field is left blank then the DELIVERY.INSTR will be left blank on the REPO Contract &amp; the user will be prompted to make an entry on committing the record as the field is mandatory input. Validation Rules: A valid entry in the SC.DEL.INSTR application. |
| 9 | `RP.TYP.DEAL.TYPE` | `RepoType_DealType` | TField |  | The valid DEAL.TYPE's are CLASSIC, OPEN, BUY/SELL BACK, SELL/BUY BACK,INTERNAL and STOCK.BORROW.LEND.These DEAL.TYPE's are available to both REPOs and REVERSE REPOs. CLASSIC : To be used for standard Repo or Reso trades. OPEN : To be used for Open Repos. When the REPO.TYPE used in a transactionhas been set up as a OPEN Repo, the Maturity.Date on the Repo transaction willaccept a value of 0. Daily accrual of interest takes place on thePrincipal.Amt.1. Accrued interest is capitalised on a daily basis and isreflected in the value of Principal.Amt.1 changing due to interest accruals. BUY/SELL BACK : This type is used when the REPO trade is treated as apurchase of the security, with an agreement to sell back the security at apredetermined price. The sale price would comprise of the purchase price + theRepo interest + any corporate action that may have been paid on the security. SELL/BUY BACK : The opposite of a BUY/SELL BACK. INTERNAL: An internal trade moving stock from a bank portfolio to a customerportfolio. STOCK.BORROW.LEND: To be used when using Stock Borrowing/Lending. Validation Rules: None, will default to CLASSIC |
| 10 | `RP.TYP.DEFAULT.PRICE` | `RepoType_DefaultPrice` | TField |  | YES/ NO field. Defaulted to YES. This field will determine if price has to be fetched from SECURITY.MASTER when the user inputs the security number in the REPO contract. For Customer repos the price is determined from principal 1 and the nominal and will not be set from SECURITY.MASTER. Validation Rules: |
| 11 | `RP.TYP.GENERATE.FT` | `RepoType_GenerateFt` | TField |  | Option to select YES or NO. When the user has selected the option of YES the cash margin process messaging associated with this REPO.TYPE will be handled via FUNDS.TRANSFER, with the generation of the SWIFT linked to the original contract. No MM.MONEY.MARKET contract will be generated. The accounting with be generated by associated SECURITY.TRANSFER. If NO, or blank, is selected the normal process associated with the child MM.MONEY.MARKET will remain. |
| 12 | `RP.TYP.FT.TRANS.TYPE` | `RepoType_FtTransType` | TField | Yes | Used in conjunction with GENERATE.FT to assign a value to the field TRANSACTION.TYPE in the FUNDS.TRANSFER contracts. Validation Rules: Can only be specified if GENERATE.FT is YES Mandatory input with GENERATE.FT is YES Must be a valid record in FT.TXN.TYPE.CONDITION |
| 13 | `RP.TYP.TRANS.TYPE` | `RepoType_TransType` | TField |  | This field determines whether the REPO trade will be cash or security driven and will set the value in the REPO trade. Validation Rules: Either CASH or SECURITY If not set then the REPO trade will default to CASH |
| 14 | `RP.TYP.DEALER.BOOK.SUSP` | `RepoType_DealerBookSusp` | TField |  | Must be a CATEGORY code from 10000 to 19999. Can only be used if GENERATE.FT field is YES and DEAL.TYPE is not equal to INTERNAL. Will be used to default a value for NEW.CU.ACCT.NO when BANK.PORTFOLIO is a dealer book SEC.ACC.MASTER. |
| 15 | `RP.TYP.EFFECTIVE.RATE` | `RepoType_EffectiveRate` | TField |  | This is a flag that will be used to allow input of effective rate in the repo contract. If this field is set to YES then the effective rate would be allowed in REPO contract. Allowed values are YES or NO or null |
| 16 | `RP.TYP.SRT.CR.CAT` | `RepoType_SrtCrCat` | TField |  | This field is allowed to input when the TRANSACTION.INDEX field in REPO.TYPE is set to LOAN to accept internal category.Allowed values are between 10000 - 19999. |
| 17 | `RP.TYP.SRT.COL.CAT` | `RepoType_SrtColCat` | TField |  | This field is allowed to input when the TRANSACTION.INDEX field in REPO.TYPE is set to LOAN to accept internal category.Allowed values are between 10000 - 19999. |
| 18 | `RP.TYP.SRT.COM.CAT` | `RepoType_SrtComCat` | TField |  | This field is allowed to input when the TRANSACTION.INDEX field in REPO.TYPE is set to LOAN to accept internal category.Allowed values are between 10000 - 19999. |
| 19 | `RP.TYP.SRT.PR.CAT` | `RepoType_SrtPrCat` | TField |  | This field is used to specify the categories defined in the SRT.LOSS.CAT or SRT.PR.CAT (based on the loss or profit respectively) and to the category defined in SRT.COM.CAT.If the price of the repo contract and the SEC.TRADE transaction is different then these additional entries are raised to the extent of the profit or loss of the transaction.Allowed values are between 50000 � 59999. |
| 20 | `RP.TYP.SRT.LOSS.CAT` | `RepoType_SrtLossCat` | TField |  | This field is used to specify the categories defined in the SRT.LOSS.CAT or SRT.PR.CAT (based on the loss or profit respectively) and to the category defined in SRT.COM.CAT.If the price of the repo contract and the SEC.TRADE transaction is different then these additional entries are raised to the extent of the profit or loss of the transaction.Allowed values are between 50000 � 59999. |
| 21 | `RP.TYP.LINK` | `RepoType_Link` | TField |  | This field will hold the repo type of the opposite repo contract that will be created automatically. Validation Rules: Input allowed only when STP is set to YES in REPO.PARAMETER. If TRANSACTION.INDEX is set as LOAN in the REPO.TYPE record, then TRANSACTION.INDEX must be set as DEPOSIT in the repo type that is attached to this field. Similarly if DEPOSIT, should be set as LOAN in repo type attached. Only repo type with DEAL.TYPE set as INTERNAL can be attached to this field. |
| 22 | `RP.TYP.RESERVED.1` | `RepoType_Reserved1` | TField |  |  |
| 23 | `RP.TYP.LOCAL.REF` | `RepoType_LocalRef` |  |  |  |
| 24 | `RP.TYP.RECORD.STATUS` | `RepoType_RecordStatus` | String |  |  |
| 25 | `RP.TYP.CURR.NO` | `RepoType_CurrNo` | String |  |  |
| 26 | `RP.TYP.INPUTTER` | `RepoType_Inputter` |  |  |  |
| 27 | `RP.TYP.DATE.TIME` | `RepoType_DateTime` |  |  |  |
| 28 | `RP.TYP.AUTHORISER` | `RepoType_Authoriser` | String |  |  |
| 29 | `RP.TYP.CO.CODE` | `RepoType_CoCode` | String |  |  |
| 30 | `RP.TYP.DEPT.CODE` | `RepoType_DeptCode` | String |  |  |
| 31 | `RP.TYP.AUDITOR.CODE` | `RepoType_AuditorCode` | String |  |  |
| 32 | `RP.TYP.AUDIT.DATE.TIME` | `RepoType_AuditDateTime` | String |  |  |
