# REPO.PARAMETER — Table Schema

> Source: `INSERTS/I_F.REPO.PARAMETER` in `RP_Config.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `RP.PAR.REPO.MARGIN.SUF` | `RepoParameter_RepoMarginSuf` |  |  |  |
| 2 | `RP.PAR.RP.MRGN` | `RepoParameter_RpMrgn` |  |  |  |
| 3 | `RP.PAR.RESO.MARGIN.SUF` | `RepoParameter_ResoMarginSuf` |  |  |  |
| 4 | `RP.PAR.RS.MRGN` | `RepoParameter_RsMrgn` |  |  |  |
| 5 | `RP.PAR.INIT.MRGN.BOOKING` | `RepoParameter_InitMrgnBooking` | TField | Yes | The initial margin of a REPO contract can be used as a device to undervalue the securities involved; hence to cover the risk of the adverse movement of the securities which may arise before a margin call can be paid. This field indicates the booking method of the initial margin. NOTIONAL for customer REPOs only - initial margin will be included in margin portfolio valuation. NONE - for information only. Note : Initial margin for Banks own book trading can be specified at the capture of the REPO / RESO by using either SC.INIT.MGN.RTE or INIT.MARGIN.RATE fields Validation Rules: Mandatory input. A maximum of 10 characters may be entered. This is a NOCHANGE field. The following values are permitted: NONE NOTIONAL |
| 6 | `RP.PAR.REPO.LIMIT.UPDATE` | `RepoParameter_RepoLimitUpdate` | TField | Yes | A REPO contract is treated as a loan of securities and a deposit of cash. However, the bank may see the loan of the securities as a potential risk, and may wish a repo contract to update the T24 limit processing as if it were a loan - thus decreasing the available limit. This field allows the user to define whether the limit for a REPO is regarded as either a LOAN or a DEPOSIT. Validation Rules: Mandatory input. A maximum of 7 characters may be entered. This is a NOCHANGE field. The following values are permitted: LOAN DEPOSIT |
| 7 | `RP.PAR.DAYS.POST.MATURITY` | `RepoParameter_DaysPostMaturity` | TField |  | This field states the number of days after maturity that a contract is removed from the live file and written to History. A typical input would be 30 so that monthly reports could include contracts that matured during the month without having to interrogate History files. Validation Rules: A maximum of 3 characters may be entered. The following values are permitted: 1 to 366 |
| 8 | `RP.PAR.CALCULATION.LINK` | `RepoParameter_CalculationLink` | TField |  | This field contains the default which will be copied into each REPO record as it is created. Validation Rules: YES or NO |
| 9 | `RP.PAR.AUTO.CALC.NOMINAL` | `RepoParameter_AutoCalcNominal` | TField | Yes | When inputting a REPO contract, if the TRANS.TYPE is set to CASH this field is set to YES, the system will take the cash amount entered in PRINCIPAL.AMOUNT.1 plus any initial margin calculate the amount of nominal required to exceed the cash amount. If this field is set to NO then manual calculations by the user will be required in the REPO contract. Validation Rules: YES or NO Mandatory if CALCULATION.LINK is set to YES NOINPUT if CALCULATION.LINK is set to NO |
| 10 | `RP.PAR.UPDATE.SC.POS.SOD` | `RepoParameter_UpdateScPosSod` | TField |  | A no change field. YES / NO field. Null has the same effect as NO. If NO or NULL is selected, The SECURITY.TRANSFER record for both legs of the REPO contract will get generated n days before the value date or maturity date of the repo contract - where n is a) if DAYS.DELIVERY field is input by the user b) otherwise the value input in DAYS.PRIOR.EVENT for activity RP-0140 in EB.ACTIVITY. The SECURITY.POSITION gets updated immediately on creation of the SECURITY.TRANSFER. If YES is selected, 1. Irrespective of the value date, the SECURITY.TRANSFER record for the near leg will get generated immediately upon authorisation of the REPO contract. The SECURITY.POSITION gets updated on creation of the SECURITY.TRANSFER. 2. The security transfer record for the far leg of the REPO contract will get generated n days before the actual maturity date of the repo contract - where n is a) if DAYS.DELIVERY field is input by the user b) otherwise the value input in DAYS.PRIOR.EVENT for activity RP-0140 in EB.ACTIVITY. 3. If the far leg SECURITY.TRANSFER record is generated prior to maturity date, (as would happen where n has a value) the SECURITY.POSITION will not get updated immediately on generation of the SECURITY.TRANSFER, but it will get updated in the SOD of the maturity date of the REPO. Validation Rules: Valid input either YES, NO or NULL. Null has the same effect as NO. |
| 11 | `RP.PAR.PROCESS.REPO.SOD` | `RepoParameter_ProcessRepoSod` | TField |  | This field determines the the following events to happen at SOD instead of at EOD a. Forward dated REPO contracts become LIVE b. Maturity of the REPO contracts NULL - Both (a) and (b) happen at EOD FWD TO LIVE - Only (a) happen at SOD MATURITY - Only (b) happen at SOD BOTH - Both (a) and (b) happen at SOD Validation Rules: If UPDATE.SC.POS.SOD is set to YES, then this field can accept only MATURITY or BOTH as the near leg SECURITY.TRANSFER record gets generated immediately on authorisation of the REPO contract. |
| 12 | `RP.PAR.ENT.CPN.SUSP` | `RepoParameter_EntCpnSusp` | TField |  | This field should be set up if a special account has to be used for the Reso entries in the Corporate Actions process (DIARY ENTITLEMENT). The CATEGORY value is used to built the ACCOUNT.NO value in the ENTITLEMENT record for the RESO portfolio. Validation Rules: the default value is null. Only a valid CATEGORY can be input. |
| 13 | `RP.PAR.SPLIT.DEBIT.CPN` | `RepoParameter_SplitDebitCpn` | TField |  | If the field is set to YES then the REPO accounting for the Nostro will be separated from the normal accounting in the Corporate Actions process (DIARY ENTITLEMENT). In case of REPO entries, the global cash amount will be shown in the multi-values set prefixed by RP in the DIARY record. The details of the REPO entries per depository are shown in the RP.ALT.NOSTRO record which is keyed on the DIARY id. Validation Rules: The default value is null. Only YES or NO is allowed. |
| 14 | `RP.PAR.AC.BLOCK.CLOSE.UPD` | `RepoParameter_AcBlockCloseUpd` | TField |  | This field can be set to NO to prevent the automatic update of AC.BLOCK.CLOSURE from the REPO application for REPO field NEW.CU.ACCT.NO. If set to NO then no updates will be made and the ACCOUNT.CLOSURE application will be able to close accounts used in live REPO contracts so caution should be used when setting it to NO as internal processes must exist to prevent such occurrences. |
| 15 | `RP.PAR.RESERVED.2` | `RepoParameter_Reserved2` | TField |  |  |
| 16 | `RP.PAR.RESERVED.1` | `RepoParameter_Reserved1` | TField |  |  |
| 17 | `RP.PAR.LOCAL.REF` | `RepoParameter_LocalRef` |  |  |  |
| 18 | `RP.PAR.RECORD.STATUS` | `RepoParameter_RecordStatus` | String |  |  |
| 19 | `RP.PAR.CURR.NO` | `RepoParameter_CurrNo` | String |  |  |
| 20 | `RP.PAR.INPUTTER` | `RepoParameter_Inputter` |  |  |  |
| 21 | `RP.PAR.DATE.TIME` | `RepoParameter_DateTime` |  |  |  |
| 22 | `RP.PAR.AUTHORISER` | `RepoParameter_Authoriser` | String |  |  |
| 23 | `RP.PAR.CO.CODE` | `RepoParameter_CoCode` | String |  |  |
| 24 | `RP.PAR.DEPT.CODE` | `RepoParameter_DeptCode` | String |  |  |
| 25 | `RP.PAR.AUDITOR.CODE` | `RepoParameter_AuditorCode` | String |  |  |
| 26 | `RP.PAR.AUDIT.DATE.TIME` | `RepoParameter_AuditDateTime` | String |  |  |
| 27 | `RP.PAR.STP` | `RepoParameter_Stp` | TField | Yes | This field will indicate whether auto creation of opposite SBL internal trade is allowed or not. Validation Rules: This field will be a non-mandatory field. Allowed values are yes or blank. |
| 28 | `RP.PAR.ACCRUE.FEE` | `RepoParameter_AccrueFee` | TField | Yes | This field will indicate whether fee will accrue until contract matures for fixed term REPO contracts or until closed out for open trades. Validation Rules: This field will be a non-mandatory field. Allowed values are yes or blank. |
| 29 | `RP.PAR.FEE.REFUND` | `RepoParameter_FeeRefund` | TField | Yes | This field will indicate whether fee will be refunded on early maturity of the fixed term REPO contracts. Refund of the fee will be required only if the fee was taken on value date or maturity date. Validation Rules: This field will be a non-mandatory field. Allowed values are yes or blank. |
| 30 | `RP.PAR.CHG.ACC.CAT` | `RepoParameter_ChgAccCat` | TField | Yes | This field will denote the internal account category which will be used for raising fee entries during accrual. Validation Rules: This field will be a mandatory field if ACCRUE.FEE is set as YES. Allowed values are from 10000 to 19999. |
| 31 | `RP.PAR.PRODUCT.CATEGORY` | `RepoParameter_ProductCategory` | TField | Yes | This field will denote the product category used for updating in fee entries Validation Rules: This field will be a mandatory field if ACCRUE.FEE is set as YES. Allowed values are from 22000 to 22999. |
| 32 | `RP.PAR.OFS.SOURCE` | `RepoParameter_OfsSource` |  |  |  |
| 33 | `RP.PAR.OFS.VERSION` | `RepoParameter_OfsVersion` |  |  |  |
| 34 | `RP.PAR.FEE.DAY.CONVENTION` | `RepoParameter_FeeDayConvention` | TField |  |  |
| 35 | `RP.PAR.FEE.DAY.BASIS` | `RepoParameter_FeeDayBasis` | TField |  |  |
| 36 | `RP.PAR.FEE.IN.PERCENTAGE` | `RepoParameter_FeeInPercentage` | TField |  |  |
