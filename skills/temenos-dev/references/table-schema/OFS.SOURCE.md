# OFS.SOURCE — Table Schema

> Source: `INSERTS/I_F.OFS.SOURCE` in `EB_Interface.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `OFS.SRC.DESCRIPTION` | `OfsSource_Description` | TField | Yes | Description of the OFS SERVICE defined in this record. Validation Rules: Mandatory Input 3 - 35 alphanumeric characters |
| 2 | `OFS.SRC.SOURCE.TYPE` | `OfsSource_SourceType` | TField | Yes | Defines the type of communication used to exchange data with this service. OFS allows communication through a BATCH process where a directory is populated with records in either a particular file, or several files. When combined with the OO module, single messages may be passed to T24 through a TELNET connection. Records can be created in a T24 program or sub-routine using the T24 setting, whilst the new T24 broswer uses the SESSION option. BATCH Not recommended for usage in TAFj runtime A Batch connection will usually run as a background Phantom task initiated by the EB.PHANTOM application. This will examine a specified directory (the IN.QUEUE.FILE), either checking for a specific file (IN.QUEUE.NAME) or checking the entire directory for files to process. Each file may contain multiple records. The processed records will be written to the out directory (OUT.QUEUE.FILE), either to a specific file (OUT.QUEUE.NAME) or a file with the same name as the inward file. The batch connection will support GTS syntax messages or OFS syntax messages. TELNET The Telnet connection allows for messages to be passed using a simple communication mechanism. Each message sent to OFS will be processed, the results will be returned to the external source. The Telnet communication will be initiated automatically when the external source initiates a login using a specific id, defined in the field LOGIN.ID. SESSION Used for new T24 browser Validation Rules: Mandatory Input Possible values are TELNET, BATCH, SESSION or T24 |
| 3 | `OFS.SRC.LOGIN.ID` | `OfsSource_LoginId` |  |  |  |
| 4 | `OFS.SRC.EB.PHANT.ID` | `OfsSource_EbPhantId` |  |  |  |
| 5 | `OFS.SRC.MAX.CONNECTIONS` | `OfsSource_MaxConnections` | TField |  | Specifies the maximum number of on-line OFS connections for the specified service which can be active at any one time. Each OFS on-line session is a single UniVerse user, so the number specified here must be less than the maximum number of users (defined in SPF - NO.OF.USERS). When a new on-line session is started the system will create a new connection is required providing the maximum number of connections defined here has not been exceeded. Validation Rules: Numeric input, in the range 1 - 99,999 The value entered should contain a minimum of 5 less than the value given in SPF>NO.OF.USER field. |
| 6 | `OFS.SRC.RESTRICT.LINK` | `OfsSource_RestrictLink` | TField | No | Used to control whether the service is currently open. The service can be closed for all on-line communication by specifying CLOSE, alternative enquiry only access can be provided by specifying ENQ. The system will not allow updates to take place during end of day processing, irrespective of the values defined here. Validation Rules: Optional Input Possible values CLOSE or ENQ |
| 7 | `OFS.SRC.INITIAL.ROUTINE` | `OfsSource_InitialRoutine` | TField | No | Allows a user defined routine to be specified for a particular service. The routine specified here will be executed when a service is started. Typically such a routine could intialise a common area or perform some initial communication. Routine Details Arguments : None Note at the point this routine is called NONE of the T24 common variables are available. Validation Rules: Optional Input Must be defined in PGM.FILE as a type S program. |
| 8 | `OFS.SRC.CLOSE.ROUTINE` | `OfsSource_CloseRoutine` | TField | No | Allows a user defined routine to be specified for a particular service. The routine specified here will be executed when a service is closed. Typically such a routine could intialise a common area or perform some final communications. Routine Details Arguments : None Note at the point this routine is called NONE of the T24 common variables are available. Validation Rules: Optional Input Must be defined in PGM.FILE as a type S program. |
| 9 | `OFS.SRC.IN.MSG.RTN` | `OfsSource_InMsgRtn` | TField |  | This field usage is deprecated. Recommend to use Integration Framework exits. |
| 10 | `OFS.SRC.OUT.MSG.RTN` | `OfsSource_OutMsgRtn` | TField |  | This field usage is deprecated. Recommend to use Integration Framework exits. |
| 11 | `OFS.SRC.MSG.PRE.RTN` | `OfsSource_MsgPreRtn` | TField | No | Allows a user defined routine or an EB.API record of type METHOD which implements an interface defined in the EB.API record OFS.SOURCE.MSG.PRE.RTN.HOOK to be specified for a particular service. The routine specified here will be executed within the OFS processing prior to application update or execution of Enquiry. Typically such a routine could update additional local applications. For java implementations: An EB.API record id with a source type of HOOK which implements an interface defined in the EB.API record OFS.SOURCE.MSG.PRE.RTN.HOOK. This field supports the MessageLifecycle.preProcess() method. The MessageLifecycle class is in the com.temenos.t24.api.hook.system package which is in EB.MessageHook.jar shipped with T24. Routine Details Arguments : OFS.MESSAGE Note the content of the message cannot be modified within this routine. Validation Rules: Optional Input Must be defined in PGM.FILE as a type S program. |
| 12 | `OFS.SRC.MSG.POST.RTN` | `OfsSource_MsgPostRtn` | TField | No | Allows a user defined routine or an EB.API record of type METHOD which implements an interface defined in the EB.API record OFS.SOURCE.MSG.POST.RTN.HOOK to be specified for a particular service. The routine specified here will be executed within the OFS processing after application update or execution of Enquiry. Typically such a routine could update additional local applications. For java implementations: An EB.API record id with a source type of HOOK which implements an interface defined in the EB.API record OFS.SOURCE.MSG.POST.RTN.HOOK. This field supports the MessageLifecycle.postProcess() methods. The MessageLifecycle class is in the com.temenos.t24.api.hook.system package which is in EB.MessageHook.jar shipped with T24. Routine Details Arguments : OFS.MESSAGE Note the content of the message cannot be modified within this routine. Validation Rules: Optional Input Must be defined in PGM.FILE as a type S program for jbc routine. |
| 13 | `OFS.SRC.LOG.FILE.DIR` | `OfsSource_LogFileDir` | TField | Yes | Defines the name of the log file directory used to log OFS communications. The directory name here can be specified as a full Unix or NT path, note that in an NT environment the path should be specified using '/' characters instead of '\'. If no path is specified then the directory will be created automatically by the system at authorisation, otherwise the directory must already exist. The log file directory name is required if message logging is required. For example, a log file directory maintained in the "EB" directory of the data account could be defined as: ../bnk.data/eb/ofslog Each on-line communication process will maintain a separate log file within the directory, the key to the log file will be the EB.PHANTOM key used to monitor the process. Using field INT.LOG.FILE in SPF, switch has been introduced to decide whether to log messages internally within T24 or to use TAFC level log file In SPF, if INT.LOG.FILE = YES meaning T24 level log is enabled and then messages will be logged into log file directory if INT.LOG.FILE = null (no input), meaning TAFC level log is enabled by default and then messages will be logged into TAFC log files Points to be noted: 1. The log file directory should be specified if message logging is required at T24 level. 2. The field LOG.FILE.DIR can have value even though the TAFC log is enabled but log messages will be routed to either T24/TAFC log files depends upon SPF setup. Validation Rules: Mandatory Input if LOG.DETAIL.LEVEL is specified. Must be a type 1 or 19 file if an existing file is defined. |
| 14 | `OFS.SRC.LOG.DETAIL.LEVEL` | `OfsSource_LogDetailLevel` | TField | Yes | Specifies the type of OFS message logging to be performed by OFS. The following levels of message logging are supported: NONE - No message logging OPEN - Only initial start and stop for processes are logged EXCEPT - Only exceptions such as incorrect message syntax, invalid user details are reported. FULL - All message communications are fully logged. The use of FULL logging is only advised in testing or debugging communications due to the large size of log file that could be created. Validation Rules: Valid input NONE, OPEN, EXCEPT or FULL Mandatory Input |
| 15 | `OFS.SRC.OFFLINE.QUEUE` | `OfsSource_OfflineQueue` | TField | No | Specifies whether the service has an offline capability. OFS will allow processing of Enquiries during online and offline mode, entry of transactions is not possible when the system is off-line. If this field is set to Y the system will maintain a store and forward queue when transactions are submitted whilst the system is offline. Transactions can be processed when the system is back on-line by running an EB.PHANTOM process using the RUN.ROUTINE OFS.QUEUE.MANAGER. Details of transactions are held in the OFS.REQUEST.DETAIL application, each message will be allocated a standard T24 format id (XXXXjjjjjnnnnn) with a prefix defined in the DET.PREFIX field. Input not allowed when the Source.type is Batch. Validation Rules: Optional field Possible value Y. |
| 16 | `OFS.SRC.MAINT.MSG.DETS` | `OfsSource_MaintMsgDets` | TField | No | Specifies whether an audit record is to be maintained for each OFS message sent and received.# If this field is set to Y the file OFS.REQUEST.DETAIL will be updated with the progress and content of each OFS request. Each message will be allocated a separate id in the format: Xxxxxxjjjjjnnnn Where: Xxxxxx is a 2-6 character prefix defined in DET.PREFIX Jjjjj is the system julian date when the message was created Nnnnn is a serial number 1-99999 Alternatively a specified message key can be supplied in the OFS message Validation Rules: Optional Field Possible value Y Note: OFS.REQUEST.DETAIL will be updated when the OFS request contains a Message Id even if this field is not set to 'Y' |
| 17 | `OFS.SRC.DET.PREFIX` | `OfsSource_DetPrefix` | TField | Yes | Specifies the prefix to be used for the message details recorded when an audit trail is maintained or an offline capability is provided. Validation Rules: Mandatory input if OFFLINE.QUEUE or MAINT.MSG.DETS are set to Y. 2 - 6 alpha characters |
| 18 | `OFS.SRC.IN.QUEUE.DIR` | `OfsSource_InQueueDir` | TField | Yes | Defines the name of the directory used to hold incoming batches of messages when operating in a BATCH mode. The UNIX or NT pathname for the directory can be specified with '/' separators (Note do not use '\' in an NT environment). If no path is specified the system will create a directory at authorisation if it does not exist, otherwise the directory must exist. A Batch OFS service can operate using either a specific record within the directory specified here, defined in IN.QUEUE.NAME, or can process any file within the directory if no specific name is specified. Each file may contain one or more records in specified OFS message format. Processed records are written to the directory specified in the OUT.QUEUE.DIR either to the specified OUT.QUEUE.NAME or the same name as the inward record. The batch interface will support both OFS and GTS format messages. Validation Rules: Mandatory if SOURCE.TYPE is BATCH Must be a type 1 or 19 file if an existing directory is defined. |
| 19 | `OFS.SRC.IN.QUEUE.NAME` | `OfsSource_InQueueName` | TField | Yes | Defines the name of a specific input file resident in the input directory to be processed when operating in a BATCH mode. A Batch OFS service can operate using either a specific record within the directory specified here, defined in IN.QUEUE.NAME, or can process any file within the directory if no specific name is specified. Each file may contain one or more records in specified OFS or GTS message format. Processed records are written to the directory specified in the OUT.QUEUE.DIR either to the specified OUT.QUEUE.NAME or the same name as the inward record. The batch interface will support both OFS and GTS format messages. Validation Rules: Mandatory if SOURCE.TYPE is BATCH Must be a type 1 or 19 file if an existing directory is defined. |
| 20 | `OFS.SRC.OUT.QUEUE.DIR` | `OfsSource_OutQueueDir` | TField | Yes | Defines the directory used to store output files, which contain details of completed transactions together with any validation errors or overrrides. The UNIX or NT pathname for the directory can be specified with '/' separators (Note do not use '\' in an NT environment). If no path is specified the system will create a directory at authorisation if it does not exist, otherwise the directory must exist. Each file may contain one or more records in specified OFS message format. Processed records are written to the directory specified in the OUT.QUEUE.DIR either to the specified OUT.QUEUE.NAME or the same name as the inward record. The batch interface will support both OFS and GTS format messages. Validation Rules: Mandatory if SOURCE.TYPE is BATCH Must be a type 1 or 19 file if an existing directory is defined. |
| 21 | `OFS.SRC.OUT.QUEUE.NAME` | `OfsSource_OutQueueName` | TField | Yes | Defines the name of the directory used to hold outgoing batches of messages when operating in a BATCH mode. The UNIX or NT pathname for the directory can be specified with '/' separators (Note do not use '\' in an NT environment). The system will create a directory at authorisation if it does not exist. A Batch OFS service can operate using either a specific record within IN.QUEUE.DIR directory, defined in IN.QUEUE.NAME, or can process any file within the directory if no specific name is specified. Each file may contain one or more records in specified OFS message format. Processed records are written to the directory specified here either to the specific OUT.QUEUE.NAME or the same name as the inward record. The batch interface will support both OFS and GTS format messages. Validation Rules: Mandatory if SOURCE.TYPE is BATCH Must be a type 1 or 19 file if an existing directory is defined. |
| 22 | `OFS.SRC.QUEUE.INIT.RTN` | `OfsSource_QueueInitRtn` | TField | No | Contains the name of the file within the OUT.QUEUE.FILE directory used for Batches of OFS messages. If a file name is specified here all results from the processing of an inward batch of messages will be written to this name. Validation Rules: Optional Input. Up to 12 alphanumeric characters |
| 23 | `OFS.SRC.QUEUE.CLOSE.RTN` | `OfsSource_QueueCloseRtn` | TField | No | Allows a user defined routine to be specified for a particular service. The routine specified here will be executed when a batch service is started. Typically such a routine could intialise a common area or perform some initial communication. Routine Details Arguments : None Validation Rules: Optional Input Must be defined in PGM.FILE as a type S program. |
| 24 | `OFS.SRC.SYNTAX.TYPE` | `OfsSource_SyntaxType` | TField |  | Indicates the syntax type of the messages within batch files in the IN.QUEUE.DIR directory. Validation Rules: Must be either OFS or GTS or XML |
| 25 | `OFS.SRC.LOCAL.REF` | `OfsSource_LocalRef` |  |  |  |
| 26 | `OFS.SRC.GENERIC.USER` | `OfsSource_GenericUser` | TField | Yes | Identifies the T24 USER @ID used by the OFS.ONLINE.MANAGER phantom process. When T24 SMS checking is required, specified by IB.USER.CHECK, this user will provide the SMS profile for all transactions. If the SERVICE.TYPE is TELNET then the T24 USER must have security access to EB.PHANTOM.PH as defined in the user's APPLICATION field. Validation Rules: Mandatory input when SERVICE.TYPE is TELNET Must be a valid T24 USER @ID. Mandatory if IB.USER.CHECK is set to Y |
| 27 | `OFS.SRC.IN.DIR.RTN` | `OfsSource_InDirRtn` | TField | No | Allows a user defined routine to be specified. The routine specified here will be executed when the input directory defined in IN.QUEUE.DIR is empty, i.e. there are no messages to process. Typically such a routine could be used to populate the input directory with OFS messages derived from a third party source. Routine Details Arguments : None Note at the point this routine is called the T24 common variables are available. The following common areas should be included in the routine I_EQUATES, I_COMMON and I_GTS.COMMON. The I_GTS.COMMON contains the following OFS information OFS$LOG.FILE.NAME Log file used for OFS processing OFS$SOURCE.ID ID of the OFS source record OFS$SOURCE.REC Parameter file read from OFS.SOURCE Validation Rules: Optional Input Must be defined in PGM.FILE as a type S program. |
| 28 | `OFS.SRC.VERSION` | `OfsSource_Version` | TField | No | Contains the name of a valid version record, which can be used in association with the routine specified in IN.DIR.RTN to create OFS.MESSAGES. For example a routine to process information for FUNDS.TRANSFER will have a valid FUNDS.TRANSFER version defined here. Validation Rules: Optional input Must be an existing VERSION record. |
| 29 | `OFS.SRC.IB.USER.CHECK` | `OfsSource_IbUserCheck` | TField | No | Specifies whether each transaction supplied to T24 using this service requires the sign on name and password to be validated for external users. For external users the sign on name and passwords are controlled using the T24 applications IB.USER and IB.USER.PASSWORD. These applications allow specification of the individual sign on name and password, the associated CUSTOMER , ACCOUNT and PORTFOLIO numbers accessible to that external user, and the required language and amount formatting options for the user. When external users are defined a generic user profile is required to provide the overall security profile and link to SMS. If this field is defined as Y a GENERIC.USER must be specified. When this field is null or N, SMS checks the USER record for security profile. When field is Y - SMS checks the IB.USER record for security profile. The external user sign on name and password must be supplied with every OFS transaction / enquiry request to allow it to be processed successfully. Validation Rules: Optional input Valid values : Y N or Null |
| 30 | `OFS.SRC.EOD.VALIDATE` | `OfsSource_EodValidate` | TField |  | If set to YES then OFS requests are allowed when the system is not online. Only If not set, then OFS requests made during offline processing are queued. Validation Rules: Blank or "YES" |
| 31 | `OFS.SRC.FIELD.VAL` | `OfsSource_FieldVal` | TField |  | Specifies whether field validation is to take place on the message. Most applications repeat field validations at record commitment and do not need this option. A few older applications rely on the input being edited as it is entered online. OFS field validation can be used to stop certain invalid data getting through to authorised records. YES - - - - - Field validation takes place NO / Validation Rules: YES, NO, or null |
| 32 | `OFS.SRC.ATTRIBUTES` | `OfsSource_Attributes` |  |  |  |
| 33 | `OFS.SRC.SAME.AUTHORISER` | `OfsSource_SameAuthoriser` |  |  |  |
| 34 | `OFS.SRC.CHANNEL` | `OfsSource_Channel` | TField | Yes | A link to the EB.CHANNEL file. This file specifies a channel through which a request can come. Validation Rules: Mandatory Input Must a valid record in the USER application |
| 35 | `OFS.SRC.PSWD.ENCRYPTED` | `OfsSource_PswdEncrypted` | TField |  | Defines if this source will expect the password passed in the OFS request to be in encrypted or Raw format. If Set to YES, the password will be considered as encrypted password for all processing purposes If set to NO or NULL, the password will be considered as raw format for all processing purposes Default is NULL. Validation Rules: Allowed values are YES, NO or NULL. |
| 36 | `OFS.SRC.OFS.MESSAGE.DECRYPT` | `OfsSource_OfsMessageDecrypt` | TField | No | OFS.MESSAGE.DECRYPT is a flag set whether the incoming message is encrypted or not. If yes then this will be decrypted by the information given in DECRYPT.KEY Validation Rules: 1. An optional field. 2. Length: Maximum of 17. |
| 37 | `OFS.SRC.DECRYPT.KEY` | `OfsSource_DecryptKey` | TField | No | The ID of OFS.DECRYPT.KEY record, which will hold the information necessary for DECRYPTION Validation Rules: 1. An optional field. 2. If the OFS.MESSAGE.DECRYPTION field is set then this field should expect a value. 3. This field has a check file validation to OFS.DECRYPT.KEY application. 4. Length: Maximum of 35. |
| 38 | `OFS.SRC.SPL.PROC.PREFIX` | `OfsSource_SplProcPrefix` | TField |  |  |
| 39 | `OFS.SRC.WARMUP.SLEEP` | `OfsSource_WarmupSleep` | TField |  |  |
| 40 | `OFS.SRC.OVERRIDE` | `OfsSource_Override` |  |  |  |
| 41 | `OFS.SRC.RECORD.STATUS` | `OfsSource_RecordStatus` | String |  |  |
| 42 | `OFS.SRC.CURR.NO` | `OfsSource_CurrNo` | String |  |  |
| 43 | `OFS.SRC.INPUTTER` | `OfsSource_Inputter` |  |  |  |
| 44 | `OFS.SRC.DATE.TIME` | `OfsSource_DateTime` |  |  |  |
| 45 | `OFS.SRC.AUTHORISER` | `OfsSource_Authoriser` | String |  |  |
| 46 | `OFS.SRC.CO.CODE` | `OfsSource_CoCode` | String |  |  |
| 47 | `OFS.SRC.DEPT.CODE` | `OfsSource_DeptCode` | String |  |  |
| 48 | `OFS.SRC.AUDITOR.CODE` | `OfsSource_AuditorCode` | String |  |  |
| 49 | `OFS.SRC.AUDIT.DATE.TIME` | `OfsSource_AuditDateTime` | String |  |  |
