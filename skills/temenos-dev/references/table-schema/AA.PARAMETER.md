# AA.PARAMETER — Table Schema

> Source: `INSERTS/I_F.AA.PARAMETER` in `AA_Framework.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `AA.PRM.ARCHIVE.BILLS` | `AaParameter_ArchiveBills` | TField |  | This field denotes the threshold value for settled bills to be moved from LIVE file to AA.BILL.DETAILS.HIST table. If the value stated is say 20, when system creates the 21st bill, it marks the table for archiving. During COB, when the archiving process runs(this could be run online as well ? service name AA.BILL.ARCHIVE.SERVICE), system moves the 20 bills to AA.BILL.DETAILS.HIST table. When moving so, it also removes the content from the live AA.ACCOUNT.DETAILS of these bill references. |
| 2 | `AA.PRM.RETAIN.BILLS` | `AaParameter_RetainBills` | TField |  | Indicates the number of bills that needs to be retained when the archiving is performed. Even though the user might want to archive when the bill count is greater than 20(ARCHIVE.ACTIVITIES field), he might want to retain the last 3 bills so that there is some residual content and the file is not completely empty. Links to ARCHIVE.BILLS and cannot be greater or equal to that. |
| 3 | `AA.PRM.ARCHIVE.ACTIVITIES` | `AaParameter_ArchiveActivities` | TField |  | This field denotes the threshold value for activities to be moved from LIVE file to AA.ACTIVITY.HISTORY.HIST table. If the value stated is say 50, when system crosses the 50th activity, it marks the table for archiving. During COB, when the archiving process runs as part of AA.ARCHIVE.SERVICE, (this could be run as online service as well ? service name AA.AH.ARCHIVE.SERVICE), system moves the 50 activity multi-values to AA.ACTIVITY.HISTORY.HIST table. When backdated activities are performed prior to this date, system automatically restores the content from HIST file where relevant and when the processing is complete, again marks the arrangement for archival which would be picked in COB process or online service. |
| 4 | `AA.PRM.RETAIN.ACTIVITIES` | `AaParameter_RetainActivities` | TField |  | Indicates the number of activities that needs to be retained when the archiving is performed. Even though the user might want to archive when the activity count is greater than 50(ARCHIVE.ACTIVITIES field), he might want to retain the last 5 activities so that there is some residual content and it is not completely empty. Links to ARCHIVE.ACTIVITIES and cannot be greater or equal to that. |
| 5 | `AA.PRM.ACTIVITY.INTEGRITY.CHECK` | `AaParameter_ActivityIntegrityCheck` | TField |  | Indicates whether system needs to run ?AA Integrity checks? when an activity is performed. When this field is flagged, ALL activities performed on ALL arrangements would be checked for integrity during run time of that activity itself(rather than being found in a service later on in COB). It is imperative that as part of integrity, system performs additional checks and when it finds any abnormality, it records the details as part of AA.EXCEPTION.LOG. The default setting is NULL which means no checks and has to be forcefully switched on if the checks have to be done. |
| 6 | `AA.PRM.AGENT.BULK.NO` | `AaParameter_AgentBulkNo` | TField |  | Indicates the count for agent charge activities to be clubbed into a single bill. Based on this field, system will create a charge bill at agent side as consolidated. For Example, If this field is set as 50 then upon reaching 50 charge activities from financial side system agent system will post the consolidated bill for these 50 charges at agent side. This consolidation will happen based on the agent per commission property for the same day. |
| 7 | `AA.PRM.INTEGRITY.CHECK.ROUTINE` | `AaParameter_IntegrityCheckRoutine` |  |  |  |
| 8 | `AA.PRM.ARCHIVE.SAME.DAY.CNT` | `AaParameter_ArchiveSameDayCnt` | TField |  | Help Text for this field is unavailable. Please refer to the T24 User Guides for further information. |
| 9 | `AA.PRM.RETAIN.SAME.DAY.CNT` | `AaParameter_RetainSameDayCnt` | TField |  | Help Text for this field is unavailable. Please refer to the T24 User Guides for further information. |
| 10 | `AA.PRM.RECONSTRUCT.SETTLEMENT` | `AaParameter_ReconstructSettlement` | TField |  | IIndicates whether the settlement processing for settlement account needs to be reversed and replayed with the new or changed amounts/rates. Once we opted the Automatic behavior then the behavior should not be changed to Manual or Null. After setting Automatic at global level(System) then system will not allow to set values at company level. Note:Backdated settlement functionality is enabled only if settlement account is AR/AC Account alone Available options are: NULL/MANUAL - System will behave as existing old settlement process. AUTOMATIC - System will behave in new way where settlement processing is reversed and replayed. |
| 11 | `AA.PRM.OVERRIDE` | `AaParameter_Override` | TField |  |  |
| 12 | `AA.PRM.RECORD.STATUS` | `AaParameter_RecordStatus` | String |  |  |
| 13 | `AA.PRM.CURR.NO` | `AaParameter_CurrNo` | String |  |  |
| 14 | `AA.PRM.INPUTTER` | `AaParameter_Inputter` |  |  |  |
| 15 | `AA.PRM.DATE.TIME` | `AaParameter_DateTime` |  |  |  |
| 16 | `AA.PRM.AUTHORISER` | `AaParameter_Authoriser` | String |  |  |
| 17 | `AA.PRM.CO.CODE` | `AaParameter_CoCode` | String |  |  |
| 18 | `AA.PRM.DEPT.CODE` | `AaParameter_DeptCode` | String |  |  |
| 19 | `AA.PRM.AUDITOR.CODE` | `AaParameter_AuditorCode` | String |  |  |
| 20 | `AA.PRM.AUDIT.DATE.TIME` | `AaParameter_AuditDateTime` | String |  |  |
| 21 | `AA.PRM.SIM.PRODUCT.LINE` | `AaParameter_SimProductLine` |  |  |  |
| 22 | `AA.PRM.SIM.PRODUCT.GROUP` | `AaParameter_SimProductGroup` |  |  |  |
| 23 | `AA.PRM.SIM.PRODUCT` | `AaParameter_SimProduct` |  |  |  |
| 24 | `AA.PRM.SIM.RETENTION.PERIOD` | `AaParameter_SimRetentionPeriod` |  |  |  |
| 25 | `AA.PRM.BANK.GO.LIVE.STATUS` | `AaParameter_BankGoLiveStatus` | TField |  | This field indicates whether the bank is in the Live or Implementation phase. Allowed value is LIVE or can be left blank LIVE - Indicates that the bank is live. Blank - Indicates bank is in the implementation stage. If the Bank is in the implementation stage, it is prudent to identify abnormalities arising out of the lifecycle scenarios tested. Any abnormalities found at this stage will result in an error so that these are reported upfront. However, the Post Live stage, it refers to the ACTIVITY.INTEGRITY.CHECK field and decides if any abnormalities should be stopped and reported or should be logged in AA.EXCEPTION.LOG while allowing the transaction to continue. |
| 26 | `AA.PRM.EXCLUDE.GROUP` | `AaParameter_ExcludeGroup` |  |  |  |
| 27 | `AA.PRM.ENABLE.SYS.GEN.MSG` | `AaParameter_EnableSysGenMsg` |  |  |  |
| 28 | `AA.PRM.EMIT.EVENT` | `AaParameter_EmitEvent` | TField |  | Indicates whether events need to be raised using the MS.EVENT.CLASS/MS.EVENT framework. The payloads and the events for every business process is configured under AA.ACTIVITY.CLASS/AA.ACTIVITY. The flag here is a switch to indicate whether events need to be emitted and banks which don?t need to emit events could leave this field as NULL. YES ? Indicates event emission would be attempted based on MS.EVENT.CLASS/MS.EVENT configurations linked through Activity class/Activity NULL ? Indicates no event emission would happen for AA. |
| 29 | `AA.PRM.ACT.THRESHOLD.PERIOD` | `AaParameter_ActThresholdPeriod` | TField |  | New field to define period values so that the activity history is moved to .HIST on reaching the threshold period. Valid Period field. Valid Suffixes are W- Week, M-Month, Y- Year and D-Days |
| 30 | `AA.PRM.ACT.RETAIN.PERIOD` | `AaParameter_ActRetainPeriod` | TField |  | New field to define period value that needs to be retained when the archiving is performed. Valid Period field. Valid Suffixes are W- Week, M-Month, Y- Year and D-Days for e.g: Current system date is 23 Dec 2020.New arrangement is triggered on 13 Dec 2020 Threshold Period is defined as 10D and Retain period is defined as 1D For the above setup,system will move the activities from 13 Dec 2020 to 22 Dec 2020 to .HIST. |
| 31 | `AA.PRM.ARCHIVE.PERIOD` | `AaParameter_ArchivePeriod` | TField |  | This field denotes the threshold value for Interest accrual period to be moved from LIVE file to AA.ACTIVITY.HISTORY.HIST table. If the value stated is say 50, when system crosses the 50th periods, it marks the table for archiving. During COB, when the archiving process runs as part of AA.ARCHIVE.SERVICE, system moves the 50 Interest accrual period multi-values to AA.ACTIVITY.HISTORY.HIST table. When backdated activities are performed prior to this date, system automatically restores the content from HIST file where relevant and when the processing is complete, again marks the arrangement for archival which would be picked in COB process or online service.It archives FROM-TO bucket corresponding to the period. |
| 32 | `AA.PRM.RETAIN.PERIOD` | `AaParameter_RetainPeriod` | TField |  | Indicates the number of Interest Accrual Period that needs to be retained when the archiving is performed. Even though the user might want to archive when the Period count is greater than 50(ARCHIVE.ACTIVITIES field), he might want to retain the last 5 Interest Accrual Periods so that there is some residual content and it is not completely empty.It archives FROM-TO bucket corresponding to the period. |
| 33 | `AA.PRM.EXC.ELIGIBILITY.CHECK` | `AaParameter_ExcEligibilityCheck` |  |  |  |
| 34 | `AA.PRM.IGNORE.SIM` | `AaParameter_IgnoreSim` | TField |  |  |
| 35 | `AA.PRM.SKIP.ITERATE` | `AaParameter_SkipIterate` | TField |  | This field is used to skip the iterate logic to arrive the calc amount in case of blended payment types if it is set to YES It is purely for technical purpose and no impact to the functionality where the calc amount is arrived based on core routine calculation for simpler definition in payment schedule condition In case of complex definitions like CAPITALISE payment method, Forward dated condition, ACTUAL.AMT definition, CHARGE part of blended properties with Charge routine attached, INTEREST property defined with BAND or LEVEL would still require iteration to be processed to arrive accurate convergence with payment amount though the SKIP.ITERATE flag is set to YES. |
| 36 | `AA.PRM.EXCLUSION.PROPERTY` | `AaParameter_ExclusionProperty` |  |  |  |
| 37 | `AA.PRM.SIM.ACCOUNT.REF` | `AaParameter_SimAccountRef` | TField |  |  |
| 38 | `AA.PRM.PARTICIPANT.OFFLINE` | `AaParameter_ParticipantOffline` | TField |  |  |
| 39 | `AA.PRM.UPDATE.PARTICIPANT.ARR` | `AaParameter_UpdateParticipantArr` | TField |  | Indicates whether to update the AA.PARTICIPANT.ARRANNGEMENT table with the arrangement id and account id. Allowed values are NONE,NO,YES. The table AA.PARTICIPANT.ARRANGEMENT will not be updated with the arrangement id and account id in two cases 1.If the UPDATE.PARTICIPANT.ARR is set as NO and the share transfer property class has defined with the pool id. 2.If the UPDATE.PARTICIPANT.ARR is set as NO and the Participant property type as Sub.Participant. |
