# ND.PARAMETER — Table Schema

> Source: `INSERTS/I_F.ND.PARAMETER` in `ND_Config.jar` (positions/aliases via `pipeline/insert_parse.py`); type/mandatory/description via `pipeline/html_parse.py` from JavaDoc HTML.
> Multivalue status is NOT captured here — cross-check `com/temenos/t24/api/records/` per the MV-field-detection rule in `skills/temenos-dev/SKILL.md` before treating any field as single-value.
> Type/mandatory are inferred from JavaDoc free text and may be blank where the HTML gave no clear signal — do not treat a blank as "optional".

| Position | Field Name | Java Alias | Type | Mandatory | Description |
|----------|------------|------------|------|-----------|--------------|
| 1 | `ND.PARA.NDF.ALLOW.CCY` | `NdParameter_NdfAllowCcy` |  |  |  |
| 2 | `ND.PARA.DEF.FIX.DAYS` | `NdParameter_DefFixDays` | TField | Yes | Indicates a default number of business days prior to the settlement date for NDF fixing for all currencies which are not listed in multi value fields FIXING.CCY Validation Rules: 0 to 99. Input in this field is mandatory. |
| 3 | `ND.PARA.FIXING.CCY` | `NdParameter_FixingCcy` |  |  |  |
| 4 | `ND.PARA.FIXING.DAYS` | `NdParameter_FixingDays` |  |  |  |
| 5 | `ND.PARA.FIXING.REP.DAYS` | `NdParameter_FixingRepDays` | TField | Yes | Identifies how many Working days before the fixing date the user want to see the unfixed NDF contracts (of FIXED type) to appear in the Enquiry ND.EOD.FIXED and COB Report ND.EOD.FIXED. The NDF contracts will then appear everyday in the report from the first day of appearance to their fixing dates. The days will be calculated for Settlement Currency. Validation Rules: 0 to 99. Input in this field is mandatory. |
| 6 | `ND.PARA.OPTION.REP.DAYS` | `NdParameter_OptionRepDays` | TField | Yes | Identifies how many Working days before the fixing date the user want to see the unfixed NDF contracts (of OPTION type) to appear in the Enquiry ND.EOD.FIXED and COB Report ND.EOD.FIXED. The NDF contracts will then appear everyday in the report from the first day of appearance to their fixing dates. The days will be calculated for Settlement Currency. Validation Rules: 0 to 99. Input in this field is mandatory. |
| 7 | `ND.PARA.DAYS.POST.MATURE` | `NdParameter_DaysPostMature` | TField | Yes | Defines the number of days after maturity that a NDF contract will be written to history file. Validation Rules: 0 to 99. Input in this field is mandatory. |
| 8 | `ND.PARA.OPN.BUY.CODE` | `NdParameter_OpnBuyCode` | TField | Yes | Identifies transaction code to indicate buy booking of ND.DEAL. Validation Rules: Mandatory input. Must be a valid definition on the RE.TXN.CODE file. Once record has been authorized this field cannot be changed. |
| 9 | `ND.PARA.OPN.SELL.CODE` | `NdParameter_OpnSellCode` | TField | Yes | Identifies transaction code to indicate sell booking of ND.DEAL. Validation Rules: Mandatory input. Must be a valid definition on the RE.TXN.CODE file. Once record has been authorized this field cannot be changed. |
| 10 | `ND.PARA.FIXED.BUY.CODE` | `NdParameter_FixedBuyCode` | TField | Yes | Identifies transaction code to indicate buy fixing of ND.DEAL. Validation Rules: Mandatory input. Must be a valid definition on the RE.TXN.CODE file. Once record has been authorized this field cannot be changed. |
| 11 | `ND.PARA.FIXED.SELL.CODE` | `NdParameter_FixedSellCode` | TField | Yes | Identifies transaction code to indicate sell fixing of ND.DEAL. Validation Rules: Mandatory input. Must be a valid definition on the RE.TXN.CODE file. Once record has been authorized this field cannot be changed. |
| 12 | `ND.PARA.NDF.REV.CODE` | `NdParameter_NdfRevCode` | TField | Yes | Identifies transaction code to indicate reversal of ND.DEAL. Validation Rules: Mandatory input. Must be a valid definition on the RE.TXN.CODE file. Once record has been authorized this field cannot be changed. |
| 13 | `ND.PARA.SETL.CR.CODE` | `NdParameter_SetlCrCode` | TField | Yes | Signifies the transaction code to be used for raising a credit accounting entry for settlement NDF transaction. Validation Rules: Mandatory input. Must be a valid transaction code set up on the TRANSACTION file. Once record has been authorized this field cannot be changed. |
| 14 | `ND.PARA.SETL.DR.CODE` | `NdParameter_SetlDrCode` | TField | Yes | Signifies the transaction code to be used for raising a debit accounting entry for settlement NDF transaction. Validation Rules: Mandatory input. Must be a valid transaction code set up on the TRANSACTION file. Once record has been authorized this field cannot be changed. |
| 15 | `ND.PARA.REV.CR.CODE` | `NdParameter_RevCrCode` | TField | Yes | Signifies the transaction code to be used for raising a credit accounting entry when NDF settlement is cancelled. Validation Rules: Mandatory input. Must be a valid transaction code set up on the TRANSACTION file. Once record has been authorized this field cannot be changed. |
| 16 | `ND.PARA.REV.DR.CODE` | `NdParameter_RevDrCode` | TField | Yes | Signifies the transaction code to be used for raising a debit accounting entry when NDF settlement is cancelled. Validation Rules: Mandatory input. Must be a valid transaction code set up on the TRANSACTION file. Once record has been authorized this field cannot be changed. |
| 17 | `ND.PARA.AMORT.CR.CODE` | `NdParameter_AmortCrCode` | TField | Yes | Signifies the transaction code to be used for raising a credit accounting entry for amortization. Validation Rules: Mandatory input. Must be a valid transaction code set up on the TRANSACTION file. Once record has been authorized this field cannot be changed. |
| 18 | `ND.PARA.AMORT.DR.CODE` | `NdParameter_AmortDrCode` | TField | Yes | Signifies the transaction code to be used for raising a debit accounting entry for amortization. Validation Rules: Mandatory input. Must be a valid transaction code set up on the TRANSACTION file. Once record has been authorized this field cannot be changed. |
| 19 | `ND.PARA.AMORT.INC.CATEG` | `NdParameter_AmortIncCateg` | TField | Yes | Defines the category code for amortizing discount income. Validation Rules: Mandatory input. Must be a valid category code on the CATEGORY file in range 50000-59999. Once record has been authorized this field cannot be changed. |
| 20 | `ND.PARA.AMORT.EXP.CATEG` | `NdParameter_AmortExpCateg` | TField | Yes | Defines the category code for amortizing discount expense. Validation Rules: Mandatory input. Must be a valid category code on the CATEGORY file in range 50000-59999. Once record has been authorized this field cannot be changed. |
| 21 | `ND.PARA.NEW.ACTIV.CODE` | `NdParameter_NewActivCode` | TField | Yes | Defines the activity code for new NDF contract. Validation Rules: Mandatory input. Must be a valid activity code set up on the EB.ACTIVITY file. |
| 22 | `ND.PARA.AMEND.ACTIV.CODE` | `NdParameter_AmendActivCode` | TField | Yes | Defines the activity code for amended NDF contract. Validation Rules: Mandatory input. Must be a valid activity code set up on the EB.ACTIVITY file. |
| 23 | `ND.PARA.FIXED.ACTIV.CODE` | `NdParameter_FixedActivCode` | TField | Yes | Defines the activity code for NDF contract which is fixed. Validation Rules: Mandatory input. Must be a valid activity code set up on the EB.ACTIVITY file. |
| 24 | `ND.PARA.SETTL.ACTIV.CODE` | `NdParameter_SettlActivCode` | TField | Yes | Defines the activity code for activity raised on settlement date. Validation Rules: Mandatory input. Must be a valid activity code set up on the EB.ACTIVITY file. |
| 25 | `ND.PARA.DEFAULT.MSG.CLASS` | `NdParameter_DefaultMsgClass` | TField | Yes | Identifies a default message class for ND.DEAL. Validation Rules: Mandatory input. Must be a valid entry in EB.MESSAGE.CLASS file. |
| 26 | `ND.PARA.REVERSE.ACTIV.CODE` | `NdParameter_ReverseActivCode` | TField | Yes | Holds the activity code needed for generating a swift confirmation message for cancellation. Validation Rules: Mandatory input. Must be a valid activity code set up on the EB.ACTIVITY file. |
| 27 | `ND.PARA.FTT.COLL.CPTY.TAX` | `NdParameter_FttCollCptyTax` | TField |  | Provides the option to T24 bank to collect the Financial Transaction Tax payable by the Counterparty also. If yes, accounting entries are raised to collect counterparty's tax wherein customer Account is debited and Tax suspense Account is credited. The values No or Null means T24 bank does not collect the FTT payable by the counterparty or in other words the counterparty himself remits the FTT. This option is provided on the premise that where the counterparty is a customer of the bank ( not an interbank counterparty) the T24 bank assumes responsibility to collect and pay the FTT payable by the customer Validation Rules: Values allowed are Yes, No or Null. Value present in this field is defaulted to FTT.COLL.CPTY.TAX field on ND.DEAL which can be overridden by the User. |
| 28 | `ND.PARA.MKT.EX.DR.CODE` | `NdParameter_MktExDrCode` | TField | Yes | Signifies the transaction code to be used for raising a debit accounting entry for marketing exchange. The accounting entries for marketing exchange will be raised only if both the debit and credit transaction codes are defined. Validation Rules: Non - mandatory Must be a valid transaction code set up on the TRANSACTION file |
| 29 | `ND.PARA.MKT.EX.CR.CODE` | `NdParameter_MktExCrCode` | TField | Yes | Signifies the transaction code to be used for raising a credit accounting entry for marketing exchange. The accounting entries for marketing exchange will be raised only if both the debit and credit transaction code are defined. Validation Rules: Non - mandatory Must be a valid transaction code set up on the TRANSACTION file |
| 30 | `ND.PARA.DEFAULT.OPTIONJ` | `NdParameter_DefaultOptionJ` |  |  |  |
| 31 | `ND.PARA.LOCAL.REF` | `NdParameter_LocalRef` |  |  |  |
| 32 | `ND.PARA.OVERRIDE` | `NdParameter_Override` |  |  |  |
| 33 | `ND.PARA.RECORD.STATUS` | `NdParameter_RecordStatus` | String |  |  |
| 34 | `ND.PARA.CURR.NO` | `NdParameter_CurrNo` | String |  |  |
| 35 | `ND.PARA.INPUTTER` | `NdParameter_Inputter` |  |  |  |
| 36 | `ND.PARA.DATE.TIME` | `NdParameter_DateTime` |  |  |  |
| 37 | `ND.PARA.AUTHORISER` | `NdParameter_Authoriser` | String |  |  |
| 38 | `ND.PARA.CO.CODE` | `NdParameter_CoCode` | String |  |  |
| 39 | `ND.PARA.DEPT.CODE` | `NdParameter_DeptCode` | String |  |  |
| 40 | `ND.PARA.AUDITOR.CODE` | `NdParameter_AuditorCode` | String |  |  |
| 41 | `ND.PARA.AUDIT.DATE.TIME` | `NdParameter_AuditDateTime` | String |  |  |
